# Registro de decisiones

Claude Code: anota aquí cada decisión técnica relevante (fecha, bloque, decisión, motivo).

## 2026-07-06 — Bloque 0.1 Scaffolding

- **Versiones pinneadas**: `next@15.5.20` (última 15.x; 16.x existe pero SPECS fija Next 15), `tailwindcss@4.3.2`, `next-intl@4.13.1`, `shadcn@4.13.0` (estilo `base-nova`, usa `@base-ui/react` en vez de Radix directo), `vitest@4.1.10`, `@playwright/test@1.61.1`. Package manager: pnpm (`packageManager` fijado en `package.json` para reproducibilidad en CI).
- **Supabase remoto pendiente**: `supabase init` + `supabase start` funcionan en local (ver `supabase status` para credenciales). El link a un proyecto remoto (`supabase link --project-ref <ref>`) requiere `supabase login` interactivo (OAuth) con la cuenta del usuario — no se pudo automatizar. Queda como acción manual antes o durante el bloque 0.2: el usuario crea el proyecto en el dashboard de Supabase, pasa el project ref, y se ejecuta el link.
- **Estructura de rutas**: todo el App Router vive bajo `src/app/[locale]/` (patrón estándar de `next-intl`); no hay `src/app/layout.tsx` propio — el layout de `[locale]` actúa como raíz. Middleware en `middleware.ts` (raíz del repo) gestiona el prefijo de idioma; default `es`, soportados `es`/`en`.
- **Tokens de diseño**: los tokens de `docs/mockups.html` (`--bg`, `--surface`, `--ink`, `--ink-soft`, `--line`, `--primary`, `--primary-soft`, `--amber`, `--amber-soft`, `--red`, `--red-soft`) se mapearon 1:1 a las variables que shadcn/ui espera (`--background`, `--foreground`, `--border`, etc.) en `src/app/globals.css`, para que los componentes de shadcn hereden la paleta de marca sin theming adicional. Solo se implementó el tema claro (el mockup no define modo oscuro); se puede añadir `.dark` en un bloque posterior si se decide soportarlo.
- **Alcance de tests en 0.1**: un test unitario real por pieza no trivial (`cn()` de shadcn, config de `routing.ts`) y un smoke e2e de Playwright que visita `/es` y `/en` y verifica el cambio de idioma — no hay lógica de negocio todavía, así que no se fuerza cobertura artificial. CI corre `lint`, `typecheck`, `test` (Vitest) y un job separado con Playwright + Chromium.
- **`@supabase/ssr` / `@supabase/supabase-js`**: no se instalan en 0.1 para evitar dependencias sin uso; se instalarán en el bloque 0.2 cuando se escriba el cliente real de Supabase Auth.
- **Supabase local — servicios no críticos parados**: `imgproxy` y `pooler` (pgbouncer) quedan detenidos tras `supabase start` en esta máquina (no se usan hasta fases posteriores: transformación de imágenes y connection pooling para serverless). El resto (db, auth, rest, storage, studio, kong, realtime) están `healthy`. Credenciales locales en `.env.local.example`.

## 2026-07-06 — Bloque 0.2 Multi-tenancy y Auth — diseño aprobado

- **Proyecto remoto enlazado**: `supabase link --project-ref mkrsicuvhnmljpurtwun` ejecutado por el usuario (login OAuth vía `supabase login` no puede correr en el entorno no-TTY de la herramienta; el usuario lo corrió en su propia terminal). Confirmado con `supabase migration list` (conecta, 0 migraciones remotas).
- **Alta de organización + primer usuario: un único trigger, no una RPC**: `handle_new_user()` (`SECURITY DEFINER`, `set search_path = ''`) se dispara `AFTER INSERT ON auth.users`. `NEW.id` es siempre el id real que GoTrue acaba de crear — no es un parámetro que un cliente pueda falsificar, así que no aplica una comprobación `auth.uid() = auth_id` (esa comprobación tiene sentido en una RPC invocable por el cliente, no en un trigger de tabla). La superficie de ataque real es `raw_user_meta_data`, y se cierra así:
  - Signup de org nueva: metadata solo lleva `org_name/org_slug/default_currency/locale` (nada preexistente que suplantar) → el trigger crea `organizations` y el `users` con rol `org_admin`, todo en la misma transacción.
  - Aceptar invitación: metadata solo lleva `invitation_token_hash` → el trigger busca en `invitations` por ese hash, exige `used_at is null`, `expires_at > now()` y `lower(email) = lower(new.email)`; `org_id`/`role` salen de la fila de invitación validada, nunca de metadata del cliente.
- **`organizations`**: INSERT bloqueado a todo cliente (`with check (false)`, solo lo hace el trigger); DELETE bloqueado explícitamente (`using (false)`) — el borrado de una org será un flujo de offboarding futuro, no un DELETE directo.
- **`invitations`**: sin políticas de INSERT/UPDATE directas para el cliente. Se gestionan con dos RPC `SECURITY DEFINER`: `create_invitation()` (valida que el caller es `org_admin`/`it_admin` de su org, inserta la invitación + fila en `audit_log`) y `revoke_invitation()` (mismo chequeo de rol, exige `used_at is null`, borra + audita). `used_at` se marca solo dentro de `handle_new_user()`, en la misma transacción que crea la cuenta — evita una condición de carrera entre "crear usuario" y "marcar invitación usada" como pasos separados.
- **Token de invitación**: se genera y se hashea (SHA-256 hex) en el Server Action (Node `crypto`), nunca en SQL. Solo `token_hash` se guarda en la tabla; el token en claro solo vive en el link del email.
- **`search_path` hardening**: todas las funciones `SECURITY DEFINER` (`current_org_id`, `current_user_role`, `handle_new_user`, `protect_users_columns`, `create_invitation`, `revoke_invitation`) fijan `set search_path = ''` y referencian tablas con schema explícito (`public.users`, `auth.uid()`).
- **`users`**: `org_id`/`role` protegidos de auto-escalado con un trigger `BEFORE UPDATE` (`protect_users_columns()`) — `org_id` es inmutable siempre; `role` solo lo puede cambiar quien ya es `org_admin` de esa org. RLS no puede restringir columnas individuales, de ahí el trigger en vez de solo políticas.
- **`department_id` diferido**: no se añade columna en 0.2; se añadirá en la migración de 0.3 cuando exista `departments` (impacto mínimo, evita una columna sin uso ni FK during 0.2).
- **`audit_log` añadido en 0.2, no pedido explícitamente en TASKS.md**: CLAUDE.md marca como no negociable que toda mutación de negocio escriba en `audit_log`, y la creación de organización/invitaciones son las primeras mutaciones de negocio del código — se crea aquí una tabla mínima (RLS: SELECT solo para `org_admin`/`it_admin` de su org; INSERT/UPDATE/DELETE bloqueados a todo cliente, solo escriben las funciones `SECURITY DEFINER` de arriba) en vez de dejar el bloque 0 violando esa regla desde el primer commit.
- **Google OAuth — límite conocido**: el código implementa `signInWithOAuth({provider:'google'})`, pero activarlo de verdad requiere configurar un Client ID/Secret real de Google Cloud Console en el dashboard de Supabase (Authentication → Providers → Google) del proyecto remoto — no es algo que se pueda automatizar por migración ni CLI; queda como acción manual pendiente del usuario.
- **Invitaciones por email — Resend mínimo, no las plantillas bilingües de 2.2**: se instaló `resend` y `sendInvitationEmail()` (`src/features/auth/email.ts`) envía un email de texto plano si `RESEND_API_KEY` está configurada; si no, loguea el enlace de invitación en consola (fallback de dev, no rompe el flujo sin clave real). El sistema de plantillas bilingües completo es explícitamente el alcance del bloque 2.2.
- **Cliente de invitaciones sin `service_role`**: `create_invitation`/`revoke_invitation` son RPC `SECURITY DEFINER` invocadas con la sesión normal del usuario autenticado (no con la service role key) — el chequeo de permisos vive dentro de la función. Esto evita tener que exponer/gestionar la service role key en el código de la app para este flujo.
- **`supabase start` local inestable en Windows/Docker Desktop**: los primeros intentos de `supabase start`/`db reset` fallaron por condiciones de carrera al recrear contenedores (`supabase_vector_stackly` con nombre en conflicto) y por un falso negativo del healthcheck de `storage` (el contenedor arrancaba bien según sus logs, pero el CLI lo marcaba `unhealthy`). Solución: `supabase stop` + `docker rm -f` de cualquier contenedor `supabase_*` colgado antes de reintentar, y `--ignore-health-check` en `supabase start` cuando el log del contenedor ya confirma "Started Successfully".
- **Verificación de RLS en remoto — método real usado**: la CLI 2.75 no tiene un comando para ejecutar SQL arbitrario contra el proyecto enlazado. Se usó `supabase db dump --dry-run` para obtener las credenciales efímeras (`PGHOST/PGUSER/PGPASSWORD`, rotadas por invocación vía el access token de `supabase login`) que la propia CLI genera internamente, y con ellas se corrió la query real de `pg_tables`/`pg_policies` con el `psql` del contenedor local de Postgres contra el proyecto remoto. Resultado: `rowsecurity = true` en las 4 tablas y las 17 políticas esperadas presentes — confirmado también por un segundo método independiente (`supabase db dump --schema public`, que muestra los mismos `ENABLE ROW LEVEL SECURITY`/`CREATE POLICY` verbatim).
- **Ciclo rojo→verde de aislamiento — cómo se hizo**: se rompió a propósito la política `organizations_select` en la base LOCAL (`using (true)` en vez de `using (id = current_org_id())`) directamente por SQL, sin tocar el archivo de migración; `pnpm test` falló mostrando que la org A leía la fila `organizations` de la org B. Se restauró la política original y `pnpm test` volvió a pasar (7/7). El test vive en `src/features/auth/rls-isolation.test.ts`, crea dos organizaciones reales vía `signUp()` (ejercitando el trigger real, no solo SQL directo) y tiene un guard que aborta si `NEXT_PUBLIC_SUPABASE_URL` no apunta a `127.0.0.1`/`localhost` — nunca debe correr contra el proyecto remoto.

## 2026-07-07 — Bloque 0.3 Usuarios y departamentos

- **Google OAuth — hallazgo antes de empezar el bloque**: se pidió verificar el login con Google configurado en el dashboard de Supabase. El código (`signInWithGoogle` en `src/features/auth/actions.ts`, callback en `src/app/api/auth/callback/route.ts`) es correcto (PKCE estándar). Pero `.env.local` apunta al Supabase **local** (`127.0.0.1:54321`) y `supabase/config.toml` no tiene ninguna sección `[auth.external.google]` habilitada — el Google OAuth configurado vive solo en el dashboard del proyecto **remoto** (`mkrsicuvhnmljpurtwun`), infraestructura distinta a la que usa `pnpm dev`. No se puede verificar el consentimiento real de Google de forma automatizada (requiere una cuenta Google real y clic humano). Queda pendiente de prueba manual por el usuario, igual que se documentó como límite conocido en el bloque 0.2.
- **`departments` — mutaciones solo vía RPC, sin excepción para `org_admin`**: a diferencia de `invitations` (0.2), donde `org_admin`/`it_admin` comparten privilegios, SPECS §5 da departamentos/usuarios/roles **exclusivamente** a `org_admin`. Todas las mutaciones (`create_department`, `update_department`, `delete_department`, `update_user_role`, `update_user_department`) son RPCs `SECURITY DEFINER` que comprueban `role = 'org_admin'` del caller y escriben en `audit_log` en la misma transacción — ninguna tiene política RLS directa de insert/update/delete (mismo patrón que `invitations`/`audit_log`).
- **`manager_user_id` sin restricción de `role`**: se decidió NO exigir a nivel de base de datos que el usuario asignado como manager de un departamento tenga `role = 'manager'` — solo que pertenezca a la misma org. El motor de aprobaciones (bloque 3.2) resolverá el aprobador de un departamento vía `manager_user_id` independientemente del `role` literal de esa persona (podría ser un `org_admin` cubriendo un departamento pequeño); forzar `role='manager'` sería una restricción artificial no pedida por SPECS.
- **Refuerzo de `protect_users_columns()` — blindaje total de auditoría, no solo para no-admins**: el trigger de 0.2 ya impedía que un no-`org_admin` cambiara `role` directamente, pero dejaba pasar el cambio si el caller SÍ era `org_admin` — sin garantizar que esa mutación pasara por una ruta auditada. Se cambió a un guard incondicional: cualquier cambio directo a `role` o `department_id` (incluso por `org_admin`) lanza excepción, a menos que la transacción actual tenga activo `set_config('stackly.bypass_user_column_guard', 'on', true)` — flag que solo ponen las RPCs `update_user_role`/`update_user_department`, y que es local a la transacción (se resetea solo). Esto cierra el hueco de auditoría: es imposible que un cambio de rol o departamento llegue a la tabla sin pasar por la RPC que escribe `audit_log`. Verificado con un test explícito (`permissions.test.ts`) que confirma que ni siquiera `org_admin` puede hacer `update({role: ...})` directo.
- **`ActionResult` extraído a `src/lib/action-result.ts`**: antes vivía solo en `src/features/auth/actions.ts`; con las nuevas features `departments` y `users` reutilizándolo, se movió a un módulo compartido. *Corrección posterior (ver entrada de hoy más abajo): NO se re-exporta desde `auth/actions.ts` — un archivo `"use server"` solo puede exportar funciones async, y ese re-export de tipo rompía el build de producción.*
- **Sin UI de shell compartido todavía**: las páginas `team/departments` y `team/members` siguen el mismo patrón standalone que `team/invite` (0.2) — verificación de rol en el Server Component, sin sidebar/nav, porque eso es el alcance del bloque 0.4.
- **Tests de la matriz de permisos — cómo se crean los 5 roles**: `src/features/users/permissions.test.ts` sigue el patrón de `rls-isolation.test.ts` (guardado a Supabase local, usuarios reales). Como `signUp()` directo solo crea el primer usuario de una org (siempre `org_admin`), los otros 4 roles se añaden a la MISMA org invitando vía la RPC `create_invitation` (con el rol deseado) y aceptando con un segundo `signUp()` que lleva `invitation_token_hash` en la metadata — sin necesidad de generar un hash real, porque el trigger compara el valor de metadata tal cual contra `invitations.token_hash`.

## 2026-07-07 — Estabilización de build/CI y migración de entorno a remoto

- **Causa raíz del build roto (bloqueaba Railway)**: `src/features/auth/actions.ts` tenía `export type { ActionResult } from "@/lib/action-result";` en un archivo con directiva `"use server"`. Next.js exige que un módulo `"use server"` exporte ÚNICAMENTE funciones async — cualquier otro export (un tipo, una constante) invalida el módulo entero en el bundle de servidor, y cualquier consumidor que importe de él (incluidas las funciones async legítimas) revienta con "export doesn't exist". `pnpm lint`/`pnpm typecheck`/`pnpm test` no lo detectan porque ninguno compila el bundle de producción de Next.js — solo `pnpm build` (o `next build`) ejecuta esa validación. Arreglo: se eliminó la línea; los tres archivos `"use server"` del repo (`auth/actions.ts`, `departments/actions.ts`, `users/actions.ts`) ahora exportan solo funciones `async`. Ninguno de sus consumidores importaba `ActionResult` desde `auth/actions.ts` (solo se usaba como tipo interno de retorno), así que no hizo falta tocar imports en componentes.
- **`pnpm build` añadido al job de CI** (`.github/workflows/ci.yml`, job "Lint, typecheck, unit tests"), junto a lint/typecheck/test — es el único paso que habría detectado este bug antes de llegar a Railway. Verificado que el build no necesita variables de entorno de Supabase para completarse (se probó moviendo `.env.local` fuera y corriendo `pnpm build` en limpio), así que no hace falta ningún secret nuevo en el workflow.
- **Hallazgo (resuelto más abajo, ver entrada del mismo día)**: `pnpm test` YA fallaba en CI desde el bloque 0.2 (confirmado con `gh run list`/`gh run view`) — el job no levanta Supabase local ni define `NEXT_PUBLIC_SUPABASE_URL`, así que `rls-isolation.test.ts`/`permissions.test.ts` abortan por su propio guard ("apunta a undefined"). Lint y typecheck sí pasaban, dando una falsa sensación de CI verde.
- **Entorno de desarrollo pasa a ser el proyecto remoto**: `.env.local` ahora apunta a `https://mkrsicuvhnmljpurtwun.supabase.co` con el `anon`/`publishable` key del dashboard (confirmado con `curl` a `/rest/v1/organizations` → 200). El anon key se pidió directamente al usuario porque `supabase projects api-keys` imprime también el `service_role` key y el sandbox bloquea esa combinación por materialización de credenciales; no había ninguna vía local con permisos para leerlo de forma segura (ni `railway` ni `gh secret`/`gh variable` tenían sesión o valores). `.env.local.example` se actualizó para reflejar el remoto por defecto.
- **Los tests de aislamiento/permisos NECESITAN seguir corriendo contra Supabase local** (crean tenants reales vía `signUp()`; no deben tocar el proyecto remoto/de producción). Solución limpia, no un parche: Vite resuelve `mode="test"` cuando se ejecuta vía `vitest run`, y su orden de precedencia de archivos de entorno (`.env` < `.env.local` < `.env.test` < `.env.test.local`) hace que un `.env.test.local` (gitignored, no commiteado) con las credenciales del Supabase LOCAL gane sobre `.env.local` (remoto) solo durante `pnpm test`. `pnpm dev`/`pnpm build` siguen usando `.env.local` (remoto) sin cambios. Documentado en `CLAUDE.md`.
- **Flakiness observada, no relacionada con el código**: en al menos dos ocasiones, la primera ejecución de `pnpm test` tras un rato de inactividad falló con `PGRST303 "JWT issued at future"` en el contenedor local de PostgREST, aunque los relojes de host/Postgres/GoTrue/Kong coincidían al segundo (probablemente un desfase transitorio de reloj de la VM de WSL2/Docker Desktop al reanudar). Reintentar `pnpm test` inmediatamente después siempre lo resolvió (26/26 en verde). Si vuelve a aparecer, reintentar antes de investigar más.
- **Google OAuth aplazado al bloque 4.2**: el código sigue existiendo (bloque 0.2) pero el botón "Continuar con Google" en login/signup ahora se oculta tras el feature flag `NEXT_PUBLIC_FEATURE_GOOGLE_OAUTH` (`src/lib/feature-flags.ts`, default `false`/no seteado). `docs/TASKS.md`: la mención de Google OAuth se quitó del checklist ya marcado de 0.2 y se añadió como ítem nuevo bajo 4.2. Motivo: no bloquear el resto del roadmap en un login social que requiere configuración manual de Google Cloud Console/dashboard de Supabase que no se puede automatizar ni verificar por este medio.
- **Migración `0002_departments.sql` aplicada al proyecto remoto** con `supabase db push` (sin prompt de contraseña — la CLI ya tenía sesión enlazada desde el bloque 0.2). Verificado con `supabase db dump --schema public --linked` (dump de esquema, sin credenciales en la salida): `departments` tiene `ENABLE ROW LEVEL SECURITY` y sus 4 políticas (`departments_select/no_insert/no_update/no_delete`), y las 5 RPCs (`create_department`, `update_department`, `delete_department`, `update_user_role`, `update_user_department`) más `protect_users_columns()` reforzado existen en el remoto — mismo método de verificación que en el bloque 0.2.

## 2026-07-07 — Supabase local dentro del job de CI (tests de aislamiento/permisos ya no se skippean)

- **Decisión**: en vez de separar unit tests de tests de integración, se levanta Supabase local DENTRO del job "Lint, typecheck, unit tests" de `.github/workflows/ci.yml`, con la action oficial `supabase/setup-cli@v3` (pineada a `2.75.0`, la misma versión usada en desarrollo local, para que los flags de `start` se comporten igual en ambos sitios).
- **Solo 4 servicios, no `supabase start` completo**: `supabase start -x realtime,storage-api,imgproxy,mailpit,postgres-meta,studio,edge-runtime,logflare,vector,supavisor` — deja corriendo únicamente `db` (Postgres), `auth` (GoTrue), `api` (PostgREST) y `gateway` (Kong, el único puerto `:54321` que usan los tests). Verificado en local: con estos 4 servicios los 26 tests pasan igual que con el stack completo (`storage`/`realtime`/`studio`/`imgproxy`/`edge-runtime`/`analytics`/`vector`/`pooler`/`mailpit` no los usa ningún test ni el código actual). Motivo: `supabase start` completo arrastra ~9 contenedores/imágenes que un runner de GitHub Actions tendría que descargar en cada ejecución (no hay caché de capas Docker entre runs) — la mayoría de ese tiempo es pull de imágenes que no aportan nada a estos tests.
- **`supabase start` ya aplica las migraciones**: en un runner de CI (siempre limpio, sin volumen previo) `supabase start` inicializa el esquema y corre TODAS las migraciones de `supabase/migrations/` automáticamente (igual que `db reset`) — verificado en local simulando un arranque en frío (`supabase stop --no-backup` + `supabase start`): el log muestra `Applying migration 0001_multi_tenancy_auth.sql` y `Applying migration 0002_departments.sql` antes de reportarse listo. No hace falta un paso separado de "aplicar migraciones".
- **Env vars de los tests, hardcodeadas en el step, no en un archivo**: `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` se pasan como `env:` del step `pnpm test` con los valores fijos y públicos que expone cualquier `supabase start` local por defecto (el JWT `anon` de demo, igual al de `.env.test.local`) — no son secretos reales del proyecto, son constantes conocidas de la CLI para desarrollo local. `vitest.config.ts` ya hace `Object.assign(process.env, loadEnv(...))`, y Vite da prioridad a lo que ya esté en `process.env` sobre lo cargado de archivos `.env*`, así que estas vars sobrescriben correctamente sin tocar ningún archivo en el runner.
- **Healthcheck/espera añadida por la flakiness observada en local**: aunque `supabase start` ya bloquea hasta que los healthchecks de los contenedores pasan, se añadió un paso extra que hace polling a `GET /auth/v1/health` (sin apikey, confirmado que responde `{"name":"GoTrue",...}`) con reintentos cada 2s hasta 30 intentos, más un margen fijo de 5s tras responder. Esto es puramente defensivo: la flakiness "JWT issued at future" vista en local viene de un desfase de reloj de la VM de WSL2/Docker Desktop al reanudar de una suspensión — algo que no debería ocurrir en un runner de GitHub Actions recién arrancado, pero el margen no cuesta nada y evita que ese patrón concreto contamine el CI si algún día aparece por otra causa (imagen recién arrancada, contención de CPU del runner, etc).
- **Criterio de salida**: `pnpm test` en CI debe mostrar `src/features/auth/rls-isolation.test.ts` y `src/features/users/permissions.test.ts` con sus tests EJECUTADOS (no `19 skipped`) y en verde. **Verificado en el run https://github.com/olipp7warlands/saascostchecker/actions/runs/28864126661** (commit `015c38e`): `rls-isolation.test.ts (4 tests)` y `permissions.test.ts (19 tests)` en verde, `26 passed (26)`, seguido de `pnpm build` en verde.
- **Mismo patrón aplicado al job "Playwright e2e"**: `middleware.ts` llama a `createServerClient()` en TODA request (refresca la sesión), así que sin `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` el `webServer` de Playwright (`pnpm build && pnpm start`) revienta con "Your project's URL and Key are required" en cuanto Playwright pide `/es` — no es un problema del smoke test en sí, es que ninguna página carga sin esas env vars. Se añadieron los mismos 4 pasos (`setup-cli`, `start -x ...`, wait-for-health, `env:` en el step `pnpm test:e2e`) al job `e2e`. Verificado en local ejecutando `pnpm test:e2e` con las mismas env vars que usa el step de CI: los 3 smoke tests (`/es`, `/en`, cambio de idioma) pasan.

## 2026-07-08 — Bloque 0.4 Layout base

- **Route group `(app)`, espejo de `(auth)`**: nuevo `src/app/[locale]/(app)/layout.tsx` hace de auth guard (redirige a `/login` si no hay sesión) y envuelve todo en `<AppShell>`. Las páginas `team/departments`, `team/invite`, `team/members` (0.2/0.3) se movieron con `git mv` a `src/app/[locale]/(app)/team/**` — al ser un route group no añade segmento a la URL, así que las rutas (`/es/team/members`, etc.) no cambian y su propia lógica de auth/rol interna (ya testeada) no se tocó.
- **Qué nav es real y qué es placeholder deshabilitado**: de los 7 ítems del mockup, solo "Equipo y reglas" (0.2/0.3) tenía página real antes de este bloque. Vendors, Renovaciones, Solicitudes, Importar gasto y Reconciliación pertenecen a fases 1-3 (no implementadas) y se muestran como placeholders deshabilitados (`aria-disabled="true"`, `tabIndex={-1}`, sin `href`, badge "Pronto") — paridad estructural con el mockup sin construir páginas de otro bloque. **Dashboard es la excepción**: se implementó como página mínima real (`/dashboard`, contenido placeholder — el KPI/pista de renovaciones llega en 1.5) porque el shell necesitaba una landing autenticada real para ser navegable y testeable; sin ella ningún rol (salvo `org_admin`) tendría una sola ruta funcional tras loguearse. Por eso también cambió el destino del redirect post-login/signup/invitación aceptada, de `/${locale}` a `/${locale}/dashboard` (`src/features/auth/actions.ts`, `src/app/api/auth/callback/route.ts`). La home pública `/${locale}` no se tocó.
- **Nav por rol (§5), mapeo real no cosmético** (`src/components/shell/nav-items.ts`): Dashboard y Solicitudes → todos los roles; Vendors, Renovaciones y la sección "Datos" (Importar gasto, Reconciliación) → `finance`/`it_admin`/`org_admin` (dominio de gasto/vendors de §5); sección "Ajustes" (Equipo y reglas) → solo `org_admin`, igual que el chequeo ya existente en esas páginas. Una sección solo se renderiza si tiene al menos un ítem visible para el rol actual. Verificado con e2e (`e2e/shell.spec.ts`): un empleado no ve "Equipo y reglas" ni "Ajustes" en el nav, aunque si visita `/team/members` por URL directa el shell se renderiza igual (el layout no bloquea, solo el nav oculta) y la página muestra su "forbidden" ya existente — defensa en profundidad sin duplicar la lógica de permisos.
- **Menú de usuario con `@base-ui/react/menu`**: mismo patrón que `button.tsx`/`input.tsx` (envolver primitivas de `@base-ui/react` en vez de tirar de la CLI de shadcn, que requeriría red durante la implementación) — da foco/teclado/ARIA correctos sin construirlo a mano. Nueva server action `signOut(locale)` en `actions.ts` (mismo patrón que `signInWithPassword`).
- **Selector de idioma persistente**: `src/components/shell/locale-switcher.tsx`, cliente, generaliza el patrón ya usado en la home (`src/app/[locale]/page.tsx`) con `usePathname()` para preservar la ruta actual al cambiar de idioma en vez de volver siempre a la raíz. La persistencia entre visitas ya la da gratis la cookie `NEXT_LOCALE` que pone el middleware de next-intl existente — no hizo falta nada nuevo para eso.
- **`getCurrentUserProfile()` (`src/features/auth/session.ts`)**: nueva utilidad envuelta en `cache()` de `react` para no duplicar la query `users`+`organizations` si el layout y una página la llaman en el mismo request. La usan el layout `(app)` y `/dashboard`; las páginas `team/*` NO se tocaron (ya tenían su propio chequeo probado, fuera del alcance de este bloque — duplican una query pero es impacto mínimo y evita tocar código ya testeado). Nota: `.select("role, org_id, full_name, organizations(name)")` con embed inicialmente rompía el typecheck (postgrest-js sin tipos generados infiere el embed como array, no objeto) — se cambió a dos queries separadas (`users` y luego `organizations` por `org_id`), igual que ya hacía `team/invite/page.tsx`.
- **Accesibilidad**: `<aside>` (sidebar) + dos `<nav aria-label>` (principal, bottom nav móvil) + un único `<main>` para todo el contenido (las páginas no renderizan su propio `<main>`). Ítems deshabilitados sin `href`, `tabIndex={-1}`, `aria-disabled="true"` — no interactivos, no capturan foco, igual que un `disabled` nativo. No se implementó resaltado de "página activa" en el nav (solo 2 rutas reales existen hoy — Dashboard y Equipo y reglas — y "activa" no tiene mucho sentido con la mayoría del nav deshabilitado); se deja para cuando haya más rutas reales.
- **Mobile**: bottom nav fijo con los 4 ítems primarios del mockup + una cabecera superior nueva (`mobile-header.tsx`, no existe en el mockup estático) porque el selector de idioma y el menú de usuario necesitan un sitio en móvil que el bottom nav (solo 4 iconos) no tiene.
- **`e2e/shell.spec.ts`**: mismo patrón que `rls-isolation.test.ts`/`permissions.test.ts` — crea un `org_admin` real vía `signUp()` y, vía RPC `create_invitation` (con el token generado/hasheado en el propio test, igual que hace `createInvitation()` en el server action) + aceptación por la UI real, un `employee` real, sin depender de email. Se skippea (no falla) si `NEXT_PUBLIC_SUPABASE_URL` no apunta a local. Dos tests: cambio de idioma dentro del shell sin perder la ruta (login → `/team/members` → clic en "EN" del selector, con locator acotado al `role="group" aria-label="Idioma"` del selector — acotarlo al `<nav>` principal falla porque el selector de idioma vive fuera de ese landmark, junto al menú de usuario), y que un empleado no ve las entradas de administración.
- **Incidente de verificación — procesos `next dev` huérfanos rompiendo el build**: durante la verificación visual (capturas de pantalla con Playwright contra `pnpm dev`), varios procesos `next dev` quedaron corriendo en segundo plano tras intentos fallidos de matarlos (en este entorno, `pkill -f "next dev"` de Git Bash no ve los procesos `node.exe` nativos de Windows). Esos procesos mantenían locks de archivo abiertos sobre `.next`, y la siguiente vez que `pnpm test:e2e` lanzaba `pnpm build` sobre un `.next` recién borrado, el build fallaba con `Cannot find module for page: /[locale]/login` (páginas que sí existen) por la colisión de escritura. Diagnóstico correcto: `Get-NetTCPConnection -State Listen | Where LocalPort -in 3000,3001,3002` para encontrar los PIDs reales por puerto (más fiable que `pkill`/`Get-Process node` a secas en Windows) y `Stop-Process -Id <pid> -Force` para matarlos uno a uno — evitar `foreach ($pid in ...)` como nombre de variable en PowerShell, `$pid` es una variable automática de solo lectura y el bucle falla en silencio. Tras matar los 3 procesos huérfanos, `pnpm build`/`pnpm test:e2e` volvieron a pasar limpio. Lección: cualquier `pnpm dev`/`next start` lanzado en segundo plano durante una sesión debe matarse por PID confirmado (vía puerto), no por nombre de comando, antes de dar el bloque por cerrado.

## 2026-07-08 — Bloque 1.1 Catálogo global de SaaS

- **Esquema `saas_catalog`**: `website` guarda el dominio pelado (`figma.com`, no `https://figma.com/`) — es literalmente lo que `<AppLogo domain>` necesita, sin parseo de URL en cada consumidor. `logo_url` (columna de SPECS §4) queda `null` en las ~513 filas del seed: "logos no se almacenan" (§7 1.1) es la decisión de producto, `AppLogo` deriva siempre del favicon vía `domain`; la columna se deja para una futura curación manual, fuera de alcance de 1.1. `category` es un `check` cerrado de 16 slugs en inglés (`crm`, `marketing`, `sales`, `design`, `productivity`, `communication`, `devtools`, `observability`, `security`, `analytics`, `hr`, `finance`, `support`, `project_management`, `video`, `other`), traducidos en `messages/*.json` (`Catalog.category.*`) — nunca texto visible en la fila, por la regla de cero strings hardcodeadas. `verified` por defecto `false`; el seed lo marca `true` en las 513 filas (curadas); una futura promoción de vendor custom al catálogo entraría `false` hasta revisión.
- **Índices de búsqueda — bug real encontrado al migrar**: `array_to_string(text[], text)` está marcado `STABLE` (no `IMMUTABLE`) en el catálogo de Postgres para cualquier `anyarray`, aunque para `text[]` el resultado es en la práctica determinista. Un índice de expresión exige `IMMUTABLE`, así que `create index ... using gin (lower(array_to_string(aliases,' ')) gin_trgm_ops)` fallaba con `functions in index expression must be marked IMMUTABLE` (42P17) — se descubrió en el primer `supabase db reset` local, no en la revisión previa a migrar. Arreglo estándar: se envolvió en `public.saas_alias_blob(text[]) returns text language sql immutable`, usada tanto en el índice como dentro de `search_saas_catalog()` con la misma expresión exacta (necesario para que el planner pueda casar la query contra el índice).
- **Diseño final de índices** (`pg_trgm` en el schema `extensions`, ya soportado por `extra_search_path` de `supabase/config.toml`): btree `(lower(name) text_pattern_ops)` para el caso de prefijo más común; GIN trigram sobre `lower(name)` para "contains"; GIN trigram sobre `lower(saas_alias_blob(aliases))` para alias (concatenados en un solo texto — un GIN de array por defecto solo acelera `@>`/`&&`, no "¿algún elemento contiene X?"). `search_saas_catalog()` pone los predicados `LIKE` directos en el `WHERE` (no ocultos en un `CASE`) para que el planner pueda usar los 3 índices vía `BitmapOr`; el ranking ("prefijo > contains, name > alias") vive solo en el `ORDER BY`. Los mismos índices trigram los reutilizará el fuzzy matcher del bloque 1.3 (`similarity()`).
- **Escritura bloqueada a todo cliente**: `saas_catalog_select` es `to authenticated using (true)` (a diferencia del resto de tablas de este proyecto, que no necesitan `to authenticated` porque su predicado ya excluye a `anon` implícitamente vía `current_org_id()`; aquí `using(true)` sí necesitaba el `to authenticated` explícito para no ser pública a `anon`). Las 3 políticas de insert/update/delete son `with check(false)`/`using(false)`: el seed entra por migración (0004), nunca por la app.
- **Seed (513 filas, no ~500 exactas)**: generado delegando en 6 subagentes en paralelo (2-3 categorías cada uno, mismo schema JSON y guía de estilo de alias), más un séptimo lote propio para reforzar `project_management` (quedó corto tras el dedupe cross-categoría — varios productos como Trello/Asana/Notion aparecían en 2-3 lotes distintos). Deduplicado por `lower(name)` con un script Node (no a mano): en colisión, se queda con la categoría del primer lote y se fusionan los alias de todas las apariciones. "Figma" verificado presente (caso de aceptación literal). Aliases con dos estilos: casual (`"GH"`, `"GSuite"`) y de extracto bancario/tarjeta (`"PDDLE.NET* LOOM"`, `"FS *TYPEFORM"`, `"ADOBE *CREATIVE CLD"`, prefijos de procesador tipo Paddle/FastSpring/Stripe/PayPal) — pensado explícitamente para el fuzzy matcher del bloque 1.3, no solo para el autocompletado de 1.1.
- **`<AppLogo>` — hallazgo real al verificar "de verdad"**: el servicio de favicons de Google (`s2/favicons?domain=...&sz=64`) es tolerante — para un dominio inventado/inexistente devuelve un icono de globo genérico en vez de un error HTTP, así que el `onerror` del `<img>` casi nunca se dispara por "dominio sin favicon real". El fallback a inicial+color SÍ se dispara de forma fiable cuando `domain` es `null` (el caso real más común en la app: un vendor custom sin website conocido) y ante fallos de red genuinos. Verificado visualmente montando el componente temporalmente (no comiteado) — confirmado con capturas, revertido antes de comitear.
- **Ranking — verificado con datos sintéticos, no con el seed real**: `src/features/catalog/search.test.ts` inserta/borra 4 filas propias vía `service_role` (bypassa RLS) diseñadas para caer cada una en un tier distinto del ranking, en vez de apoyarse en nombres reales del seed — así el test no se rompe si el seed cambia, y no hace falta forzar coincidencias artificiales en datos por lo demás reales.
- **"<150ms" — medido de verdad, dos veces**: (1) test automatizado (`search.test.ts`) con `performance.now()` alrededor de la llamada real `.rpc()` contra Supabase LOCAL — pasa reproduciblemente. (2) verificación manual con Playwright conduciendo un navegador real contra `pnpm dev` (también apuntando a Supabase local): la llamada de red real a `/rest/v1/rpc/search_saas_catalog` tardó ~118ms; el tiempo total percibido desde la pulsación de tecla hasta ver "Figma" fue ~238ms porque el combobox aplica un debounce deliberado de 120ms antes de lanzar la búsqueda (para no disparar una request por cada tecla). El criterio de TASKS.md se interpreta como latencia de la búsqueda en sí (confirmado <150ms en ambas mediciones), no como el UX completo incluyendo el debounce intencional. **No se repitió la medición contra el proyecto remoto**: hacerlo habría requerido crear una cuenta de prueba real en el proyecto de producción sin una vía limpia de borrado automatizado después (`SUPABASE_SERVICE_ROLE_KEY` de `.env.local` está vacía a propósito, ver bloque de estabilización de build/CI) — se prefirió no dejar datos de prueba residuales en el proyecto real antes que completar esa medición extra.
- **Combobox — decisiones de accesibilidad/arquitectura**: patrón ARIA 1.2 "combobox con input" (el propio `<input>` lleva `role="combobox"` + `aria-expanded/controls/activedescendant/autocomplete="list"`, sin wrapper `role="combobox"` separado — más simple, es el patrón moderno recomendado). `search`/`onSelect`/`onCreateCustom` son props inyectables (no hardcoded a `searchSaasCatalog()` internamente) para que sea genuinamente reutilizable en 1.2/3.1 y testeable sin red. Usa el cliente de navegador (`src/lib/supabase/client.ts`) para la búsqueda por defecto, no una Server Action — evita el salto extra por el servidor de Next.js en cada tecla, relevante para el objetivo de <150ms.
- **CI rojo tras el push — causa real**: el job "Lint, typecheck, unit tests" falló en `pnpm test` con `Error: supabaseKey is required` en `search.test.ts`. No era el índice `pg_trgm`/`saas_alias_blob` (esos pasaron bien, igual que en local) — el step `pnpm test` de `.github/workflows/ci.yml` nunca definía `SUPABASE_SERVICE_ROLE_KEY` en su `env:` (solo `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY`), y `search.test.ts` es el primer test del repo que necesita la clave de servicio. En local no se detectó porque `.env.test.local` (gitignored) ya la trae y Vitest la carga vía `loadEnv()`. Arreglo: añadida la misma clave `service_role` de demo, fija y pública para cualquier `supabase start` local (no es un secreto real — mismo patrón que la `anon` key ya hardcodeada ahí), al `env:` del step.

## 2026-07-08 — Bloque 1.2 Vendors y contratos

- **RPCs `security definer` en vez de RLS directa para las mutaciones**: igual que `create_department`/`update_user_role` (0.2/0.3) — CLAUDE.md exige `audit_log` en cada mutación de negocio, y eso solo se puede garantizar atómicamente si la escritura pasa por una función que hace ambas cosas en la misma transacción. `insert`/`update`/`delete` de `vendors`/`contracts` están bloqueados en RLS (`with check(false)`/`using(false)`) — ningún cliente puede saltarse las 6 RPCs (`create_vendor`, `update_vendor`, `delete_vendor`, `create_contract`, `update_contract`, `delete_contract`). El `select` sí es una política RLS normal, pero a diferencia de `departments` (visible a toda la org), aquí exige explícitamente `role in ('finance','it_admin','org_admin')` — SPECS §5 no da esta sección a `employee`/`manager`, y CLAUDE.md exige que el servidor lo haga cumplir, no solo el nav del bloque 0.4.
- **`contracts.vendor_id` sin `on delete cascade`**: decisión deliberada. Borrar un vendor con contratos falla con un error de FK claro (verificado en `permissions.test.ts`: `foreign key|violat`) en vez de arrastrar el borrado de contratos (y sus PDFs huérfanos en Storage) en silencio.
- **`document_url` guarda una ruta de Storage, no una URL pública**: el bucket `contracts` es privado (`public = false`); la UI pide una signed URL de 60s al vuelo (`getContractDocumentUrl()`) cada vez que se pulsa "Ver PDF". El nombre de columna sigue a SPECS §4 literalmente aunque semánticamente sea un path (`{org_id}/{contract_id}/{filename}`).
- **Políticas de Storage — mismo aislamiento por rol que las tablas**: las 4 políticas sobre `storage.objects` (select/insert/update/delete) exigen `bucket_id = 'contracts' and (storage.foldername(name))[1] = current_org_id()::text and current_user_role() in (...)`. Verificado con `file_size_limit`/`allowed_mime_types` a nivel de bucket (10MB, solo `application/pdf`) — Storage los rechaza en el propio upload, antes de evaluar RLS. No se valida que el segundo segmento de la ruta sea un `contract_id` real de esa org (ver migración 0006 para el razonamiento completo) — el aislamiento por `org_id` ya es la garantía de seguridad real.
- **Categoría duplicada en `vendors` (no un tipo compartido con `saas_catalog`)**: mismo `check` de 16 slugs copiado literalmente. Postgres no tiene un mecanismo limpio de "enum compartido reutilizable entre tablas" sin crear un `create type ... as enum` dedicado, y hacerlo ahora para 2 usos es sobreingeniería — si aparece un tercer uso, se reconsiderará. Al crear un vendor desde el catálogo, `category` se copia como snapshot (no hay FK a `saas_catalog.category`); queda editable después sin tocar el catálogo, tal como pide el enunciado.
- **`ContractFields` extraído como componente compartido** (`src/app/[locale]/(app)/vendors/contract-fields.tsx`): los mismos 9 campos del contrato aparecen 3 veces (alta combinada, edición inline en el detalle, "añadir otro contrato") — con 3 usos reales se justifica la extracción (regla de tres), evita triplicar ~90 líneas de JSX idéntico.
- **Flujo de alta combinado en una sola Server Action** (`createVendorWithContract`): `create_vendor` → `create_contract` (sin `document_url`) → si hay PDF, sube a Storage → `update_contract` con la ruta. Esto genera 2 entradas en `audit_log` para un alta con PDF (`contract.created` + `contract.updated`) en vez de 1 — se aceptó como un rastro de auditoría honesto (el contrato de verdad se crea primero y se actualiza después) en vez de añadir una séptima RPC solo para evitarlo.
- **Formato de moneda — hallazgo real al verificar "de verdad"**: `Intl.NumberFormat("es", {style:"currency", currency:"EUR"})` para 9.120 en este entorno (Node 22, ICU 76.1) devuelve `"9120 €"`, SIN separador de miles — el CLDR de `es`/`es-ES` en esta versión de ICU solo agrupa a partir de 5 dígitos (confirmado con `Intl.NumberFormat('es-ES').format(9120)` → `"9120"` vs `.format(38400)` → `"38.400"`), a diferencia de `de-DE` que agrupa desde 4 dígitos. El mockup estático asume agrupación siempre (`€9.120`); se mantuvo `Intl.NumberFormat` real (locale-correcto) en vez de forzar una agrupación hardcodeada — es el comportamiento nativo correcto del navegador/Node para ese locale, no un bug. El test e2e se ajustó para no asumir el punto de miles.
- **Sin conversión de divisas**: cada fila del listado muestra el coste en la moneda propia del contrato (`cost_amount`/`currency`), sin convertir a la moneda default de la org — esa infraestructura (tipos de cambio semanales) es SPECS §8/bloque 1.5, no 1.2. `annualizedCost()` (`src/features/vendors/renewal.ts`) solo anualiza el ciclo de facturación (mensual ×12), no convierte moneda.
- **Renovación siempre como badge fecha+días, sin caso especial "mensual"**: el mockup muestra un vendor custom (Loom) con un pill `mensual` en vez de fecha+días en la columna Renovación — se interpretó como variación visual de la demo estática, no como regla de negocio, y se implementó la regla explícita del enunciado ("rojo ≤7d, ámbar ≤45d, neutro después") de forma consistente para todas las filas, incluidas las custom.
- **`e2e/vendors.spec.ts` — bug de loader de Playwright encontrado y evitado**: usar `import.meta.url` (para computar `__dirname` de forma portable) dispara `ReferenceError: require is not defined` en el loader TS de esta versión de Playwright (1.61.1) — el archivo se compila como ESM por la presencia de `import.meta`, pero el resto de imports siguen pasando por un shim de interop CJS. Se evitó `import.meta` del todo: la ruta del PDF de fixture se resuelve con `path.resolve(process.cwd(), "e2e/fixtures/...")`, válido porque Playwright siempre corre desde la raíz del proyecto.
- **Verificación real de <1 min**: el test e2e completo (login → elegir "Figma" del catálogo real → rellenar contrato → subir `e2e/fixtures/sample-contract.pdf` real → submit → aterrizar en el detalle) tarda ~3-9s de principio a fin contra Supabase local, medido con `Date.now()` real (no simulado) — muy por debajo del minuto del criterio de aceptación.
- **CI rojo en el job "Playwright e2e" — causa real**: `e2e/vendors.spec.ts` colgaba con "Test timeout of 30000ms exceeded" en `waitForURL` justo tras el submit, en las 3 reintentos. No era el flujo en sí (pasaba en <12s en local) — el `supabase start` del job e2e excluye `storage-api` desde el bloque 0.4 (cuando ningún test tocaba Storage todavía). `vendors.spec.ts` es el primer test que sube un PDF de verdad al bucket `contracts` a través del navegador real; sin `storage-api` corriendo, esa llamada de Storage se queda colgada indefinidamente y la Server Action nunca completa ni redirige. Arreglo: se quita `storage-api` de la lista de exclusión del job e2e (`imgproxy` sigue excluido, no se usa — solo servimos PDFs tal cual, sin transformación de imágenes). El job de unit tests no lo necesita (`search.test.ts`/`permissions.test.ts` no suben archivos) y se deja igual. Verificado reproduciendo el mismo subconjunto mínimo de servicios en local (`supabase stop` + `supabase start` con la misma lista de exclusión que ahora usa CI): el test fallaba igual sin `storage-api` y pasaba (11.7s) con él incluido.

## 2026-07-08 — Bloque 1.3 Import de gasto (CSV)

- **Algoritmo de fuzzy matching diseñado y validado ANTES de implementar** (pedido explícito del enunciado): 10 descripciones bancarias reales trazadas a mano contra el algoritmo propuesto, revisadas por un segundo pase adversarial (agente Plan) que encontró un fallo real antes de escribir código — ver el hallazgo de PayPal más abajo. El algoritmo final vive en `best_catalog_match()` (SQL, `supabase/migrations/0007_spend_import.sql`), reutilizando `pg_trgm`/`similarity()` instalado en 0003 tal como su propio comentario anticipaba. Umbrales de la cola de reconciliación: `confidence >= 0.65` → "alta" (auto-sugerida y elegible para bulk-accept), `0.40 <= confidence < 0.65` → "media" (auto-sugerida, requiere confirmación manual, NO elegible para bulk-accept), `confidence < 0.40` → sin sugerencia (`suggested_catalog_id` null, la fila queda igualmente en la cola para vincular/crear vendor/ignorar a mano).

  Las 10 descripciones se re-verificaron después contra `best_catalog_match()` real (Supabase local, seed completo) — resultado exacto, columna "obtenido" es lo que devolvió la función, no una expectativa:

  | Descripción bancaria | Vendor obtenido | Confidence | Regla que dispara |
  |---|---|---|---|
  | `GITHUB.COM SAN FRANCISCO US` | GitHub | 0.90 | (c) prefijo de la cadena completa |
  | `ADOBE *CREATIVE CLD` | Adobe Creative Cloud | 1.00 | (a) coincidencia exacta |
  | `PAYPAL *FIGMA INC` | **Figma** (no PayPal) | 1.00 | (b) coincidencia exacta con el segmento tras `*` |
  | `SP * SHOPIFY` | Shopify | 1.00 | (a) coincidencia exacta |
  | `MSFT * CLIPCHAMP` | Clipchamp | 1.00 | (a) coincidencia exacta |
  | `PDDLE.NET* LOOM` | Loom | 1.00 | (a) coincidencia exacta |
  | `ZOOM.US 888-799-9666 WA` | Zoom | 0.90 | (c) prefijo (racha de 4 dígitos "9666" eliminada) |
  | `SLACK T-A1B2C3D4` | Slack | 0.90 | (c)+(d) doble señal: nombre "SLACK" y alias placeholder-stripped "SLACK T" |
  | `AWS EMEA 4857392` | AWS | 1.00 | (a) coincidencia exacta (racha de 7 dígitos eliminada) |
  | `OFICINA SUMINISTROS SL` | Typeform (irrelevante) | 0.10 | ninguna regla fuerte — por debajo de 0.40, **sin sugerencia** (correcto: no es gasto SaaS) |

  El caso de PayPal es el que motivó la regla (b) del segmento-cola: sin ella, `"PAYPAL *FIGMA INC"` habría resuelto a PayPal (alias vacío `'PAYPAL *'`, confirmado en `0004_saas_catalog_seed.sql:338`) en vez de al comerciante real.
- **Hallazgo del diseño (encontrado en revisión, no en producción): agregadores de pago con alias "vacíos" ganan al comerciante real**. `saas_catalog` tiene `PayPal → ['PAYPAL *', 'PYPL']` (confirmado en `0004_saas_catalog_seed.sql:338`). Sin tratamiento especial, `"PAYPAL *FIGMA INC"` empataría o perdería contra Figma porque "PAYPAL" es un prefijo válido de la cadena completa. Fix: si la descripción cruda contiene `*`, se calcula también el segmento tras el ÚLTIMO `*` (`desc_tail`) y una coincidencia EXACTA con ese segmento puntúa 1.0 (el techo), por encima del 0.9 que un prefijo de la cadena completa puede alcanzar — así el comerciante real (tras el `*`) siempre gana sobre el rail de pago (antes del `*`), sin tocar el seed y generalizando a Stripe/Square/Adyen si algún día se seedeasen con un alias-rail igual de "vacío".
- **Alias cortos (<4 caracteres, p.ej. "AI"/"CC"/"GH"/"PS"/"ID") nunca usan `similarity()` de pg_trgm**: sobre strings de 2-3 caracteres la similitud de trigramas es degenerada (ni fiablemente 0 ni fiablemente significativa). Estos alias solo matchean si aparecen como TOKEN COMPLETO de la descripción normalizada (split por espacios), nunca por contención de substring — evita que un alias de 2 letras dispare falsos positivos sobre cualquier texto que lo contenga de casualidad.
- **Alias-plantilla con placeholder** (p.ej. `'SLACK T-XXXX'`, donde "XXXX" representa un ID de transacción real y nunca aparece literal en un extracto real): se detecta la cola de placeholder (racha de ≥3 del mismo carácter al final del alias normalizado) y se registra un candidato adicional con esa cola eliminada, usado solo para matching por prefijo. Sin esto, ese tipo de alias nunca podría matchear ningún string real.
- **`dedup_hash` NO es una columna `generated always as (...)`**: se intentó primero como columna generada (una sola fuente de verdad para la normalización), pero Postgres rechaza la expresión con `generation expression is not immutable` — `date::text`/`numeric::text` dependen de `DateStyle` de sesión y cuentan como solo `STABLE`, no `IMMUTABLE`, requisito estricto de las columnas generadas. Se resolvió calculando el hash explícitamente dentro de `import_spend_records()` (mismo `normalize_bank_text()`, misma única implementación en Postgres, solo que en el INSERT en vez de en la definición de columna) — el insert masivo usa `ON CONFLICT (org_id, dedup_hash) DO NOTHING RETURNING id`, y los contadores de `import_batches` se derivan de lo que Postgres realmente insertó, nunca de un pre-filtro en JS.
- **Lección de proceso: nunca editar un archivo de migración ya empujado a remoto.** Tras el primer `supabase db push` de 0007/0008, se detectó que faltaba persistir el `has_header` confirmado por el usuario (solo se guardaba la suposición inicial). Se editó 0007 in-place y se hizo `db reset` local + `db push` — pero `supabase db push` rastrea migraciones aplicadas por NOMBRE de archivo, no por contenido, así que el remoto quedó con la versión VIEJA de la función mientras local tenía la nueva (drift silencioso, sin ningún error). Se revirtió 0007 a su contenido original y el cambio se aplicó como una migración nueva (`0009_import_spend_records_has_header.sql`, `create or replace function`) — la única forma correcta de modificar una función ya desplegada.
- **`import_batches.status` sin estado `'failed'`**: un fallo dentro de `import_spend_records()` revierte toda la transacción (incluida la transición `uploaded → processing`), así que el batch simplemente vuelve a `'uploaded'` y queda listo para reintentar. No hay ningún camino de código que necesite persistir un estado de error explícito — añadirlo habría sido un campo sin escritor real.
- **Índice único `vendors_org_catalog_id_unique_idx (org_id, catalog_id) where catalog_id is not null`** (nuevo en 0007, `vendors` es de 0005): necesario porque `reconciliation_queue.suggested_catalog_id` apunta al catálogo GLOBAL pero `spend_records.vendor_id` necesita un vendor de la ORG — `link_reconciliation()` hace "find-or-create" (busca un vendor existente con ese `catalog_id` en la org; si no existe, lo crea) vía `INSERT ... ON CONFLICT ... DO NOTHING` + re-`SELECT`, atómico frente a dos reconciliaciones concurrentes resolviendo al mismo `catalog_id`. Verificado con un test explícito (`permissions.test.ts`) que vincula dos movimientos distintos sugiriendo el mismo vendor del catálogo y confirma que se reutiliza el mismo vendor, no se duplica.
- **`bulk_accept_reconciliation()` re-valida `confidence >= 0.65` en el servidor**, no confía en que el cliente ya filtró la selección — cualquier caller autenticado con rol válido puede invocar la RPC directamente (CLAUDE.md regla 5: "la UI solo oculta"). Las filas que no califican se saltan sin error; la RPC devuelve `{linked, skipped}` para que la UI muestre un resultado parcial.
- **`papaparse` añadido como dependencia nueva** (parseo CSV con auto-detección de delimitador — resuelve el caso común de bancos españoles con `;`). No se añadió ninguna librería de fuzzy-matching en JS (`fuse.js`/`string-similarity`): el matching vive enteramente en Postgres reutilizando los índices trigram ya instalados en 0003, tal como su comentario original preveía.
- **Encoding sin dependencia nueva**: UTF-8 estricto primero (`TextDecoder('utf-8', {fatal: true})`), y si falla (secuencia inválida — típico de CSVs bancarios en ISO-8859-1), `Buffer.from(buf).toString('latin1')`. `Buffer` soporta `latin1` de forma nativa en Node sin ICU completo ni una librería como `iconv-lite`.
- **Subida a Storage en dos pasos, mismo patrón que los PDFs de contrato (0006)**: `previewCsvImport()` crea el `import_batches` (RPC `create_import_batch`) y sube el CSV crudo al bucket privado `spend-imports` (`{org_id}/{batch_id}/{filename}`, RLS idéntica a `contracts`); `commitCsvImport()` vuelve a DESCARGAR el fichero de Storage y lo re-parsea COMPLETO server-side con el delimiter/encoding guardados — nunca confía en lo que el cliente dice haber parseado en el preview. El checkbox "primera fila es cabecera" se resuelve completamente client-side sobre las filas crudas ya recibidas (sin una segunda llamada al servidor cuando el usuario lo cambia de opinión).
- **Criterio de aceptación medido con un fixture real, no simulado**: `src/features/spend-import/__fixtures__/bank-statement-200.ts` — 150 filas de gasto SaaS real (50 vendors del seed demo × 3 variantes de ruido bancario: descriptor+ciudad, boilerplate SEPA español+referencia, agregador PayPal/referencia) + 50 filas de gasto NO-SaaS (alquiler, nómina, suministros, viajes, gestoría...) que deben quedar correctamente SIN sugerencia — mezclar ambas evita inflar el porcentaje con un CSV 100% "matcheable". Resultado real contra el seed demo en Supabase local: **200/200 (100%)**, muy por encima del 70% exigido (`src/features/spend-import/import-acceptance.test.ts`).
- **Verificación real del wizard en navegador** (no solo tests): build apuntado temporalmente a Supabase local (para no escribir datos de prueba en el proyecto remoto compartido) + un script Playwright desechable (mismo patrón que `e2e/vendors.spec.ts`) que firma una org real, sube un CSV con delimitador `;`, encoding latin1 genuino (con una "Ñ" real) y fechas `DD/MM/YYYY`, confirma el fallback de encoding en pantalla, importa con una fila duplicada a propósito (resultado: 4 importadas / 1 duplicada / 0 errores), vincula la sugerencia de Adobe Creative Cloud, y hace bulk-accept del resto de sugerencias de alta confianza — solo queda pendiente la fila sin match razonable, como se esperaba.

## 2026-07-09 — Bloque 1.4 Licencias manuales

- **`seat_assignments.contract_id` y `.user_id` SÍ cascadean** (`on delete cascade`), a diferencia de `contracts.vendor_id` (0005, sin cascade a propósito). Un `seat_assignment` no es un registro histórico independiente — no tiene sentido sin su contrato ni sin su usuario, mismo criterio que `reconciliation_queue.spend_record_id` (0007). `delete_contract()`/borrado de usuario no necesitaron ningún cambio: la cascada libera los asientos sola.
- **"Activo" = `last_seen_active_at is not null`**, no una columna booleana propia. `assign_seat()` la fija a `now()` al asignar (se asume en uso); `set_seat_active(p_active=false)` la limpia a `null`. SPECS §4 pide explícitamente que la Fase 5 (sync SSO) reutilice esta misma columna con timestamps reales de login sin cambiar el modelo — con este diseño, el sync solo necesita sobrescribir la columna, la definición de "activo" no cambia (aunque en Fase 5 probablemente pase a ser "no null Y reciente", eso es decisión de esa fase, no de esta).
- **Exceso de asientos: permitido, señalado, nunca bloqueado** (criterio literal de `docs/TASKS.md` 1.4 — "pasa en la vida real"). `assign_seat()` es `returns table (seat_id uuid, over_capacity boolean)` en vez de solo `uuid`, para que la Server Action tenga el aviso en la misma respuesta (sin una segunda consulta) y lo muestre como aviso no bloqueante justo después de asignar; `over_capacity` también queda en el `diff` de `audit_log` para el histórico. Verificado en `permissions.test.ts` con un contrato de 1 asiento comprado y 2 asignaciones reales.
- **Métrica de utilización como funciones TS puras (`src/features/vendors/seats.ts`), no una función/vista SQL**: mismo estilo que `renewal.ts` (`annualizedCost`/`renewalTone`). El listado de vendors y el futuro dashboard (1.5) ya resuelven sus datos con selects normales (RLS de por medio) y agregan en JS — igual que `vendors/page.tsx` ya hace con `activeContracts` — así que no hacía falta una función SQL nueva solo para esto. `seatUtilizationPct`/`utilizationTone` (umbral **teal ≥70%, ámbar <70%**, literal del mockup y de TASKS.md) y `wastedSeatCost` (coste anualizado / asientos comprados × asientos comprados no activos, clamped a 0 cuando hay más asignados que comprados) están cubiertas por `seats.test.ts` con los bordes pedidos explícitamente (0 comprados, más asignados que comprados) y con los números exactos del mockup (HubSpot 12/20 → 60% y 8.640€ desperdiciados sobre 21.600€/año).
- **Columna "Asientos" del listado sin tocar**: TASKS.md 1.4 solo pide sustituir el placeholder de "Utilización"; `vendors/page.tsx`/`vendor-row.tsx` no cambian esa columna (sigue mostrando `seatsPurchased` a secas) — solo se añade el join a `seat_assignments` necesario para calcular el % activo.
- **Gestión de asientos en el detalle del contrato, no en el listado**: nuevo `contract-seats.tsx` (client) montado dentro de `ContractRow` — asignar/quitar/marcar activo-inactivo por asiento, resumen de utilización + € desperdiciado, aviso de `over_capacity`. El desplegable de "asignar asiento" excluye en JS a los usuarios ya asignados a ESE contrato (no hace falta una RPC de "usuarios elegibles": la lista de miembros y la de asientos ya están cargadas en la página).
- **`TestTenant.userId` (de `permissions.test.ts`) es el `auth_id`, no el `id` de `public.users`** — hallazgo real al escribir los tests de `assign_seat()`: el RPC valida `p_user_id` contra `public.users.id`, así que pasar `tenant.userId` directamente fallaba con `user does not belong to this organization` (nunca se había ejercitado antes: los tests de 1.2 siempre pasaban `p_owner_user_id: null`). Se añadió `publicUserId(tenant)` (select a `users` por `auth_id`, permitido por la RLS estándar) para resolver el id correcto antes de llamar a `assign_seat`.
- **Verificación real en navegador**: build apuntado temporalmente a Supabase local (mismo motivo que 1.3 — no dejar datos de prueba en el remoto compartido) + un script Playwright desechable que firma una org, invita a 2 miembros más, crea un vendor/contrato custom con `seats_purchased=2`, y comprueba en pantalla: barra a 0% en el listado antes de asignar nada, 100% (teal) al asignar 2/2, 50% (ámbar) + "≈ 600 € desperdiciado al año" al marcar un asiento inactivo, el aviso de `over_capacity` al asignar un 3º asiento por encima del límite, y que "Quitar" (con `window.confirm`) funciona. Capturas confirmaron visualmente el color ámbar/teal de la barra (script y capturas borrados antes de comitear, no forman parte del repo).
- **`supabase db push` aplicado al proyecto remoto** (`mkrsicuvhnmljpurtwun`) tras verificar `pnpm lint && pnpm typecheck && pnpm test && pnpm build` en verde contra Supabase local (`supabase db reset` para aplicar 0010 desde cero).

## 2026-07-14 — Bloque 1.5 Dashboard

- **Atribución de gasto a departamento — OPCIÓN 1 elegida** (de 2 alternativas discutidas al final del bloque 1.4): `contracts.department_id uuid references departments(id) on delete set null`, nullable. Se descartó una alternativa de prorrateo de coste por asientos entre varios departamentos (más fiel a la realidad de contratos compartidos, pero exige un modelo de asignación N:M nuevo — `contract_department_allocations` con porcentajes — que no pide TASKS.md 1.5 y añadiría complejidad sin un caso de uso confirmado). El campo queda como base para esa evolución futura: si se necesita prorrateo, se añadiría una tabla de asignación que conviva con `department_id` (que pasaría a ser "departamento primario" o se dejaría solo para contratos no prorrateados), no un rediseño.
- **Contratos sin departamento se agrupan visibles, no se ocultan**: el widget "Gasto por departamento" (`department-spend-table.tsx`) agrupa `department_id is null` bajo la fila "General / Sin asignar" (`Shell.dashboard.departmentSpend.unassigned`, ambos idiomas) en vez de excluirlos del todo — el gasto sin categorizar es información real y accionable (es una señal de que faltan datos, no algo que ocultar). `buildDepartmentSpend()` (`src/features/dashboard/aggregate.ts`) usa una clave interna `__unassigned__` para esa fila, nunca expuesta fuera de la función.
- **Selector de departamento añadido a `ContractFields`** (componente compartido de 1.2, ver bloque 1.2 más arriba — sigue siendo el único punto de edición de campos de contrato, ahora con un cuarto uso implícito: alta combinada, alta desde detalle de vendor, edición inline, y el propio selector reutilizado en los 3). `create_contract`/`update_contract` se redefinen en `0011_dashboard.sql` (no se edita 0005 in-place — lección de proceso del bloque 1.3) con `p_department_id uuid default null`; como `create or replace function` no puede cambiar la lista de parámetros de una función ya desplegada sin ambigüedad para PostgREST (error PGRST203 de sobrecarga), la migración hace `drop function` explícito de las dos signaturas viejas antes de crear las nuevas. Ambas RPC validan que `p_department_id` (si no es null) pertenece a la misma org antes de escribir — mismo patrón de validación cruzada que ya usa `create_contract` con `vendor_id`.
- **Columna "vs. presupuesto" del mockup — explícitamente FUERA de este bloque**: TASKS.md pedía "gasto por departamento vs. presupuesto", pero no existe ningún campo de presupuesto en el modelo de datos (ni en SPECS §4) — inventar un valor o un placeholder habría sido una figura falsa presentada como dato real. Se implementa solo "gasto por departamento" (sin la columna de presupuesto) y se anota aquí como candidata a una fase futura, cuando exista un flujo real para que la org defina presupuestos por departamento (probablemente Fase 3, junto al motor de aprobaciones, donde el presupuesto ya es un concepto de negocio necesario). TASKS.md 1.5 se marcó completo con esta acotación explícita en el propio checkbox.
- **`exchange_rates` — tabla global nueva, mismo patrón que `saas_catalog`**: sin `org_id`, RLS de solo lectura para `authenticated` (`using(true)`), insert/update/delete bloqueados a todo cliente (`with check(false)`/`using(false)`) — los rates entran solo por migración. Regla 7 de CLAUDE.md prohíbe APIs de terceros hasta Fase 5, así que no hay refresco automático; son 6 pares de divisas comunes de facturación SaaS (EUR/USD/GBP/CHF, ambas direcciones) con una fecha `as_of` fija y literal (2026-07-01) para reproducibilidad entre entornos. El refresco periódico real (o una fuente de rates viva) queda pendiente de la infraestructura de cron del bloque 2.1 — no antes.
- **Conversión de moneda — degradación explícita, no bloqueante**: `convertAmount()` (`src/features/dashboard/currency.ts`) intenta el rate directo, luego el inverso, y si no encuentra ninguno de los dos devuelve el importe SIN convertir en vez de lanzar. Una moneda sin sembrar en `exchange_rates` no debe tirar el dashboard entero — produce un agregado ligeramente subestimado/sobrestimado en vez de un error 500, documentado en el propio código (no es un intento de aparentar precisión que no hay).
- **Rendimiento — una sola query anidada, agregación en JS, mismo criterio que 1.4**: `dashboard/page.tsx` hace una única query a `vendors` con `contracts(...)` y `seat_assignments(...)` anidados vía embed de PostgREST (más `exchange_rates`, `reconciliation_queue` preview y `organizations.default_currency` en el mismo `Promise.all`) — sin N+1 por vendor/contrato. Los 3 KPIs, la pista de renovaciones y el gasto por departamento se derivan todos de la misma lista de `DashboardContract` en memoria (`buildKpis`/`buildRenewalTrack`/`buildDepartmentSpend`, funciones TS puras en `src/features/dashboard/aggregate.ts`, cubiertas por `aggregate.test.ts`) — se descartó una vista o función SQL dedicada por el mismo motivo que `seats.ts` en 1.4: el volumen de datos de una org (decenas/cientos de contratos, no millones) no lo justifica, y mantener la lógica en TS testeable sin SQL es más simple.
- **Pista de renovaciones — 2 carriles anti-solape, no offsets fijos del mockup**: el mockup estático coloca tickets en `top:6px`/`top:60px` alternos a mano; `buildRenewalTrack()` calcula la posición real (`xPercent` proporcional a días restantes sobre una ventana de 120 días) y asigna cada ticket al carril 0 o 1 según si colisiona en X con el último ticket de ese carril (umbral 6% del eje) — generaliza a cualquier número/distribución real de renovaciones en vez de asumir que nunca hay más de 2 tickets próximos entre sí. Contratos ya vencidos (`daysUntil < 0`) se muestran clamped al extremo izquierdo (día 0) en vez de ocultarse — información real y accionable, igual que el criterio de "Sin asignar" de arriba. La marca de preaviso vencido (`noticeWarning`) solo aplica a contratos con `auto_renews = true` y `cancellation_notice_days > 0` cuyo `daysUntil` ya esté dentro de esa ventana de preaviso.
- **Verificación con dataset conocido — e2e, no solo unit tests**: `e2e/dashboard.spec.ts` crea una org real con 2 miembros, 1 departamento, y 7 contratos/vendors diseñados a mano para ejercitar cada regla de negocio del bloque en una sola pasada (renovación en 5 días con preaviso ya vencido, ámbar a 26 días, neutro a 70 días sin departamento, vencido hace 3 días, un contrato en USD para forzar conversión de moneda real vía el rate sembrado, un contrato cancelado que debe desaparecer de todos los agregados, un vendor activo sin owner) más un movimiento de gasto pendiente de reconciliar con sugerencia de alta confianza conocida (Adobe Creative Cloud, ver bloque 1.3) — y verifica las cifras EXACTAS resultantes en pantalla (4230€ anualizado, 600€ desperdiciados, 3000€/3 vendors en "Ingeniería", 1230€/2 vendors en "Sin asignar", etc.), no solo que los widgets se rendericen. Requiere Supabase local (crea tenants reales) — se skippea si `NEXT_PUBLIC_SUPABASE_URL` no apunta a local, mismo patrón que el resto de la suite; se verifica en CI, no en la máquina de desarrollo (ver "Entorno de desarrollo" en CLAUDE.md).
- **`supabase db push` aplicado al proyecto remoto** (`mkrsicuvhnmljpurtwun`) para `0011_dashboard.sql`, confirmado con `supabase migration list` (0011 presente en ambas columnas Local/Remote).

## 2026-07-15 — Incidente: 500 en producción tras el deploy de 1.5 (Railway)

- **Síntoma**: tras el push de 1.5 (`4a8c879`), `https://saascostchecker-production.up.railway.app/` devolvía 500 en TODAS las rutas — confirmado con `curl` (`/`, `/es`, `/en`, `/es/login`), salvo `/api/auth/callback` (307, funcionaba). Railway y el status check de GitHub sobre el commit marcaban `state: success`. `pnpm build` local (mismo commit) pasaba en verde.
- **Causa raíz**: `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` no estaban disponibles durante el paso de BUILD del deploy en Railway (aunque sí lo estuvieran para runtime/deploy — las vars llevaban semanas configuradas, pero aparentemente no expuestas al contenedor de build de este despliegue concreto). Next.js sustituye toda referencia a `process.env.NEXT_PUBLIC_*` por su valor literal en tiempo de BUILD (vía el compilador, no en tiempo de ejecución) — esto ocurre en CUALQUIER código que las lea, incluido código server-only como `middleware.ts`/`src/lib/supabase/server.ts`, aunque la lectura esté escrita dentro de una función y "parezca" runtime. Sin esas vars en el build, la sustitución deja `undefined` compilado en el bundle de middleware; `createServerClient(undefined!, undefined!, ...)` lanza `"Your project's URL and Key are required to create a Supabase client!"` en cada request. `middleware.ts` corre en TODA ruta excepto las excluidas por su `matcher` (`/((?!api|trpc|_next|_vercel|.*\\..*).*)`) — de ahí que únicamente `/api/auth/callback` sobreviviera; cualquier otra página, incluida la portada, revienta.
- **Por qué "deploy success" no lo detectó**: tanto el status check de Railway sobre el commit de GitHub como el propio dashboard de Railway solo confirman que el contenedor arrancó y quedó escuchando en el puerto esperado — no hacen ninguna petición HTTP real a una ruta de la app para comprobar que responde. Un 500 en el primer render de cada página es completamente compatible con un healthcheck de arranque en verde. **Fix aplicado a raíz de este incidente**: `CLAUDE.md` ahora exige verificar con `curl` que la producción responde 200 en al menos una ruta real después de cada deploy, en vez de fiarse del estado de Railway/GitHub.
- **No fue un bug de código de 1.5**: se descartó explícitamente que el bloque introdujera una dependencia nueva de variable de entorno (auditado el diff completo del commit — el único `process.env` nuevo vive en `e2e/dashboard.spec.ts`, que no forma parte del bundle de producción). El bloque 1.5 no tocó `middleware.ts` ni ningún cliente de Supabase.
- **Receta de diagnóstico usada (reproducible sin acceso a los logs de Railway)**: `mv .env.local /tmp/env.local.backup` → `rm -rf .next && pnpm build` (build sin las vars, igual que un build de Railway al que le faltan) → `pnpm start` → comparar con `curl` el patrón de códigos HTTP entre rutas (`/`, `/es`, `/es/login`, `/api/auth/callback`) contra el mismo patrón observado en producción. El patrón coincidió exactamente (500/500/500/307) y el log local mostró el mismo mensaje de error que cabía esperar de `@supabase/ssr`, confirmando la causa sin necesitar los logs reales de Railway. Se restauró `.env.local` inmediatamente después y se verificó que `pnpm build && pnpm start` volvía a servir 200 en `/es`/`/es/login` antes de dar el diagnóstico por bueno.
- **Resolución**: el usuario revisó y corrigió las variables en el dashboard de Railway (disponibles ahora también en el paso de build) y disparó un redeploy. Verificado con `curl` tras el redeploy: `/` → 307, `/es` → 200, `/es/login` → 200, `/api/auth/callback` → 307 — patrón idéntico al esperado en local. Sin cambios de código ni de migraciones; el incidente fue puramente de configuración de la plataforma de despliegue.

## 2026-07-15 — Ampliación de 1.1: catálogo de herramientas de IA

- **No es un bloque nuevo, es una ampliación de datos sobre 1.1 ya cerrado**: `docs/TASKS.md` gana un checkbox bajo 1.1, no una sección nueva. El pedido explícito era "sin cambios de schema", pero **el CHECK constraint de `category` sí se amplía** (en `saas_catalog` Y en `vendors`, que lo copia — ver bloque 1.2 más arriba) porque no hay otra forma de aceptar 7 slugs nuevos sin tocarlo; ninguna tabla/columna nueva se crea. Se decidió proceder con esta única excepción — la alternativa (no ampliar el constraint) habría hecho imposible el pedido literal del usuario.
- **Nombre del constraint no hardcodeado**: esta máquina de desarrollo no tiene Docker/Postgres local (ver "Entorno de desarrollo" en CLAUDE.md), así que no había forma de confirmar contra una base real si Postgres había nombrado el constraint `saas_catalog_category_check` (la convención estándar) o algo distinto. La migración (`0012_ai_catalog_seed.sql`) usa un bloque `do $$ ... $$` que localiza el constraint en `pg_constraint`/`pg_attribute` por `(tabla, columna)` en vez de asumir el nombre, lo dropea por el nombre real encontrado, y crea uno nuevo con nombre explícito (`saas_catalog_category_check`/`vendors_category_check`) para que futuras migraciones sí puedan asumirlo. Verificado: `supabase db push` aplicó la migración sin error contra el remoto, lo que confirma que la búsqueda dinámica encontró y dropeó el constraint correcto en ambas tablas.
- **3 entradas ya sembradas en 0004 eran herramientas de IA sin categoría de IA** (encontradas al auditar el catálogo antes de generar la ampliación, no pedidas explícitamente por el usuario): `Descript` (`video`→`ai_audio_voice`), `Otter.ai` (`productivity`→`ai_meeting_agents`), `Synthesia` (`video`→`ai_image_video`). Recategorizadas con `UPDATE` en la propia migración — mismo `id`/aliases, no son inserts nuevos. `Seamless.AI` (IA de prospección de ventas) se dejó tal cual en `sales`: no encaja limpiamente en ninguna de las 7 categorías nuevas y forzarla habría sido inventar un encaje que el usuario no pidió.
- **Generación de las 144 filas nuevas — mismo patrón que el seed original de 1.1**: 7 subagentes en paralelo (uno por categoría), cada uno con la lista completa de los 513 nombres ya sembrados para no duplicar, y la instrucción explícita de devolver únicamente tuplas SQL en un formato fijo (sin nombres inventados, solo productos reales confirmados). Los 5 pares "producto de consumo vs. API/plataforma de la misma empresa" (OpenAI/ChatGPT, Anthropic API/Claude, GitHub/GitHub Copilot) se sembraron a mano en vez de por subagente, para controlar con precisión sus alias — ver más abajo.
- **Dedupe automatizado, no solo de nombres — también de alias, y contra las 513 filas existentes**: un pase de verificación (case-insensitive, por script) encontró y corrigió **10 colisiones reales de alias** entre entradas nuevas (o entre una nueva y una ya existente) que habrían dejado dos `catalog_id` distintos empatados a `confidence = 1.0` para la misma cadena bancaria — indeterminista, PostgREST/Postgres resolvería el empate de forma arbitraria (orden físico de filas):
  - `Jasper` (`ai_writing`) vs. `Jasper Chat` (agente ai_assistant, descartada) y `Chatsonic` vs. `Writesonic` (mismo fabricante, alias `WRITESONIC.COM` duplicado — se descartó `Chatsonic` como entrada separada).
  - `HuggingChat` (descartada) vs. `Hugging Face` — mismo dominio `huggingface.co` y alias `HUGGINGFACE.CO` compartido.
  - `Amazon Q` / `Amazon Q Developer` vs. la entrada `AWS` ya existente (0004) — las 3 compartían el alias literal `AMAZON WEB SERVICES`; se quitó de las 2 entradas nuevas, dejando solo sus alias específicos (`AWS*AMAZON Q`, `AWS Q DEVELOPER`).
  - `Grok` vs. `xAI API` — alias `XAI CORP`/`X.AI` duplicados; se dejaron solo en la entrada de API, `Grok` se quedó con `GROK.COM`/`XAI *GROK`.
  - `DeepSeek` vs. `DeepSeek API` — alias `DEEPSEEK` duplicado; la entrada de API pasó a `DEEPSEEK API`/`DEEPSEEK PLATFORM`.
  - `Le Chat` vs. `Mistral AI` — alias `MISTRAL AI` duplicado; `Le Chat` se quedó con aliases específicos del producto de chat.
  - `ContentShake AI` vs. `Semrush` (0004, alias `SEMRUSH INC` ya existente) — se quitó `SEMRUSH INC` de `ContentShake AI`.
  - `Wordtune` vs. una futura entrada de `AI21 Labs` — alias genérico `AI21 LABS` cambiado a `AI21 LABS WORDTUNE` (específico del producto).
  - `v0` vs. `Vercel` (0004, alias `VERCEL INC` ya existente) — se quitó `VERCEL INC` de `v0`.
  - `ElevenLabs` fue un hallazgo tardío: se le había pedido al subagente de `ai_audio_voice` que la excluyera ("se añade a mano"), pero no se añadió en el primer borrador de la migración — detectado al preparar las filas del fixture del matcher (el usuario la cita literalmente como ejemplo de alias) y corregido antes de aplicar `db push`.
- **Los 5 pares "consumo vs. API", el caso de doble candidato pedido explícitamente — resueltos de forma determinista, no por casualidad**: `best_catalog_match()` (0007) da 1.0 a una coincidencia EXACTA (cadena completa o segmento tras el último `*`), y ninguna otra regla puede alcanzar ese techo — así que mientras cada miembro del par tenga como alias exacto la forma en que factura ESE producto concreto, y no comparta ese alias exacto con el otro miembro, no hay ambigüedad posible aunque los dos compartan marca/trigramas parecidos:
  - `"OPENAI *CHATGPT SUBSCR"` → segmento tras `*` = `"CHATGPT SUBSCR"`, alias exacto de `ChatGPT` → 1.0. `"OPENAI"` a secas → alias exacto de `OpenAI API` → 1.0. Ninguno de los dos alcanza 1.0 contra el otro candidato.
  - `"ANTHROPIC PBC"` → alias exacto de `Claude` → 1.0. `"ANTHROPIC API"` → alias exacto de `Anthropic API` → 1.0.
  - Verificado a mano regla por regla contra la implementación real de `best_catalog_match()` (no solo intención) antes de escribir el fixture — mismo rigor que el diseño original del matcher en 1.3 (ver bloque 1.3 más arriba).
- **Fixture del matcher ampliado de 200 a 220 filas** (`bank-statement-200.ts` — nombre del fichero sin cambiar para no tocar imports; el comentario interno ya deja claro que ahora son 220): 20 filas nuevas de gasto en IA, incluidos los 2 pares de doble candidato de arriba. `import-acceptance.test.ts` actualizado (`toHaveLength(220)`, descripción del test) — el criterio de aceptación original de TASKS.md 1.3 (≥70% sobre el CSV) sigue siendo el mismo umbral, solo sobre más filas; no se reabre el bloque 1.3.
- **Matiz de producto (pedido explícito): coste variable de las API de IA no se modela aparte**: un cargo de uso de `OpenAI API`/`Anthropic API`/etc. se reconcilia igual que cualquier otro `spend_record` del vendor — mismo importe/fecha/vendor, sin distinguir "suscripción fija" de "coste variable por uso". Modelar esa distinción (tracking de uso, alertas de burn-rate, proyección de gasto variable) es una evolución de producto futura, no inventada aquí sin un caso de uso confirmado — se deja anotado para cuando SPECS lo pida explícitamente.
- **i18n**: 7 claves nuevas en `Catalog.category.*` (es/en) y `CATALOG_CATEGORIES` (`src/features/catalog/types.ts`) ampliado — no hay ningún `switch`/`Record` exhaustivo por categoría en el código (se auditó explícitamente) que necesitara tocarse aparte; todo consumidor (selects de categoría en formularios, columna de categoría del listado de vendors) itera `CATALOG_CATEGORIES`/traduce por clave, así que las 7 nuevas categorías quedan disponibles automáticamente.
- **"Filtro por categoría del listado de vendors" — no existe como control interactivo, es una columna traducida**: se verificó `vendors/page.tsx`/`vendor-row.tsx` — la categoría se muestra como columna (`tCategory(vendor.category)`), no hay un desplegable de filtrado en esa vista hoy. Se verificó que la columna renderiza correctamente las 7 categorías nuevas (traducción presente, sin fallback a la clave cruda) — no se construyó un filtro nuevo, no pedido y fuera del alcance literal de "que las muestra correctamente".
- **`supabase db push` aplicado al proyecto remoto** (`mkrsicuvhnmljpurtwun`) para `0012_ai_catalog_seed.sql`, confirmado con `supabase migration list` (0012 presente en ambas columnas Local/Remote) — el propio éxito del push confirma que el bloque `do $$` localizó y dropeó los constraints reales sin error y que las 144 filas no chocaron con las 513 ya sembradas (índice único `lower(name)`).

## 2026-07-16 — Home pública real (tarea extra, adelantada de la Fase 4.3)

- **`docs/landing.html` no existía — el archivo real estaba en la raíz del repo** (`landing.html`, sin trackear en git). Se preguntó al usuario antes de inventar nada; confirmó la ruta real. El archivo se dejó tal cual (no se movió a `docs/` ni se comiteó) — es un mockup de referencia puntual del usuario, no un artefacto del repo como `docs/mockups.html`.
- **`HomePage` → `Home`**: el namespace de i18n del placeholder de 0.1 se renombró por completo (auditado: no lo usaba ningún otro fichero) en vez de convivir con uno nuevo — evita un namespace muerto.
- **Redirect de sesión activa reutiliza `getCurrentUserProfile()`**, mismo helper cacheado que ya usa `(app)/layout.tsx` para el guard inverso (redirigir a `/login` si NO hay sesión) — no se creó un segundo camino más ligero (`supabase.auth.getUser()` a pelo) solo para esta comprobación; es una request más, pero una sola función de verdad para "¿quién es el usuario actual" en todo el árbol de rutas.
- **`--teal-bright`/`#5FBFB4` — reutilizado como hex literal, no como variable CSS nueva**: `landing.html` introduce `--teal-bright:#5FBFB4` para los acentos de la sección oscura "cómo funciona" (no existe en `docs/mockups.html` ni en `globals.css`). Antes de añadir una variable nueva se comprobó si ese exacto valor ya se usaba en el código — sí: `src/components/shell/sidebar.tsx:35` ya pinta la "ly" del wordmark del sidebar con `text-[#5FBFB4]` inline. Se siguió ese mismo precedente literal (clases arbitrarias `text-[#5FBFB4]`/`bg-[#5FBFB4]/14`) en vez de promover un token nuevo — "mismos tokens ya existentes" se cumplió con más precisión así que inventando un nombre de variable para un valor que el código ya usa de esta forma.
- **Animación `drift` + `prefers-reduced-motion` — CSS puro, sin JS/`"use client"`**: `@keyframes drift`/`.animate-drift` en `globals.css`, con `@media (prefers-reduced-motion: reduce) { .animate-drift { animation: none } }` — mismos valores exactos que `landing.html` (26s linear infinite, `margin-left` de 26px a -26px). Verificado con Playwright (`page.emulateMedia({reducedMotion:'reduce'})` + `getComputedStyle(...).animationName`): `"drift"` en modo normal, `"none"` con reduced-motion — comportamiento confirmado, no solo declarado.
- **Pista de renovaciones del hero — datos de demostración estáticos, no reales**: `renewal-demo.tsx` reproduce 1:1 los 5 tickets/posiciones/tonos/importes literales de `landing.html` (Salesforce/Figma/HubSpot/Notion/ChatGPT Team). Es un componente deliberadamente distinto de la pista de renovaciones real del dashboard (`(app)/dashboard/renewal-track.tsx`, que si calcula posiciones a partir de datos reales de la org) — mismo patrón visual, dato de origen distinto, sin compartir código entre ambos porque uno es ilustrativo y fijo, el otro es real y dinámico.
- **Formato de moneda sin separador de miles en 4 dígitos (`9120 €`, `6240 €`) — mismo comportamiento de ICU ya documentado en el bloque 1.2**, no un bug nuevo: `Intl.NumberFormat('es', {style:'currency', currency:'EUR'})` en este Node/ICU no agrupa por debajo de 5 dígitos. Se mantuvo el formateador real (locale-correcto) en vez de forzar agrupación a mano, mismo criterio que 1.2.
- **Bug real encontrado en verificación visual — overflow del nav en móvil**: capturas con Playwright a 390px mostraron el botón "Crear organización" del nav cortado fuera del viewport (calculado: logo + 2 botones + gaps ≈ 388-390px, al límite exacto). El mockup estático (`landing.html`) no define ningún comportamiento responsive para `.topnav .cta` más allá de ocultar los enlaces centrales — habría tenido el mismo problema. Fix: el botón "Iniciar sesión" (ghost) se oculta por debajo de `sm:` (login sigue accesible desde `/signup` o navegando directo); ambos botones reducen padding/tamaño de fuente en móvil. Verificado con captura antes/después — el fix elimina el overflow sin tocar el layout desktop.
- **2 bugs reales encontrados al ejercitar el e2e de verdad, no solo con capturas**: `pnpm exec playwright test e2e/home.spec.ts` se corrió contra un server real (build+start local, sin necesidad de Supabase local para estas 4 rutas ya que solo comprueban "sin sesión" contra el proyecto remoto real) — (1) `test.skip(condición, motivo)` llamado a nivel de `describe` (no dentro del `test()`) salta TODOS los tests del bloque, no solo el siguiente — con un solo test por describe (patrón de `dashboard.spec.ts`/`shell.spec.ts`) esto es invisible, pero con 5 tests en el mismo describe saltó los 5. Fix: mover `test.skip(...)` dentro del cuerpo del test que de verdad lo necesita. (2) Dos `getByText`/`getByRole` con matching por substring (comportamiento por defecto de Playwright) resolvían a 2 elementos: "Cómo funciona" (nav) vs. "Ver cómo funciona" (CTA del hero), y "Tu pista de renovaciones"/"Your renewals runway" (título del demo) vs. el mismo texto dentro del subtítulo del CTA final. Fix: `{ exact: true }` en ambos. Los 4 tests que no requieren Supabase local pasan de verdad (no solo compilan); el 5º (redirect con sesión) se auto-salta aquí y se verifica en CI.
- **SEO básico vía `generateMetadata` en `page.tsx`** (primer uso de esta API en el repo): `title`/`description` por locale desde `Home.meta.*`, más `openGraph: {title, description, type: 'website'}` mínimo — el `metadata` estático del layout raíz (`[locale]/layout.tsx`) sigue existiendo como fallback para el resto de rutas; Next.js hace merge dando prioridad al nivel de página.

## 2026-07-16 — Ajuste responsive de la pista de renovaciones del hero (móvil)

- **Bug real reportado por el usuario tras la primera entrega**: a <640px la franja horizontal de 5 tickets no tenía sitio — el ticket de Salesforce (rojo, el protagonista del hero) quedaba recortado por la izquierda. `landing.html` no define ningún comportamiento responsive para `.demo`/`.track` más allá del scroll horizontal del contenedor (`overflow-x:auto`), así que no había una versión "correcta" del mockup que replicar aquí — se diseñó la solución mobile desde cero, sin desviarse del criterio dado en la propia petición.
- **Ambas variantes (lista móvil + franja desktop) viven siempre en el DOM, alternadas por CSS (`sm:hidden` / `hidden sm:block`)** — mismo patrón que ya usa `sidebar.tsx`/`bottom-nav.tsx` para el mismo tipo de swap sidebar↔bottom-nav, en vez de un `"use client"` con `matchMedia`/resize listener. Server Component puro, sin JS de layout en el cliente.
- **Orden de la lista móvil calculado (`.sort((a,b) => a.days - b.days)`), no asumido del orden del array de datos** — aunque los 5 tickets de `landing.html` ya estaban en orden ascendente de días por coincidencia, ordenar explícitamente evita que un cambio futuro en `TICKETS` rompa el orden silenciosamente.
- **Alerta compacta ligada a `tone === 'hot'`, no a "el primer ticket de la lista"**: se renderiza inmediatamente después del ticket cuyo tono es `hot` (hoy Salesforce, por ser el único con preaviso vencido) durante la iteración de la lista ordenada — así la relación causa→efecto se mantiene correcta aunque los datos de demostración cambien y el ticket urgente deje de ser el primero por casualidad.
- **Hallazgo propio durante la verificación visual, no pedido explícitamente**: el hint de cabecera ("próximos 120 días · posición = días restantes") deja de tener sentido en la lista vertical — no hay ninguna "posición" que leer. Se añadió `Home.demo.hintMobile` ("...ordenado por días restantes") y se alterna con el hint original vía las mismas clases `sm:hidden`/`hidden sm:inline`, en vez de dejar una frase que ya no describe lo que el usuario ve.
- **Verificado con capturas reales, no solo revisión de código**: Playwright a 375px (lista completa, Salesforce sin cortar, alerta justo debajo), 768px (franja intacta, sin cambios), y el límite exacto del breakpoint (639px → lista, 640px → franja) — confirma que el corte ocurre exactamente en `sm:` (640px), no en un punto aproximado. `getComputedStyle(...).animationName` confirmado `"drift"` en la franja desktop normal y `"none"` con `prefers-reduced-motion: reduce` emulado — la lista móvil no tiene la clase `animate-drift` en absoluto, no depende de que el navegador respete la media query para quedarse quieta.
- **2 tests e2e nuevos, y un bug de Playwright encontrado al escribirlos**: como las dos ramas (móvil/desktop) están siempre en el DOM, cualquier `getByText` sobre contenido presente en ambas (p.ej. "auto-renueva en 5 días") resuelve a 2 elementos y viola el modo estricto — el fallo apareció corriendo el test de verdad, no en la revisión. Fix: `locator.and(page.locator(':visible'))` para quedarse solo con la instancia realmente visible en ese viewport. Los tests fijan el viewport con `page.setViewportSize()` y verifican, además de visibilidad, que el `boundingBox()` del aviso de Salesforce cae dentro de los 375px del viewport (no solo que "existe en el DOM", que no habría detectado el bug original del recorte).

## 2026-07-16 — Soporte multi-empresa (feedback de uso real) + menú Ajustes + polish del formulario de contrato

- **`companies` es una dimensión INDEPENDIENTE de `departments`, decisión de producto explícita del usuario**: empresa = quién paga (sociedad legal del grupo), departamento = quién usa (transversal a todo el grupo — el mismo departamento de Ingeniería puede tener gasto repartido en varias empresas). `contracts` gana `company_id` nullable exactamente en paralelo a `department_id` (mismo `on delete set null`, misma validación cruzada de org en `create_contract`/`update_contract`, mismo fallback "Grupo / Sin asignar" en agregados si en el futuro se añade desglose por empresa al dashboard — **no se añade ese desglose en este bloque**, solo se deja el dato bien modelado, pedido explícito del usuario). No hay FK ni relación entre `companies` y `departments` — un contrato puede tener cualquier combinación de las dos, incluidas ambas nulas.
- **Invariante "una empresa por defecto por org"**: `companies.is_default boolean`, forzado con un índice único parcial (`companies_org_default_unique_idx on companies(org_id) where is_default`). `create_company`/`update_company` desmarcan la empresa default anterior de la misma org (en la misma transacción, antes del insert/update) cuando `p_is_default = true` — evita que dos filas con `is_default = true` en la misma org violen el índice. El campo se guarda pero no se consume por ninguna UI todavía (ningún flujo de auto-selección lo usa aún); queda listo para cuando haga falta.
- **RPCs `create_company`/`update_company`/`delete_company` — org_admin únicamente, mismo esqueleto exacto que `create_department`/`update_department`/`delete_department` (0002_departments.sql)**: `security definer`, `set search_path=''`, comprueban `role = 'org_admin'` del caller, escriben `audit_log` en la misma transacción, RLS bloquea insert/update/delete directos (`with check(false)`/`using(false)`) — mismo patrón ya establecido para todo dato organizativo sensible (departments, invitations). `delete_company` no comprueba contratos en uso: `contracts.company_id` es `on delete set null`, igual que `department_id`, así que el borrado nunca falla por FK — los contratos afectados caen a "Grupo / Sin asignar".
- **Creación inline restringida a `org_admin`, aunque el formulario de vendor+contrato lo usan también `finance`/`it_admin`**: el botón "+ Nueva empresa"/"+ Nuevo departamento" en `ContractFields` solo se renderiza si `canManageOrgDimensions` (= `role === 'org_admin'`, calculado en cada Server Component padre con `getCurrentUserProfile()`) es `true` — mismo principio que CLAUDE.md regla 5 ("la UI solo oculta"): la RPC ya rechaza a cualquier no-org_admin independientemente de lo que la UI muestre, así que ocultar el botón para finance/it_admin no es la única defensa, es solo evitar un botón que fallaría siempre para ese rol. Sin este flag, un usuario finance vería un botón de creación que su rol nunca puede usar.
- **Mini-modal de creación inline deliberadamente mínimo (decisión del usuario tras preguntarlo explícitamente)**: empresa = nombre + CIF opcional; departamento = solo nombre. Sin selector de manager (departamento) ni checkbox de "empresa por defecto" (empresa) en el modal — ambos se configuran después en Ajustes → Empresas/Departamentos. Motivo: mantener el modal rápido/mínimo y evitar tener que enhebrar la lista de `members` (necesaria para el selector de manager) a los 3 puntos de uso de `ContractFields` (alta combinada, "+ Añadir contrato", edición inline de contrato) solo para un caso de uso secundario del flujo rápido.
- **`createCompany`/`updateCompany` (y, por extensión aditiva, `createDepartment`) devuelven `ActionResult & { id?: string }`**, no solo `{success}` — mismo patrón ya usado por `assignSeat` en `src/features/vendors/actions.ts` (que añade `overCapacity` al resultado estándar). Necesario para que el mini-modal de creación inline pueda seleccionar la empresa/departamento recién creado actualizando solo estado local de React, **sin `router.refresh()`** — un refresh en mitad de un formulario de alta de vendor+contrato sin guardar arriesgaría el resto de campos ya rellenados por el usuario. `department-form.tsx` (uso preexistente de `createDepartment`, que sigue haciendo `router.refresh()` porque ahí sí es la página completa de gestión) ignora el campo `id` extra sin cambios.
- **`src/components/ui/select.tsx` y `src/components/ui/dialog.tsx` nuevos**: wrappers de `@base-ui/react/select` y `@base-ui/react/dialog` (mismo paquete y mismo patrón de wrapping ya usado por `Menu` en `src/components/shell/user-menu.tsx` y por `Input`/`button.tsx` — envolver la primitiva de Base UI en vez de tirar de la CLI de shadcn, que requeriría red durante la implementación). `Select.Root` soporta `name`/`form` con un input oculto integrado, así que es un reemplazo directo del `<select>` nativo sin tocar el patrón `formData.get(...)` que ya usan todos los server actions del repo. Se usan para pulir el `<select>` de `billingCycle`/`status`/empresa/departamento en `ContractFields` (pedido explícito: "el formulario de contrato usa estilo nativo del navegador") y el input de archivo pasa a usar `<Input type="file">` (que ya tenía clases `file:*` completas sin usar ningún consumidor). **Alcance deliberadamente acotado al formulario de contrato**: no se retroalimentaron otros `<select>` nativos preexistentes del repo (categoría/owner de vendor en `new-vendor-form.tsx`, manager de `department-form.tsx`/`department-row.tsx`, rol/departamento de `member-row.tsx`) — fuera del alcance literal del pedido y evita ampliar el blast radius de esta tarea.
- **`/team/departments` gana su primera entrada real de nav**: existía desde el bloque 0.3 pero nunca se añadió a `NAV_ITEMS` (`src/components/shell/nav-items.ts`) — solo accesible por URL directa (ver DECISIONS.md 0.4, "sin UI de shell compartido todavía"). Se añade junto con la entrada nueva de `/team/companies`, ambas `section: 'settings'`, `roles: ['org_admin']` (mismo criterio que `teamRules`), después de `teamRules` en el array.

## 2026-07-16 — Rebrand Stackly → StackX (sistema de diseño "23 · Lima quirúrgica")

- **Nombre interno del código NO se renombra**: package name (`stackly` en `package.json`), `project_id` de `supabase/config.toml` (proyecto remoto ya enlazado, `mkrsicuvhnmljpurtwun`) y la clave de config de sesión Postgres `stackly.bypass_user_column_guard` (usada por `protect_users_columns()`, ya desplegada — ver bloque 0.3) se mantienen tal cual. Sin beneficio de cara al usuario y riesgo real: renombrar el `project_id` podría romper el link de la CLI con el proyecto remoto, y renombrar la clave de sesión exigiría una migración coordinada (actualizar el trigger y las RPCs `update_user_role`/`update_user_department` en el mismo despliegue) sin ninguna ganancia funcional. Solo cambia la marca visible: nombre de producto, wordmark, i18n, metadata, emails.
- **Peligro/urgencia de renovación se queda en rojo real (`#D92D20`), override deliberado sobre el mock de la piel 23**: la piel "23 · Lima quirúrgica" de `docs/design-stackx.html` usa tinta/lima también para su estado de peligro, pero lima/negro se lee como "neutral/bien" — inaceptable para una alerta de renovación crítica. Se tomó prestado el tratamiento de peligro de la piel "58 · Prensa marcador" (rojo real) en su lugar. Aprobado explícitamente por el usuario al iniciar el rebrand.
- **Hover del CTA primario (`#2A2E30`), decisión de diseño no especificada por el usuario**: el CTA pasó de fondo teal a fondo casi negro (`#15181A`) con texto lima (`#C6FF3E`); el oscurecimiento de hover que tenía el teal no tiene sentido sobre un fondo ya casi negro, así que se eligió un gris carbón ligeramente más claro como tratamiento de hover. Ajustar si en revisión visual no convence — no hay mockup que lo especifique.
- **`docs/` no se re-skinea en cada rebrand**: `docs/mockups.html` (mockup original pre-StackX) se deja tal cual, sin ni siquiera cambiar "Stackly" por "StackX" en su contenido — es el registro histórico de dónde viene el diseño, y su valor está precisamente en documentarlo sin tocar. Se le añadió solo un comentario HTML al inicio marcándolo como histórico y apuntando a `docs/design-stackx.html` como referencia vigente. Regla general: un artefacto de diseño superado se marca histórico, no se edita para que combine con la marca nueva. `landing.html` (prototipo estático suelto en la raíz, untracked, ya implementado hace semanas en la landing real de Next.js) se eliminó por ser un artefacto de trabajo que ya cumplió su función y solo generaba confusión.
- **Bug real cazado solo por verificación visual, no por `pnpm typecheck`/`pnpm build`: el token `--font-disp` borrado por error**: al migrar la tipografía (Bricolage Grotesque + Instrument Sans → Inter única), la intención era mantener el alias `font-heading`→`font-disp`→`font-sans` para no tocar los ~36 archivos que usan la clase Tailwind `font-disp` directamente. En la ejecución real se borró la línea `--font-disp` del bloque `@theme inline` de `globals.css` por completo (en vez de solo repuntarla a `--font-sans`), asumiendo erróneamente que esos 36 archivos usaban `font-heading` (que sí quedó bien aliasado) en vez de `font-disp` (la clase realmente usada). Consecuencia: la utilidad `font-disp` habría dejado de generarse — clase inerte, sin `font-family` aplicado — en cabeceras de dashboard, vendors, team, auth y toda la landing. **Ni `pnpm lint`, ni `pnpm typecheck`, ni `pnpm build` lo detectan**: una clase de Tailwind que no genera ninguna regla CSS no es un error de TypeScript ni de build, es una regresión puramente visual (degradaba en silencio a la fuente heredada, que además coincide visualmente con Inter en body, así que ni siquiera se ve muy distinto — el tipo de bug que pasa una revisión de código superficial). Se detectó al revisar capturas de pantalla reales de `/es` (Playwright contra `pnpm dev`) y confirmar mentalmente qué clases usaba cada `<h1>`/`<h2>` visto en pantalla, no por ningún gate automatizado. Arreglo: añadir `--font-disp: var(--font-sans);` de vuelta al `@theme inline`, en paralelo a `--font-heading`. **Refuerza la regla de CLAUDE.md de que el gate de cierre incluye verificación visual real (screenshots), no solo lint/typecheck/test/build** — este bug concreto es el ejemplo canónico de una clase de regresión (CSS/Tailwind token roto) que los cuatro comandos automatizados no pueden atrapar por construcción.

## 2026-07-16 — Rediseño UI post-rebrand: sidebar claro, jerarquía de botones, patrón "ficha" de vendor

- **Bug real, causa raíz confirmada por lectura de código (no solo hipótesis): `--font-sans` definido en `<body>` pero consumido en `<html>`**. `src/app/[locale]/layout.tsx` aplicaba las clases de variable de next/font (`inter.variable`, `ibmPlexMono.variable`) en `<body>`, mientras `globals.css` tenía `html { @apply font-sans; }`. Las custom properties de CSS heredan hacia abajo en el DOM — `<html>` es ancestro de `<body>`, no puede leer una variable definida en su descendiente. Resultado: `font-family: var(--font-sans)` en `<html>` resolvía a inválido, fallback serif del navegador (Times), heredado a todo el árbol porque nada más re-aplicaba `font-sans`. Preexistía al rebrand (con Bricolage/Instrument también pasaba, solo que el fallback coincidía menos visiblemente). Fix de una línea: mover las clases de variable de `<body>` a `<html>` en `layout.tsx`; `globals.css` no se tocó. Confirmado con Playwright: `getComputedStyle(document.body).fontFamily` pasó de heredar el fallback a "Inter, Inter Fallback" en landing/login/signup/dashboard/vendors.
- **Segundo bug real, encontrado durante la verificación visual autenticada (no por lint/typecheck/build, que pasaron en verde con el bug presente)**: al convertir `nav-link.tsx` a "use client" para poder usar `usePathname()` (necesario para el estado activo del ítem de nav), el prop `item: NavItem` seguía pasándose entero desde `Sidebar`/`BottomNav` (Server Components), incluyendo `item.icon`, una referencia a un componente de lucide-react. Cruzar el límite servidor-cliente de RSC con una referencia a componente (no serializable) rompe el render con "Only plain objects can be passed to Client Components from Server Components" en runtime, error que solo aparece al visitar la página real, invisible para tsc/eslint/next build. Fix: NavLink pasa a recibir `href: string | null` + `icon: ReactNode` (ya renderizado) en vez de `item: NavItem`; Sidebar/BottomNav (que siguen siendo Server Components) renderizan el icono ellos mismos y pasan el elemento ya construido, que sí es serializable a través del límite RSC. Regla general para el resto de la sesión y futuras: al convertir cualquier componente a "use client" que recibe props desde un padre Server Component, auditar cada prop por serializabilidad, con especial atención a objetos que embeban componentes/iconos/funciones.
- **Patrón "primario contextual" de la cabecera de ficha**: `pickPrimaryAction` (`src/features/vendors/primary-action.ts`) decide un único botón primario por vendor según su estado: renovación crítica -> "Renegociar" (atajo de UI al formulario de contrato en la pestaña Contratos, no un flujo de renegociación nuevo, eso es bloque 3.2/3.3, no implementado); sin contrato activo -> "+ Añadir contrato"; caso normal -> "Editar". Crítico se define como `actionableDaysUntil` (`renewal.ts`) menor o igual a 7, donde `actionableDaysUntil` es días hasta la fecha de renovación si el contrato no auto-renueva, o días hasta que vence el preaviso de cancelación si sí auto-renueva. Esta misma función se reutiliza en `vendor-rail.tsx` para decidir si la fecha de renovación se resalta en rojo. Inicialmente se implementó por separado con `renewalTone(daysUntil(...))` (solo fecha bruta, sin contar preaviso) y quedó inconsistente con la cabecera (un contrato marcado "Renegociar" no salía en rojo en el rail); se detectó revisando la propia captura de pantalla generada, no por revisión de código, y se corrigió extrayendo `actionableDaysUntil` a `renewal.ts` como fuente única de verdad.
- **Jerarquía de botones** (`src/components/ui/button.tsx`): primario = variant="default" (bg-primary/text-primary-foreground, ya correcto); secundario = variant="outline" (blanco/border-line/ink, ya coincidía, no se renombró la variante para no romper call sites); terciario = variant="link", cambiado a subrayado permanente en vez de solo hover; destructivo, bug real corregido, antes bg-destructive/10 (relleno rosa pálido permanente, se leía como "pill rosa"), ahora border-destructive bg-background text-destructive en reposo con hover:bg-destructive/10 solo como feedback de interacción. Toda acción destructiva (vendor, contrato, departamento, empresa, asiento) pasa por un ConfirmDialog nuevo (`src/components/ui/confirm-dialog.tsx`) en vez de window.confirm o sin ninguna confirmación (departamento/empresa no tenían ninguna guarda antes de este cambio).
- **Ficha de vendor con tabs** (Detalles/Contratos/Asientos/Documentos/Notas) sustituye la página anterior, que mostraba el formulario de edición siempre visible. Patrón "ficha de lectura + Editar" aplicado también dentro de la pestaña Contratos (fila compacta por contrato, expandible a formulario solo al pulsar Editar; solo un contrato editable a la vez, con ConfirmDialog de "descartar cambios" si se intenta abrir otro con cambios sin guardar). Fuera de alcance deliberadamente: la pestaña Notas usa el campo `notes` de texto plano que ya existía, un historial de negociación con autor/fecha por entrada es bloque 3.3 (no implementado); "Renegociar" es un atajo de UI al formulario de contrato existente, no un flujo de renegociación real.
- **Procedimiento estándar para verificación visual autenticada contra el Supabase remoto (sin Supabase local en esta máquina)**: cuando una captura real requiere sesión iniciada y no hay cuenta de prueba confirmada disponible, el patrón aprobado es: (1) usar SUPABASE_SERVICE_ROLE_KEY (verificar longitud antes de asumir que tiene valor real, no solo que la clave existe en el archivo) para crear un usuario con email_confirm=true, con email de un dominio reservado no real (*.invalid) y organización marcada como borrable; (2) contraseña generada aleatoriamente en memoria en el mismo script, nunca impresa a consola ni escrita a ningún archivo del repo; (3) sembrar datos mínimos representativos (vendors con contratos en los estados relevantes) directamente en las tablas vía el cliente de service-role, bypassando las RPCs que exigen sesión real; (4) Playwright inicia sesión de verdad contra la app y navega/hace capturas; (5) al terminar, borrar contratos, vendors, perfil, organización y usuario de auth en ese orden, y verificar con una query que las tablas quedan en 0 filas antes de dar la verificación por buena. Nunca ejecutar los pasos (1)/(3) sin autorización explícita del usuario en esa misma conversación, es una escritura a estado remoto compartido; un intento previo de hacerlo sin pedir permiso fue bloqueado correctamente por el clasificador de modo automático de Claude Code.

## 2026-07-16 — Mini-bloque: gráficas del dashboard (donut, evolución de gasto, barras departamento/empresa)

- **Librería: SVG propio, sin dependencia nueva**: no había ninguna librería de charts en `package.json` (ni recharts/d3/chart.js). Se descartó añadir una: el sistema de tokens de StackX exige colores exactos (`--danger`/`--warning`/`--success`/`--lime`/`--barfill`) y formas concretas (radios de 4px, área con wash de `--lime-soft`) que es más simple garantizar a mano con SVG que pelear con el theming por defecto de una librería de ~90kb+ para solo 3 widgets. Se consultó el skill `dataviz` (procedimiento: forma → color por el trabajo que hace → validar → marks/spacers → hover → accesibilidad) antes de escribir código.
- **Donut "Estado del stack" agrupa por VENDOR, no por contrato**: `contracts.renewal_date` es `not null` (0005_vendors_contracts.sql), así que un bucket "sin fecha" a nivel de contrato nunca se poblaría. `buildStackStatus()` (`src/features/dashboard/aggregate.ts`) clasifica cada vendor con `status = 'active'` (mismo filtro que `activeVendorCount`, ya existente) por el `actionableDaysUntil` mínimo entre sus contratos activos; un vendor activo sin ningún contrato activo cae en el bucket **"Sin contrato activo"** (nombre elegido tras feedback explícito del usuario — "sin fecha" es confuso hablando de vendors). Ese bucket es un link real a `/vendors` con una pista de acción ("→ añadir su primer contrato") en vez de solo texto — no existe hoy un filtro de vendors "sin contrato" en `/vendors`, así que el link va a la lista completa; añadir ese filtro queda como mejora futura, fuera de alcance de este mini-bloque.
- **Umbrales de renovación consolidados de verdad**: `CRITICAL_THRESHOLD_DAYS = 7` y `WARNING_THRESHOLD_DAYS = 45` se extrajeron como constantes exportadas de `src/features/vendors/renewal.ts` (antes `renewalTone()` los hardcodeaba inline y `primary-action.ts` tenía su propia copia de `7`). Ahora `renewalTone()`, `pickPrimaryAction()` y `buildStackStatus()` leen las mismas dos constantes — un solo sitio para cambiar el criterio de urgencia en toda la app, sin cambiar ningún comportamiento existente (mismos valores 7/45, tests de `renewal.test.ts`/`primary-action.test.ts` no se tocaron y siguen en verde).
- **Evolución de gasto — "reconciliado" = `spend_records.vendor_id is not null`**: se le asignó vendor real vía `link_reconciliation()`/`create_vendor_from_reconciliation()` (bloque 1.3); un movimiento todavía en `reconciliation_queue` con `status = 'pending'` no cuenta como gasto real todavía. Nueva función SQL `dashboard_monthly_spend(p_months integer default 12)` (`supabase/migrations/0014_dashboard_monthly_spend.sql`), agrupando por mes+moneda en Postgres (agregación real, no N+1: la única iteración en JS es sobre el puñado de filas `(mes, moneda)` resultantes, para la conversión de divisa con `convertAmount()` ya existente). Sin `security definer`: no hace falta bypass de RLS, la política `spend_records_select` (org + rol finance/it_admin/org_admin) se aplica igual al ejecutarse como invoker — mismo patrón que `search_saas_catalog()` (0003), que tampoco es security definer.
- **Meses sin gasto se rellenan a 0, no se omiten**: `buildMonthlySpendSeries()` (`src/features/dashboard/monthly-spend.ts`) genera siempre los N meses de la ventana en orden, así un mes sin ningún `spend_record` reconciliado se lee como "0€ ese mes" en vez de desaparecer del eje. Estado vacío (`monthsWithData < 2`) muestra un mensaje con link a `/import` en vez de un chart plano de ceros, que sería engañoso.
- **Gasto por departamento pasa de tabla a chart de barras real, con toggle Departamento/Empresa**: `DepartmentSpendTable.tsx` se eliminó; `SpendByGroupChart` (`src/app/[locale]/(app)/dashboard/spend-by-group-chart.tsx`) usa el primitivo `Tabs` ya existente (`src/components/ui/tabs.tsx`, mismo patrón que la ficha de vendor) para el toggle, y reutiliza `buildDepartmentSpend`/nueva `buildCompanySpend` (agregación idéntica, extraída a un helper interno `buildSpendByKey` para no duplicar la lógica de agrupar+sumar+ordenar). La barra mayor de cada vista se pinta en `--lime`, el resto en el nuevo token `--barfill` (`#EDF0EA`, valor literal del skin 23 de `docs/design-stackx.html`), radio 4px.
- **Patrón de accesibilidad de los 3 charts, aplicado de forma consistente**: el SVG/las barras visuales son decorativos (`aria-hidden="true"`); los datos reales viven en texto normal siempre visible (la leyenda del donut ya es texto real, no necesita nada más) o en una `<table className="sr-only">` junto al chart (barras, evolución de gasto). `sr-only` usa `clip`, no `display:none` — el nodo sigue en el árbol de accesibilidad, así que `getByRole("row", ...)` de `e2e/dashboard.spec.ts` sigue encontrando las mismas filas (departamento/importe/nº vendors) sin que se tocara ese test. Condición explícita del usuario al aprobar el enfoque: ninguna barra "pierde" el dato que tenía la tabla que sustituye — nombre, importe en mono y nº de vendors siguen siendo texto visible normal en cada fila del chart, no solo altura/anchura de barra.
- **Hover/tooltip solo en el chart de evolución de gasto, no en donut/barras**: el dataviz skill pide una capa de hover por defecto en cualquier chart interactivo, pero en donut y barras el dato ya está 100% visible como texto de forma permanente (leyenda con conteos; nombre+importe+vendors junto a cada barra) — un tooltip flotante ahí sería redundante. La evolución de gasto sí lleva crosshair + tooltip flotante (mes + importe) porque con 12 puntos no es viable etiquetar cada uno directamente sin saturar el chart.
- **2 bugs reales encontrados solo en verificación visual autenticada (no por lint/typecheck/build)**: (1) el área del chart de evolución de gasto se pintaba con un `fill` sólido (`--lime-soft` al 100%) en vez del wash semitransparente que pedía el propio plan — tapaba 3 de las 4 etiquetas del eje Y allí donde la curva quedaba alta a la derecha (justo donde se posicionan los labels). Fix: `fill="var(--lime)" fillOpacity={0.12}` + las etiquetas de gridlines/meses se repintan DESPUÉS del área/línea en el orden de pintado SVG, nunca antes, para que un mark de datos no pueda taparlas. (2) el título de la card de `SpendByGroupChart` quedaba fijo en "Gasto por departamento" aunque el toggle activara la pestaña Empresa — el `<h2>` leía siempre `t("departmentSpend.title")` en vez de condicionar al `activeTab`. Ninguno de los dos lo detectan `pnpm typecheck`/`pnpm build` (son bugs de composición visual/z-order y de texto estático, no de tipos) — mismo patrón que otros bugs ya documentados en este archivo que solo aparecen en una captura real.

## 2026-07-16 — Backlog futuro: inventario de herramientas internas (vibe-coded/no-code)

- **Motivación**: la era del "vibe coding" (herramientas construidas in-house con asistentes de IA, plataformas low-code/no-code) genera una forma de shadow IT que los gestores de gasto SaaS clásicos no cubren — no son un vendor de terceros con una factura mensual, son código propio con un owner técnico, un hosting y APIs externas que consume, y el mismo riesgo de "nadie sabe que existe esto" cuando su creador deja la empresa. Se registra como diferenciador de producto frente a la competencia (herramientas de gestión de SaaS que solo ven gasto de terceros), a petición explícita del usuario, para no perder la idea antes de que llegue su fase.
- **Por qué "post-Fase 3" y no una fase intermedia**: el concepto reutiliza dos piezas que solo existen a partir de ahí — el motor de alertas genérico de 2.1 (cron diario + tabla `notifications`, para el estado "huérfana") y el patrón de vinculación a `spend_records` ya existente desde 1.3 (para correlacionar el gasto de API que una herramienta interna consume). Construirlo antes duplicaría infraestructura que 2.1 ya va a crear. Se añadió como **Fase 6, backlog sin comprometer** (`docs/TASKS.md`) en vez de intercalarlo entre fases ya numeradas, para no reordenar el roadmap existente por una idea todavía sin diseño detallado.
- **Diseño deliberadamente NO decidido todavía**: el checklist de 6.1 es un placeholder de alto nivel (campos propuestos: owner técnico, repo/URL, hosting, APIs consumidas, criticidad, estado de riesgo vía `vendors.source = 'internal'`), no una especificación cerrada — quedan sin resolver, a propósito, preguntas como si "repo/URL" necesita verificación de que el owner técnico tiene acceso real, o cómo se detecta automáticamente que una API consumida corresponde a un `spend_record` concreto (¿match manual, o mismo fuzzy matcher de 1.3?). Se decidirán al planificar el bloque, no ahora.

## 2026-07-16 — Fix UI: toggle Departamento|Empresa ilegible y barras del chart

- **Bug real: el segmento activo del toggle usaba `bg-ink`/`text-lime-ink`** — `lime-ink` (`#2A3D00`) es el tono oscuro pensado para texto SOBRE un fondo lima claro (ver sidebar/badges), no para texto sobre `ink` (`#15181A`, casi negro): texto oscuro sobre fondo casi negro, prácticamente ilegible. El token correcto para "activo estilo CTA primario" ya existe (`--primary`/`--primary-foreground`, `#15181A`/`#C6FF3E`, el mismo que usa `button.tsx` variant="default") — el toggle no lo reutilizaba, tenía su propio `data-[active]:bg-ink data-[active]:text-lime-ink` ad hoc.
- **`SegmentedControl` extraído como componente del design system** (`src/components/ui/segmented-control.tsx`), no un parche solo en `spend-by-group-chart.tsx`: reutiliza los mismos primitivos base-ui de `Tabs` (mismo estado/teclado/`role="tablist"`/`role="tab"`), solo cambia el tratamiento visual (contenedor `bg-surface`/`border-line`/`rounded-btn`, segmento activo `bg-primary`/`text-primary-foreground`, foco `ring-ring/50` = `#E9FFB8`). El `Tabs` de subrayado (`vendor-ficha.tsx`, tabs de contenido) no se tocó — es un patrón visual distinto y correcto para ese caso.
- **Barras del chart — track de fondo siempre visible**: antes la barra sin relleno visible detrás no daba sentido de escala a valores pequeños. Ahora cada fila es un track `bg-line/40` a ancho completo con la barra de valor superpuesta (`position: absolute`, ancho = %), altura bajada de 10px a 9px (rango pedido 8-10px).
- **Nuevo token `--lime-chart: #B8E834`** (lima menos saturado, `globals.css`): el `--lime` de acento (`#9BE000`, pensado para CTAs/glifos grandes) resultaba "demasiado grito" a todo lo ancho de una barra de datos. Se usa solo en la barra mayor de cada chart; el resto sigue en `--barfill` (`#EDF0EA`, ya existente desde el mini-bloque de gráficas del 2026-07-16).
- **Verificación visual autenticada contra el Supabase remoto**: siguiendo el procedimiento ya documentado más arriba (usuario/org temporal `*.invalid` vía `service_role`, capturas con Playwright, limpieza verificada con conteo de filas a 0 al final) — autorizado explícitamente por el usuario en esta conversación antes de ejecutar el paso de escritura. Confirmado en capturas: toggle legible en ambos idiomas/tabs (Departamento activo con fondo negro/texto lima, Empresa igual al cambiar), barras con track visible y solo la mayor en `--lime-chart`.

## 2026-07-16 — Bloque 2.1 Motor de alertas de renovación

- **Decisión de cron, pendiente desde la migración a Railway: `pg_cron` en Supabase, no un cron de Railway a un endpoint firmado** — se propusieron ambas opciones con pros/contras concretos para este stack (qué falla, cómo se observa, cómo se testea, comportamiento a doble ejecución) y el usuario eligió `pg_cron`. Motivo de peso principal: cero dependencia de que el contenedor de Railway esté sano con las env vars correctas — exactamente la causa raíz del incidente de 500 en producción del 2026-07-15 (ver entrada de esa fecha) — y una garantía de idempotencia real (constraint de Postgres) en vez de lógica de aplicación. Coste aceptado: la lógica de umbrales vive en PL/pgSQL (`evaluate_renewal_alerts()`, `supabase/migrations/0015_renewal_alerts.sql`) en vez de TypeScript.
- **`pg_cron` disponible sin configuración manual previa en el proyecto remoto**: no había forma de verificarlo de solo lectura con la CLI 2.75 (no tiene un comando de "ejecutar SQL arbitrario" contra el proyecto enlazado, y esta máquina no tiene `psql` ni Docker para usar las credenciales efímeras de `supabase db dump --dry-run` como en el bloque 0.2). Se optó por escribir la migración completa (`create extension if not exists pg_cron;` incluido) y aplicarla directamente con `supabase db push` — si la extensión no hubiera estado disponible, el push habría fallado con un error claro y sin efectos secundarios. `supabase db push`/`supabase migration list` confirmaron 0015 aplicada sin error en local Y remote.
- **`notifications` amplía el shape genérico de SPECS §4**: la tabla de la spec (`user_id, type, payload jsonb, channels, sent_at`) no lleva `org_id` (violaría la regla 1 de CLAUDE.md, multi-tenancy obligatoria) ni nada para idempotencia real ni para "no leída". Se añadieron `org_id`, `contract_id` (FK, solo para dedupe/trazabilidad server-side), `threshold_days` (90/60/30/7, o **sentinela `-1` para "preaviso de cancelación vencido"**) y `read_at`. Idempotencia real: `unique index (contract_id, user_id, threshold_days) where type = 'renewal_alert'` — ejecutar `evaluate_renewal_alerts()` dos veces el mismo día no puede insertar la misma alerta dos veces, lo garantiza el propio índice vía `on conflict do nothing`, no un chequeo previo en la función.
- **`payload` es una foto autosuficiente, la UI nunca hace join contra `contracts`**: el destinatario de una alerta puede ser un `employee`/`manager` marcado como `owner_user_id` de un vendor sin tener acceso RLS a la tabla `contracts` (esa RLS exige `finance`/`it_admin`/`org_admin`, ver bloque 1.2) — si la campanita intentara resolver `contract_name`/`vendor_name` con un join en el momento de mostrar la notificación, fallaría en silencio (0 filas) para ese destinatario. En vez de eso, `evaluate_renewal_alerts()` congela `vendor_name`, `contract_name`, `renewal_date` y `days_until`/`notice_expired` dentro de `payload` en el momento de generar la alerta — mismo principio que la materialización de pasos del motor de aprobaciones (SPECS §6). La FK `contract_id` sigue existiendo (necesaria para el unique index de idempotencia) pero ningún código de cliente la usa para navegar.
- **Umbrales por coincidencia exacta de día, no `<=` acumulativo**: `(renewal_date - p_today) in (90, 60, 30, 7)` — con un cron diario, `days_until` decrece 1 cada día y pasa por cada umbral exacto una sola vez de forma natural; usar `<=` habría generado una fila el primer día dentro de cada ventana (protegida igual por el unique index) pero complica el test de "ninguna alerta de más": un contrato a 89 días quedaría "dentro de 90" con `<=` y generaría ruido conceptual aunque el índice lo dedupe. El aviso de preaviso vencido usa la misma filosofía: dispara quand `renewal_date - cancellation_notice_days = p_today` (el día exacto en que el deadline accionable —`actionableDaysUntil` de `renewal.ts`— llega a 0), solo si `auto_renews` y `cancellation_notice_days > 0` (mismo condicional que ya usa el `noticeWarning` del dashboard, bloque 1.5).
- **Destinatarios — un `join ... on (u.id = owner_user_id or u.role = 'finance')` con `distinct`**: cubre "owner + finance" y "solo finance si no hay owner" (comparar contra `owner_user_id is null` nunca es true) en una sola query, sin caso especial. Si el owner también es finance, `distinct` (más el unique index como red de seguridad) evita la fila duplicada.
- **`evaluate_renewal_alerts()` con permisos revocados a `anon`/`authenticated`**: es `security definer` (necesita cruzar todas las orgs, algo que ninguna política RLS permitiría a un usuario normal) pero eso NO la hace seguro exponerla como RPC pública — cualquier `authenticated` podría forzar una pasada anticipada del motor para otras orgs. `revoke execute ... from public/anon/authenticated` + `grant ... to service_role` (el propio `postgres`, dueño de la función, conserva su privilegio de invocarla igualmente, así que `cron.schedule` sigue funcionando). Verificado con un test explícito (`renewal-alerts.test.ts`, "un cliente authenticated no puede invocar...").
- **RLS de `notifications` — primera tabla del proyecto con aislamiento por FILA de usuario, no solo por rol/org**: `using (org_id = current_org_id() and user_id = current_user_id())`, con un helper `current_user_id()` nuevo (mismo patrón que `current_org_id()`/`current_user_role()` de 0.2). Mutaciones (insert/update/delete) bloqueadas a todo cliente; `mark_notification_read()`/`mark_all_notifications_read()` son RPCs `security definer` que solo tocan filas de `current_user_id()`.
- **Test con fechas simuladas — 8 contratos, `p_today` fijo, nunca `current_date`**: `src/features/renewals/renewal-alerts.test.ts` crea org real (signUp), un segundo usuario `finance` real (create_invitation + signUp, mismo patrón que `permissions.test.ts`), y siembra vendors/contracts vía `service_role` (mismo patrón que `search.test.ts`) con `renewal_date` calculado desde una fecha simulada fija (`2026-09-01`) — nunca se usa `current_date`, así que el resultado no depende de cuándo corra el test. Cubre exactamente los 8 casos pedidos (91/90/89/60/30/7/6 días + preaviso vencido aislado con `days_until=40`, que no coincide con ningún umbral, para no mezclar aserciones), el test explícito de idempotencia (segunda ejecución del mismo día inserta 0 filas nuevas), el caso "sin owner → solo finance", el bloqueo de ejecución directa por un cliente autenticado, y RLS de lectura propia. Como toda esta familia de tests, requiere Supabase local — se verifica en CI, no en esta máquina (ver "Entorno de desarrollo" en CLAUDE.md); en local solo se pudo confirmar que falla con el mismo `ECONNREFUSED 127.0.0.1:54321` que el resto de la suite de aislamiento/permisos, no un error distinto.
- **Panel mínimo: campanita con Menu de base-ui, mismo patrón que `UserMenu`**: `src/components/shell/notification-bell.tsx`, cliente, lee `notifications` directamente con el cliente de navegador (RLS ya scoped a `current_user_id()`, sin necesidad de Server Action) — carga al montar y al abrir el menú, badge de no leídas (máx. "9+"), clic en una fila la marca leída (optimista + RPC), "marcar todo como leído". Colocada en `Sidebar` (junto al wordmark) y `MobileHeader` (junto al selector de idioma), igual que `UserMenu`/`LocaleSwitcher` ya aparecen en ambos. Verificado en navegador (mismo procedimiento remoto autorizado que el fix de UI de esta misma fecha): badge baja de 3 a 2 al marcar una notificación como leída, persiste tras recargar (RPC real, no solo estado local).
- **Fuera de alcance deliberado de 2.1** (explícitamente el bloque 2.2 siguiente): envío real por email/Teams (`channels`/`sent_at` quedan poblados en 0/null), plantillas bilingües de contenido de notificación (los mensajes de la campanita usan `next-intl` con interpolación, no plantillas de email), y cualquier UI de "ver todas"/paginación más allá de las últimas 20.

## 2026-07-19 — Bloque 2.2 Canales (email + Teams)

- **Trigger de envío desacoplado del cron diario de 2.1**: un SEGUNDO `pg_cron` (`send-pending-notifications`, cada 15 min, `0016_notification_channels.sql`) llama vía `pg_net` a una ruta interna (`/api/cron/send-notifications`) que hace el envío real en TypeScript — a diferencia de la *generación* de alertas (2.1, deliberadamente pura SQL para no depender de Railway), el *envío* tolera un retraso: si Railway está caído, `sent_at` sigue `null` y la siguiente pasada de 15 min reintenta sin pérdida de datos. Se prefirió esto a hacer las llamadas HTTP a Resend/Teams directamente desde Postgres (`pg_net` puro) porque las plantillas bilingües y la tarjeta Adaptive Card de Teams son mucho más mantenibles y testeables en TypeScript (Vitest) que como texto/jsonb en PL/pgSQL.
- **URL base y secreto del cron vía GUC de Postgres, no hardcodeados en la migración**: `pg_net` corre dentro de Postgres y no puede leer `.env`. Se optó por `current_setting('app.settings.site_url'/'app.settings.cron_secret', true)`, leídos en cada ejecución por la función wrapper `trigger_send_pending_notifications()`, en vez de un literal en el SQL de `cron.schedule` — más flexible si cambia el dominio, pero exige un paso manual **fuera de las migraciones versionadas**: ejecutar una vez en el SQL editor del proyecto remoto:
  ```sql
  alter database postgres set app.settings.site_url = 'https://saascostchecker-production.up.railway.app';
  alter database postgres set app.settings.cron_secret = '<mismo valor que CRON_SECRET en Railway>';
  ```
  Si no se ejecuta, `trigger_send_pending_notifications()` sale con un `raise warning` y no hace nada (falla "abierto" hacia no-op, nunca rompe el cron ni el resto del sistema) — **pendiente de ejecutar** en esta sesión, ver checklist de `tasks/todo.md`.
- **Dos decisiones de seguridad añadidas tras revisión, ambas no negociables**:
  - **Validación anti-SSRF de `teams_webhook_url` duplicada en dos sitios, ninguno confiando en el otro**: el Zod del formulario (`isValidTeamsWebhookUrl` en `src/features/notification-settings/schemas.ts`) da feedback inmediato en la UI, pero la autoridad real es el RPC `upsert_org_notification_settings()` (`security definer`, revalida con el mismo regex en PL/pgSQL) porque nada impide invocar el RPC directamente sin pasar por el formulario. Ambos exigen `https` y un host anclado exactamente a `*.webhook.office.com` o `*.logic.azure.com` — un `like '%webhook.office.com%'` habría dejado pasar `https://evil.com/webhook.office.com`; el regex ancla el host real (`^https://([a-z0-9-]+\.)*(webhook\.office\.com|logic\.azure\.com)(/.*)?$`), verificado con ese caso trampa explícito en `schemas.test.ts`.
  - **Comparación del secreto del cron con `crypto.timingSafeEqual`, no `===`**: `/api/cron/send-notifications` compara `X-Cron-Secret` con `CRON_SECRET` en tiempo constante (con una guarda de longitud previa, ya que `timingSafeEqual` lanza si los buffers no miden igual) para no filtrar el secreto por temporización — primera ruta API del repo protegida por secreto compartido en vez de sesión de usuario, así que no había precedente que copiar.
- **`org_notification_settings` con RLS más estricta que `organizations`**: `organizations_select` (0001) permite lectura a toda la org; aquí ni siquiera lectura para no-`org_admin` — nadie más necesita ver o gestionar el webhook de Teams de la org. Mutaciones bloqueadas en RLS, todo pasa por RPC `security definer` (mismo motivo que `companies`: `audit_log_no_insert` bloquea el insert de auditoría a cualquier cliente que no sea `security definer`, así que una Server Action con `supabase.from(...).upsert()` directo —aunque la RLS ya bastara para el permiso— no podría cumplir la regla 6 de `CLAUDE.md`).
- **Semántica de fallo parcial de canal, sin retry por canal individual**: si al menos un canal aplicable tiene éxito (o si la org no tiene ningún canal habilitado), `sent_at` se marca igual y `channels` lleva solo los que tuvieron éxito — no hay reintento de "solo el canal que falló", se acepta perder ese canal puntual antes que trackear intentos por canal. Si TODOS los canales aplicables fallan, `sent_at` queda `null` y la siguiente pasada de 15 min reintenta el envío completo. Justificación: la campanita in-app (2.1) sigue siendo la garantía dura y siempre funciona; email/Teams es una capa de conveniencia adicional.
- **Alerta de prueba usa los valores del formulario aún sin guardar, no la fila persistida**: así el admin puede verificar el webhook antes de confirmar el guardado. Deep-link real: busca el contrato de `renewal_date` más próximo de la org y reutiliza el mismo `buildContractDeepLink()` que las alertas reales (sin duplicar esa construcción); si la org no tiene contratos activos, cae a `/vendors` con datos sintéticos y lo señala explícitamente en el resultado (`testSyntheticNotice`). Nunca escribe en `notifications`.
- **Fix incluido, no deuda técnica nueva**: `vendor-ficha.tsx` no leía `window.location.hash` al montar, así que ningún deep-link `#contract-{id}` (ni el ya existente del dashboard, ni los nuevos de email/Teams) aterrizaba en la pestaña Contratos — siempre caía en Detalles. Corregido con un `useEffect` mínimo que activa la pestaña si el hash empieza por `contract-`.
- **Formato de tarjeta de Teams — Adaptive Card v1.4 envuelta en `attachments`**: el formato moderno de los workflows de Teams sobre `logic.azure.com`; los Incoming Webhooks clásicos sobre `webhook.office.com` en despliegues recientes de Teams también lo aceptan. No se implementó el formato `MessageCard` legado por separado — si un webhook concreto no lo renderiza, es el único caso conocido no cubierto por este bloque.
- **Pendiente de ejecutar (no completable desde esta máquina/sesión)**: las env vars `CRON_SECRET`/`NEXT_PUBLIC_SITE_URL` en Railway (esta última es `NEXT_PUBLIC_*`, así que debe estar presente en tiempo de BUILD, no solo runtime — mismo gotcha ya documentado en `CLAUDE.md`), y los 2 secretos de Supabase Vault descritos abajo (0017). Hasta que se completen, el cron de envío corre cada 15 min sin efecto (no-op silencioso vía `raise warning`), y la aceptación de 2.2 queda verificada por tests unitarios de las funciones puras pero no de extremo a extremo con datos reales.

## 2026-07-19 — GUC de Postgres descartado, sustituido por Supabase Vault (0017)

- **El GUC del paso manual de 0016 no es viable en un proyecto Supabase gestionado**: `alter database postgres set app.settings.site_url = '...'` (y la variante `alter role postgres set ...`, probada también) falla con `permission denied to set parameter` — el rol `postgres` de un proyecto remoto gestionado no tiene privilegio para fijar parámetros a nivel de database/role vía `ALTER DATABASE`/`ALTER ROLE ... SET`, aunque sí sea dueño de la función `SECURITY DEFINER` que los leería con `current_setting()`. Este privilegio típicamente requiere superusuario real, que Supabase no expone en proyectos gestionados. No detectable sin probarlo contra el remoto — no hay Supabase local en esta máquina para reproducirlo antes (ver "Entorno de desarrollo" en `CLAUDE.md`).
- **Sustituido por Supabase Vault, no por hardcodear el valor en la migración**: `0017_notification_secrets_vault.sql` reescribe `trigger_send_pending_notifications()` para leer `site_url`/`cron_secret` de `vault.decrypted_secrets` (por `name`) en vez de `current_setting()`. Vault es la primitiva de Supabase pensada exactamente para esto (secretos legibles en runtime por funciones `SECURITY DEFINER` sin GUCs de sesión/database) y evita hardcodear el secreto del cron en SQL versionado. Comportamiento defensivo sin cambios: si algún secreto no existe todavía en Vault, `raise warning` y sale sin error — el cron nunca debe romperse por falta de configuración.
- **Paso manual vigente — crear los 2 secretos una sola vez en el SQL editor del proyecto remoto** (o Dashboard → Project Settings → Vault, si se prefiere la UI):
  ```sql
  select vault.create_secret(
    'https://saascostchecker-production.up.railway.app',
    'site_url',
    'Base URL para las llamadas HTTP del cron de notificaciones (trigger_send_pending_notifications)'
  );
  select vault.create_secret(
    '<mismo valor que CRON_SECRET en Railway>',
    'cron_secret',
    'Secreto compartido para /api/cron/send-notifications, debe coincidir con CRON_SECRET de Railway'
  );
  ```
  El `name` (`'site_url'`/`'cron_secret'`) es literal — lo lee `trigger_send_pending_notifications()` por ese nombre exacto. Si se re-ejecuta para rotar un valor, usar `select vault.update_secret(id, '<nuevo valor>')` con el `id` devuelto por `create_secret` (o buscarlo con `select id from vault.decrypted_secrets where name = 'cron_secret'`), no volver a insertar con el mismo `name` (violaría el unique constraint de `vault.secrets`).
- **Verificación de que ningún rol cliente puede leer los secretos, embebida en la propia migración**: sin Supabase local en esta máquina no hay forma de inspeccionar `pg_class.relacl`/grants a mano (`supabase db dump` también requiere Docker, no disponible aquí). En su lugar, `0017` termina con un bloque `do $$ ... $$` que comprueba `has_table_privilege('anon'/'authenticated', 'vault.decrypted_secrets'/'vault.secrets', 'SELECT')` y hace `raise exception` si alguno tuviera acceso — así `supabase db push` falla en el momento del deploy, en cada push, en vez de depender de una comprobación manual puntual que pueda quedar desactualizada. Confirmado en el push real de esta sesión: el bloque no lanzó excepción, es decir, `anon`/`authenticated` no tienen `SELECT` en ninguna de las dos relaciones (postura por defecto de Supabase Vault, no algo que este proyecto tuviera que configurar). `trigger_send_pending_notifications()` es, además, la única función del repo que toca `vault.*`, y nunca devuelve los valores leídos a quien la invoque (`returns void`), así que no hay una segunda vía de fuga que auditar.
- **Verificación para la siguiente pasada del cron una vez creados los secretos**: sin acceso a SQL directo contra el remoto desde esta máquina, la forma de confirmar que ya los está leyendo es indirecta — antes de crear los secretos, cada ejecución de `send-pending-notifications` (cada 15 min) termina en el `raise warning` de la función y no llega ninguna petición a `/api/cron/send-notifications`; tras crearlos, la siguiente pasada (máx. 15 min después) debería generar una petición real, visible como una entrada de log en Railway para esa ruta (o, más concluyente: crear una notificación pendiente real via el flujo normal de alertas de 2.1 y comprobar que su `sent_at`/`channels` pasan de `null`/vacío a poblados tras la siguiente pasada).
- **Cierre — pasos manuales completados por el usuario (fuera de esta máquina/sesión)**: los 2 secretos de Vault (`site_url`/`cron_secret`) creados en el proyecto remoto y las env vars `CRON_SECRET`/`NEXT_PUBLIC_SITE_URL` confirmadas en Railway. Verificación de extremo a extremo con datos reales: `curl` manual a `/api/cron/send-notifications` devuelve 200 con el secreto correcto y 401 sin él; la siguiente pasada automática del cron (`send-pending-notifications`, cada 15 min) aparece en los logs de Railway con 200, confirmando que `trigger_send_pending_notifications()` ya lee ambos secretos de Vault en producción; la alerta de prueba de la página de ajustes llegó a un email y a un webhook de Teams reales, con el deep-link aterrizando correctamente en la pestaña Contratos (fix de `vendor-ficha.tsx` de 0016 confirmado en producción, no solo en local). Bloque 2.2 queda completo — checkbox de aceptación actualizado en `docs/TASKS.md`. Adicionalmente, la `service_role` key se rotó tras esta verificación (higiene tras haber circulado en el flujo de pruebas) con smoke test posterior confirmando que la app sigue funcionando con la clave nueva.

## 2026-07-21 — Bloque 2.3 (subconjunto): calendario de renovaciones

- **Alcance acotado explícitamente con el usuario**: de las 3 piezas que pide `docs/TASKS.md` §2.3 (vista calendario, snooze/renegociado/cancelado, registro de ahorro conseguido), esta pasada implementa solo la vista calendario. El checkbox de aceptación de §2.3 sigue sin marcar — se dejó una nota explícita en `docs/TASKS.md` de qué falta (snooze/renegociado, ahorro) para no dar una falsa sensación de bloque cerrado.
- **Único sitio para el path de deep-link de un contrato**: `/{locale}/vendors/{vendorId}#contract-{contractId}` se construía a mano en dos sitios (`send-notifications.ts` dentro de `buildContractDeepLink`, y `renewal-track.tsx` en el `href` del `<a>`). Se extrajo `buildContractPath(locale, vendorId, contractId)` en `src/features/renewals/send-notifications.ts` — `buildContractDeepLink` pasa a ser `SITE_URL + buildContractPath(...)`, y tanto `renewal-track.tsx` como el calendario nuevo lo reusan. Efecto colateral necesario: el cliente `new Resend(...)` de ese archivo vivía a nivel de módulo (`const resend = resendApiKey ? new Resend(resendApiKey) : null`), lo que habría ejecutado `process.env.RESEND_API_KEY` (variable sin prefijo `NEXT_PUBLIC_`, inexistente en el navegador) en cuanto el calendario — un Client Component — importara `buildContractPath` de ese módulo. Se movió a una función `getResendClient()` invocada solo dentro de `sendRenewalAlertEmail`, así el módulo queda libre de efectos secundarios a nivel de import y es seguro de bundlear tanto en servidor como en cliente; confirmado en `pnpm build` que la ruta `/renewals` compila a un tamaño de bundle normal (no arrastra el SDK de Resend). Mismo motivo por el que las clases Tailwind por tono (`TONE_CLASSES`/`TONE_TEXT_CLASSES`, antes locales a `renewal-track.tsx`) se extrajeron a `src/features/vendors/renewal-tone-classes.ts`: `renewal-track.tsx` importa `getTranslations` de `next-intl/server` (server-only), así que el calendario no puede importar esas constantes directamente desde ese archivo sin arrastrar ese import server-only al bundle de cliente.
- **Query vendor→contratos compartida**: la query anidada `vendors→contracts→departments(name)/companies(name)` (~40 líneas) vivía solo inline en `dashboard/page.tsx`. Se extrajo a `fetchDashboardContracts(supabase)` en `src/features/dashboard/fetch-contracts.ts`, reusada por el dashboard y por `renewals/page.tsx` — comportamiento idéntico, decisión confirmada explícitamente con el usuario porque tocaba un archivo fuera del scope estricto del bloque.
- **Semántica de los dos marcadores por contrato**: marcador "accionable" en la fecha real en que la decisión deja de poder tomarse (`renewalDate - cancellationNoticeDays` si `autoRenews`, si no `renewalDate` — mismo criterio que `actionableDaysUntil()` de `renewal.ts`, ya usado por el primario contextual y la pista del dashboard), coloreado por `renewalTone()`. Marcador "informativo" mudo (tono siempre neutro) en `renewalDate` tal cual, mostrado SOLO cuando difiere del accionable — evita mostrar dos chips idénticos para la mayoría de contratos (solo divergen cuando `autoRenews && cancellationNoticeDays > 0`).
- **Accesibilidad deliberadamente distinta a los 3 charts del dashboard**: donut/barras/evolución de gasto son decorativos (`aria-hidden` + tabla `sr-only` con los mismos datos). Aquí los chips SON el contenido interactivo real (deep-links a la ficha del vendor), así que ocultarlos con `aria-hidden` habría roto el acceso por teclado/lector de pantalla al único elemento que importa. En su lugar: `<table>` semántica real sin `aria-hidden` (mismo criterio que `renewal-track.tsx`) más una lista de agenda `sr-only` adicional (`<ul>` lineal, un `<li>` por marcador del mes visible) como alternativa accesible a la rejilla 2D — confirmado explícitamente con el usuario antes de implementar.
- **Rejilla propia sin librería de calendario**: mismo criterio que los charts del dashboard (SVG/CSS propio, sin dependencia nueva) — 42 días fijos (6 semanas), lunes como primer día de semana independiente del locale (decisión menor confirmada con el usuario), filtros de empresa/departamento/auto-renovación como estado local (`useState`) sin params de URL, igual que `spend-by-group-chart.tsx`.
- **Bucketing de fechas puro y testeable, filtrado fuera de esa función**: `buildCalendarMonth()` (`src/features/renewals/calendar.ts`) solo mapea contratos→días de la rejilla; el filtrado por empresa/departamento/auto-renovación es responsabilidad del componente cliente sobre la lista de contratos de entrada, para mantener la función de un solo propósito y fácil de testear con fixtures (`calendar.test.ts`).

## 2026-07-21 — Deuda de 2.3: `buildContractPath` a un módulo sin dependencias

Antes de empezar 2.3b, el usuario pidió cerrar una deuda dejada por 2.3: `buildContractPath` vivía en `send-notifications.ts` junto al SDK de Resend y las plantillas de email/Teams — el calendario (Client Component) lo importaba de ahí, arrastrando ese peso al bundle de cliente (mitigado antes con un `getResendClient()` perezoso, pero la causa raíz seguía siendo compartir el módulo). Se extrajo a `src/features/renewals/deep-link.ts`, sin ninguna otra dependencia — `send-notifications.ts`, `renewal-track.tsx` y el calendario importan de ahí; `buildContractDeepLink` sigue componiendo `SITE_URL + buildContractPath`. Confirmado con `pnpm build`: el bundle de `/renewals` bajó de 10.8 kB a 6.05 kB. Commit `7ba48bd`.

## 2026-07-21 — Bloque 2.3b: snooze, renegociado/cancelado, registro de ahorro

Cierra las 3 piezas que quedaron explícitamente pendientes de §2.3 (ver entrada anterior del mismo día sobre el calendario). Diseño propuesto y aprobado antes de escribir código (`docs/TASKS.md` §2.3), con 2 preguntas explícitas resueltas por el usuario antes de implementar (ambas "sí, la opción recomendada"):

- **Dónde viven las acciones**: se confirmó la hipótesis del usuario (el `KebabMenu` ya existente en `ContractList`, pestaña Contratos de la ficha del vendor) con un recorte — NO se añaden acciones a la campanita (`notification-bell.tsx`): sus filas son un único bloque clicable sin hueco para una acción secundaria, y añadirlo habría sido un cambio estructural, no un añadido, para un beneficio marginal (todo notification ya enlaza al contrato, donde viven las acciones).
- **Bug encontrado y corregido en el motor 2.1 (no solo en el código nuevo)**: el índice único de idempotencia de `notifications` es `(contract_id, user_id, threshold_days)`, **sin fecha**. Si `renewal_date` cambia sin limpiar esa tabla, el índice bloquea para siempre la regeneración de esos mismos umbrales en la fecha nueva — esto ya era un bug latente en `update_contract()` para cualquier edición manual de fecha, no solo en el flujo nuevo de renegociar. Se arregló en los dos sitios (confirmado con el usuario antes de tocar código ya en producción): `update_contract()` y el nuevo `renegotiate_contract()` limpian `notifications where contract_id = X and type = 'renewal_alert'` cuando `renewal_date` cambia.
- **`update_contract()` ya no acepta `p_status='cancelled'`** (confirmado con el usuario, con el coste conocido de antemano: el helper `cancelContract()` de `e2e/renewals.spec.ts`, que sembraba datos de prueba cancelando directamente, se migró a llamar `cancel_contract()`). Motivo: sin este bloqueo, cualquier cancelación vía el formulario genérico de edición se saltaría por completo la captura de ahorro, dejando huecos permanentes en el ledger que es "la semilla del futuro motor de ahorros". El formulario genérico (`contract-fields.tsx`) ya no ofrece "Cancelado" como opción seleccionable — un contrato ya cancelado se muestra de solo lectura ahí (con `<input type="hidden">` para no romper el envío del resto del formulario), en vez de un `<Select>` deshabilitado cuyo comportamiento de `FormData` con el primitivo base-ui no estaba garantizado.
- **Snooze = columna `snoozed_until date` en `contracts`, no un `status` nuevo**: un contrato pospuesto sigue `active` (cuenta en gasto/KPIs con normalidad) — snooze es ortogonal al ciclo de vida, solo silencia alertas. Semántica elegida (la más simple, según lo pedido): suprime GENERACIÓN, no solo envío — `evaluate_renewal_alerts()` gana `and (c.snoozed_until is null or c.snoozed_until < p_today)` en sus 2 CTEs. Si un umbral cae dentro de la ventana de snooze, se salta y no se regenera después (sin "recuperación" retroactiva) — es el comportamiento correcto para "deja de molestarme con esto N días", no un bug. La pista/calendario (pasivos, sin push) siguen mostrando el contrato con su urgencia real durante todo el snooze. 3 duraciones fijas en el kebab (7/14/30 días, sin date picker) + "Quitar snooze" cuando ya está pospuesto.
- **Renegociado = evento, no `status` nuevo**: un contrato renegociado sigue `active` con términos nuevos (`cost_amount`/`currency`/`billing_cycle`/`renewal_date`) — el hecho de que se renegoció queda registrado por la fila que crea en `savings_records`, sin flag redundante en `contracts`. El diálogo (`renegotiate-dialog.tsx`) sugiere el ahorro en vivo (`computeSavings()`, reusa `annualizedCost()`+`convertAmount()` ya testeados) mientras el usuario edita el nuevo coste, **editable** antes de enviar — puede ser negativo (renegociación que sale peor), se muestra tal cual sin forzar un suelo en 0.
- **`savings_records`: tabla propia, no campos en `contracts`** — un contrato puede renegociarse más de una vez en su vida; campos en `contracts` solo capturarían el último evento, perdiendo historial necesario para "agrega los cierres del año". `contract_id`/`vendor_id` con `on delete set null` (no cascade, `delete_contract` ya existe) — el ledger de ahorro es dato financiero, no debe desaparecer si el contrato se borra después; `vendor_name` queda como snapshot para seguir siendo legible aunque ambas FKs terminen en null. Agregable por vendor (denormalizado)/departamento/empresa (JOIN en vivo a través de `contract_id`, igual criterio que `buildDepartmentSpend`/`buildCompanySpend`)/periodo (`closed_at`). Los montos se guardan ya convertidos a la moneda de la org **en el momento de capturar** (no se reconvierten al leer) — igual que un ledger contable real, para que un cambio futuro de moneda default no reinterprete cifras históricas.
- **2 RPCs separados (`renegotiate_contract`/`cancel_contract`), no uno fusionado con `p_kind`**: coherente con el idioma ya establecido del repo de funciones pequeñas y de propósito único (`create_vendor`/`create_contract` separados, `update_user_role()` aparte de `update_contract`), aunque repitan el boilerplate de limpieza de `notifications`+`audit_log`. Ambos reciben `previous_annual_cost`/`new_annual_cost`/`savings_amount` ya calculados desde TypeScript — el RPC persiste, no reimplementa conversión de divisa en PL/pgSQL (esa cuenta ya vive una sola vez, testeada, en `computeSavings()`).
- **Dashboard**: 5º KPI "Ahorro conseguido" (`buildSavingsYtd()`, suma `savings_records.savings_amount` con `closed_at` del año en curso) — la rejilla de `kpi-cards.tsx` pasa de `grid-cols-2 lg:grid-cols-4` a `grid-cols-2 lg:grid-cols-5` (en móvil el 5º queda solo en su fila, patrón normal y aceptado, no amerita rediseño). Además, no pedido explícitamente pero barato y de alto valor: tarjeta "Ahorro conseguido" (histórico, todo el tiempo) en `vendor-rail.tsx`, mismo template que la tarjeta "Gasto anual" ya existente — refuerza el efecto de la acción en el mismo sitio donde se tomó.
- **Verificación visual autenticada** (seed vía service-role con `auth.admin.createUser({email_confirm:true})`, autorización explícita del usuario, borrado completo al terminar — 0 filas huérfanas confirmado en las 8 tablas): posponer 14 días (badge "Pospuesto hasta..." visible, kebab cambia a "Quitar snooze") y quitar snooze confirmados; renegociar 1200€→900€ confirmado con recomputo en vivo del ahorro sugerido (300€), reflejado de inmediato en la tarjeta de la ficha y en el KPI del dashboard tras navegar; cancelar confirmado (contrato pasa a "Histórico", excluido del calendario de `/renewals`, tarjeta de ahorro de la ficha muestra 800€, primario contextual de la cabecera cambia a "+ Añadir contrato" por quedarse sin contrato activo); formulario genérico de edición confirmado mostrando "Cancelado" de solo lectura (sin `<Select>`) para un contrato ya cancelado, guardado de otros campos sigue funcionando.
- **Checkbox de aceptación de §2.3 marcado como completo** en `docs/TASKS.md` — con el calendario (pasada anterior) + snooze/renegociado/cancelado + registro de ahorro (KPI que agrega los cierres del año) verificados de extremo a extremo, las 3 piezas pendientes quedan cerradas.

## 2026-07-21 — Bloque nuevo: Tags por vendor + Presupuestos por bolsa

Fuera de la numeración de fases — ni SPECS.md ni TASKS.md lo cubrían; cierra la deuda dejada explícitamente en 1.5 ("vs. presupuesto" quedó fuera por falta de un concepto de presupuesto en el modelo). Diseño completo (modelo de datos, atribución de gasto, semáforo, tags, UI) propuesto en modo planificación y aprobado por el usuario antes de escribir código.

- **`budgets`: bolsa departamento × empresa × año fiscal, con precedencia por especificidad al resolver, nunca suma de bolsas que coexisten**: `company_id`/`department_id` ambos nullable (constraint: al menos uno no nulo) soportan las 3 formas de bolsa pedidas (específica, depto para todas las empresas, empresa entera). Para atribuir un € de gasto a una bolsa: exacta (empresa+depto) > depto-only (empresa null) > empresa-only (depto null) > sin presupuesto — nunca se consume contra dos bolsas a la vez, evita doble contabilización si coexisten una bolsa de depto y otra de empresa que se solapan (`resolveBudget()`, `src/features/budgets/aggregate.ts`).
- **Atribución de `spend_records` a departamento/empresa — problema real, no cubierto por el modelo existente**: `spend_records` solo tiene `vendor_id`, esa dimensión vive en `contracts.department_id`/`company_id`, y un vendor puede tener varios contratos activos en departamentos/empresas distintos (soporte ya existente desde 1.2). Sin `contract_id` en `spend_records` para desambiguar, se reparte el importe a partes iguales entre los pares `(department_id, company_id)` distintos de los contratos activos del vendor — simplificación documentada explícitamente en el código (mismo espíritu que la degradación de `convertAmount()`: no fingir precisión que no hay), la suma total nunca se duplica ni se pierde. Caso normal (1 solo par) es exacto.
- **Cálculo de consumo/proyección en TypeScript, sin RPC de agregación nueva**: mismo dataset/definición de "reconciliado" que `dashboard_monthly_spend()` (`spend_records.vendor_id is not null`), pero esa RPC agrupa solo por mes+moneda — no sirve para derivar departamento/empresa. `buildBudgetConsumption()` hace la query directa y agrega en JS, mismo criterio que 1.5 ("TS testeable sin SQL es más simple" para este volumen de datos). Proyección de fin de año: run-rate simple (`consumido / días transcurridos × días totales del año`), solo para el año fiscal en curso — un año ya cerrado o todavía futuro no se proyecta.
- **Semáforo de presupuesto — métrica de RITMO, no umbrales fijos**: `% consumido del presupuesto ÷ % del año fiscal transcurrido` (`src/features/budgets/thresholds.ts`, `BUDGET_STABLE_PACE_RATIO=1.05`, `BUDGET_WARNING_PACE_RATIO=1.25`), reutilizando el mismo vocabulario de 3 tonos (`critical`/`upcoming`/`stable`) y los mismos colores (rojo/ámbar/verde, `Pill`) que "Estado del stack" del dashboard — nunca una paleta nueva. Un presupuesto ya agotado (≥100% consumido) es siempre crítico, sin importar el ritmo.
- **Escritura de `budgets` más estrecha que el resto de MANAGER_ROLES**: lectura sigue el criterio estándar (finance/it_admin/org_admin, `budgets_select`), pero crear/editar/borrar bolsas está restringido a **finance + org_admin únicamente** (`BUDGET_WRITE_ROLES`, comprobado dentro de las RPCs `create_budget`/`update_budget`/`delete_budget`) — decisión de negocio explícita del usuario ("el presupuesto lo aprueban finance + dirección", no IT). Única tabla del proyecto con este split lectura/escritura dentro de MANAGER_ROLES.
- **`vendors.annual_cap`/`annual_cap_currency`**: cap anual informativo propio del vendor, puramente visual en la ficha (`update_vendor_annual_cap()`, RPC separada de `update_vendor()` para no tocar su firma ya establecida en múltiples callers) — no participa en el cálculo de consumo de bolsas, que es 100% departamento×empresa.
- **`vendor_tags`: sin taxonomía predefinida ni catálogo de tags separado** — el autocompletado de "tags de la org" es un `select distinct tag`; deja los tags consultables por agrupación/solapamiento para el futuro motor de ahorros sin refactor. Tags siempre editables desde la ficha del vendor (`VendorTags`, sin depender del toggle `editMode` general — edición ligera y frecuente, mismo criterio que marcar un asiento inactivo en 1.4), normalizados server-side (`lower(btrim(...))`) en `add_vendor_tag()`/`remove_vendor_tag()`, `on conflict do nothing` para idempotencia.
- **UI de presupuestos en `/team/budgets`**, agrupa con `companies`/`departments` (estructura organizativa), no bajo `/settings/*`. Resumen discreto en el dashboard (`budget-summary.tsx`) solo se muestra si la org ya tiene al menos una bolsa presupuestada para el año en curso — sin dato real que resumir, no se renderiza nada (no un prompt vacío invitando a configurar).
- **Filtro de vendors por tag en `/vendors`**: primera vez que esa página tiene filtros — `searchParams` (`?tag=...`) leído server-side, `<VendorTagFilter>` cliente empuja el query param, patrón estándar de App Router sin estado global nuevo.
- **Verificación visual autenticada** (seed vía `service_role` con `auth.admin.createUser({email_confirm:true})`, borrado completo al terminar — 0 filas huérfanas confirmado en `budgets`/`vendor_tags`/`vendors`/`contracts`/`spend_records`/`departments`/`users`): bolsa creada desde la UI (150€ de gasto reconciliado vs. 100€ de presupuesto) mostró semáforo "Crítico", barra roja, proyección de fin de año, y desglose de 1 vendor consumiéndola; el resumen discreto del dashboard reflejó el mismo "150% consumido · Crítico" enlazando a `/team/budgets`; tag añadido desde la ficha del vendor persistió tras recargar, y el filtro de `/vendors` excluyó correctamente al vendor sin ese tag. Un "1 Issue" de hidratación (id de `base-ui` de la campanita de notificaciones) se confirmó preexistente y no relacionado — presente igual en `/dashboard` sin ninguno de los componentes nuevos de este bloque.
- **Migraciones `0019_budgets.sql`/`0020_vendor_tags.sql` aplicadas al proyecto remoto** (`mkrsicuvhnmljpurtwun`) con `supabase db push`, confirmado con `supabase migration list` (ambas presentes en Local/Remote).
- **Tests**: unitarios de `resolveBudget()`/`buildBudgetConsumption()`/`budgetTone()` en verde local (23 casos — precedencia, reparto multi-departamento, conversión de moneda, año parcial/pasado/futuro, overrun siempre crítico). RLS/permisos (`budgets`, `vendor_tags` — escritura restringida, aislamiento entre orgs) y e2e (`budgets.spec.ts`, `vendor-tags.spec.ts`) requieren Supabase local — se verifican en CI, no en esta máquina (ver "Entorno de desarrollo" en `CLAUDE.md`).

## 2026-07-21 — Sesión de reparación de CI: main llevaba en rojo desde el 16 de julio

`main` fallaba en CI desde el push de `2460ace` ("fix: light sidebar, button hierarchy, and vendor ficha with tabs", 2026-07-16T18:32) — 15 runs consecutivos en failure hasta hoy, incluidos los 5 bloques de features enteros que se construyeron encima (2.1, 2.2, calendario 2.3, 2.3b, tags+budgets) sin que nadie lo notara. Diagnóstico por fallo, sin parches genéricos (skip/timeout/borrado de specs, todos prohibidos explícitamente para esta sesión):

- **`renewal-actions.test.ts` (unit) — bug del test, no de cálculo real**: las 2 aserciones nuevas de 2.3b (`renegotiate_contract`/`cancel_contract`) comparaban columnas `numeric(14,2)` con literales de texto (`"1200.00"`) vía `toMatchObject`. PostgREST devuelve `numeric` como JSON number, no string — el resto del repo (incluida la línea 281 del mismo archivo, más 2 suites de bloques anteriores) ya usa `Number(...)` para esto, convención que estas 2 aserciones no siguieron. Los valores calculados eran correctos (300/800/900/1200) — no hay bug de ahorro/renegociación, ningún `savings_records` de producción está afectado. Fix: comparaciones `Number(...)` en vez de `toMatchObject` con strings.
- **`e2e/dashboard.spec.ts` — regresión real de 2.3b, de la suite de test, no de producción**: tenía su propio helper `cancelContract()` (copia local, no compartida con `renewals.spec.ts`) que seguía llamando `update_contract(p_status='cancelled')` — bloqueado exactamente por el cambio de 2.3b que forzó toda cancelación a pasar por `cancel_contract()`. Solo se migró la copia de `renewals.spec.ts` en su momento; esta quedó atrás. Migrada al mismo RPC.
- **`e2e/vendors.spec.ts` y `e2e/companies.spec.ts` — selectores desactualizados tras el rediseño de tabs (`2460ace`, 2026-07-16)**: la ficha de vendor pasó de mostrar todo siempre visible a un patrón de tabs (Detalles/Contratos/Asientos/Documentos/Notas) sin que ninguno de los e2e existentes se actualizara para navegar a la pestaña correcta antes de sus aserciones — "Ver PDF" ahora vive en Documentos, los contratos en Contratos. Arreglados con un clic a la pestaña correspondiente antes de la aserción.
- **`e2e/companies.spec.ts` — además, regresión REAL de producto, no solo de test**: el mismo rediseño sustituyó la fila de contrato antigua (siempre expandida, formulario completo visible, con `id="contract-{id}"`) por una fila compacta en `ContractList` (nombre, coste, pill de renovación, pill de estado, kebab) que no llevaba ningún dato de compañía/departamento, y cuyo `id="contract-{id}"` solo existía cuando el contrato estaba en modo edición. Para un vendor con 2+ contratos activos en compañías distintas — el caso exacto que el soporte multi-empresa (feedback 2026-07-16) fue construido para resolver — ya no había forma de distinguir a simple vista qué contrato pertenecía a qué compañía sin abrir cada uno en modo edición. No estaba documentado como trade-off aceptado en la entrada de DECISIONS.md del rediseño de tabs; era un efecto colateral no buscado de compactar la fila. **Reportado al usuario antes de tocar producción** (regla de esta sesión); decisión explícita: restaurar el dato. Fix de producción en `contract-list.tsx`: la fila compacta añade `· {compañía}` / `· {departamento}` (cada uno en su propio `<span>` para que sea localizable de forma inequívoca), y el contenedor de la fila recupera `id="contract-{id}"` también en modo vista (antes solo existía en modo edición) — efecto colateral positivo: el deep-link `#contract-{id}` (dashboard, alertas por email/Teams, calendario) ahora hace scroll al contrato correcto sin depender de que esté siendo editado.
- **`e2e/contract-actions.spec.ts` — locator ambiguo, mismo patrón que el fix ya aplicado a budgets/tags**: `getByText("900 €")` sin `exact` matcheaba tanto la subfila de coste del contrato ("900 € · Anual") como el número grande de "Coste anual" en `VendorRail` (barra lateral, siempre visible, no depende de la pestaña activa). Fix: aserción sobre el texto completo `"900 € · Anual"`.
- **Por qué nadie vio el CI rojo**: no es un fallo de configuración del workflow — `gh api notifications` confirma 22 notificaciones `ci_activity` sin leer para este repo, es decir, GitHub sí ha estado avisando desde el primer push roto. El hueco es que esas notificaciones se acumulan en la bandeja de GitHub sin que nadie las revise (o la entrega por email de "Actions" está desactivada en la configuración de notificaciones de la cuenta) — con push directo a `main` sin PRs (branch protection confirmado desactivado, 404 en `branches/main/protection`), no hay ningún otro punto de fricción que lo hiciera visible. **Decisión del usuario**: no añadir otro canal automático (email/Slack/Teams) al workflow — tendría el mismo modo de fallo que la bandeja de GitHub, otro sitio que nadie revisa. En su lugar, Claude Code vigila (`gh run watch`) cada push a `main` y reporta el resultado directamente en la conversación, sin infraestructura nueva. No se tocó `.github/workflows/ci.yml`.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` con el mismo patrón de siempre (9 suites que requieren Supabase local fallan por `ECONNREFUSED`, incluida `renewal-actions.test.ts` — su fix se verifica en CI, no localmente). Commit `966e116` pusheado a `main`; **CI run 29842286530 en verde en ambos jobs** (Lint/typecheck/unit tests + Playwright e2e) — primera vez desde `2460ace`. Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/vendors` → 307 (redirección a login, confirma rutas vivas sin 500). **CI cerrado en verde.**

## 2026-07-22 — Sesión de fixes de móvil: overflow horizontal + bottom nav completo

Bugs reales vistos en un Android físico (ficha de vendor). Diagnóstico con Playwright + `devices["Galaxy S8"]` (emulación real de dispositivo — `isMobile`/`hasTouch`/`deviceScaleFactor`, no solo una ventana de escritorio estrecha) contra el proyecto remoto, con un tenant/vendor/contrato efímero sembrado vía service-role (autorización explícita ya cubierta por el propio pedido de "verificación visual autenticada" de esta sesión), borrado al terminar cada verificación.

- **Causa raíz real, distinta de la hipótesis inicial**: no era (solo) un problema de wrapping CSS. La app no tenía **ningún `<meta name="viewport">`** (`src/app/[locale]/layout.tsx` no exportaba `viewport`, y Next.js no inyecta uno por defecto) — se añadió (`width: "device-width", initialScale: 1`), correcto y necesario en general, pero **no fue suficiente por sí solo** para el bug de `/vendors`: confirmado con la tabla ya presente, con meta viewport correcto, y con el propio `overflow-x-auto` conteniendo visualmente la tabla (verificado con `getBoundingClientRect` en toda la cadena de ancestros, todos a 360px) — Chromium móvil seguía ensanchando `window.innerWidth` de la página entera a 859px. La causa real: una `<table>` con layout automático (columnas dimensionadas por contenido de celda) y `min-width` fija, dentro de un `overflow-x-auto`, hace que el navegador considere el ancho intrínseco de la tabla al decidir si el "layout viewport" necesita ser más ancho que el dispositivo — el `overflow-x-auto` contiene el *scroll visual*, pero no evita ese cálculo. Confirmado el fix real probando en vivo con `page.evaluate` antes de tocar código: `contain: layout` en el wrapper `overflow-x-auto` baja `window.innerWidth` de 859 a 360 sin cambiar nada visual (a diferencia de `table-layout: fixed`, que solo mitigaba parcialmente, o de `overflow-x: hidden`/`max-width: 100vw`, que no tenían ningún efecto). Aplicado (clase `contain-layout` de Tailwind) a los 4 sitios del repo con el mismo patrón exacto (tabla auto-layout + `min-w-[Npx]` + `overflow-x-auto`, sin `table-fixed`): `vendors/page.tsx` (confirmado con el bug real), y preventivamente en `reconciliation-table.tsx`, `import/page.tsx` e `import-wizard.tsx` (mismo patrón, no ejercitado por los datos de prueba pero con el mismo riesgo). `renewals-calendar.tsx` ya usa `table-fixed` desde su diseño original (para las columnas iguales del calendario) y se confirmó limpio sin tocarlo — no se le añadió `contain-layout` por no tener el bug (impacto mínimo).
- **`src/components/ui/tabs.tsx` (`TabsList`) — la causa del bug reportado (ficha de vendor)**: sin `overflow-x-auto` ni `flex-nowrap` explícito, los 5 tabs (Detalles/Contratos/Asientos/Documentos/Notas, ~390-400px en español) desbordaban los ~328px disponibles a 360px de viewport, forzando scroll horizontal real de toda la página. Fix: `overflow-x-auto` + ocultar la scrollbar (`[scrollbar-width:none]`/`[&::-webkit-scrollbar]:hidden`) en `TabsList`, `shrink-0 whitespace-nowrap` en cada `TabsTrigger` — tabs scrollables horizontalmente, contenidos dentro de su propia fila, sin desbordar la página. Único consumidor actual de `TabsList` en el repo, cambio sin riesgo de regresión visual en otro sitio.
- **`contract-list.tsx` — fila de contrato compacta, segundo bug real (no solo hipótesis)**: con un contrato activo + pospuesto + compañía asignada (3 pills + kebab), la fila `flex items-center` sin wrap forzaba el bloque de texto (`min-w-0 flex-1`) a comprimirse a casi cero ancho, partiendo "Figma contract" y el coste en una columna de una palabra por línea — visualmente roto aunque ya no desbordara la página. `flex-wrap` a secas tampoco bastaba (el navegador seguía intentando encajar 2 pills junto al texto antes de saltar de línea). Fix real: mismo patrón ya usado en `vendor-header.tsx` — el contenedor de la fila pasa a `flex-col sm:flex-row` (apila en móvil, fila en `sm+`), y las pills+kebab se agrupan en su propio `<div className="flex flex-wrap items-center gap-2">` separado del bloque de texto. Resultado: nombre completo visible, coste/ciclo/compañía en 1-2 líneas legibles, pills en su propia fila que envuelve si hace falta.
- **Bottom nav — 5ª entrada "Más" con panel deslizante, misma fuente de verdad que el sidebar**: diseño propuesto y aprobado por el usuario antes de escribir código (2 opciones presentadas: mantener las 4 fijas actuales —incluida "Solicitudes", placeholder deshabilitado— vs. promocionar Presupuestos y mover Solicitudes al panel; el usuario eligió mantener las 4 actuales sin cambios). El panel "Más" (`more-nav-sheet.tsx`, nuevo) reusa exactamente `NAV_ITEMS`/`isNavItemVisible` de `nav-items.ts` — cero lista duplicada: muestra `section === "data"` y `section === "settings"` filtrados por rol, agrupados con los mismos headers "Datos"/"Ajustes" que ya usa `sidebar.tsx`. Construido como panel inferior (Base UI `Dialog` con `Popup` anclado a `fixed inset-x-0 bottom-0`, no el `dialog.tsx` centrado compartido, para no afectar `ConfirmDialog`/`RenegotiateDialog`/`CancelContractDialog`) — bug real cometido y corregido durante la implementación: pasar `NAV_ITEMS` (o cualquier `NavItem` crudo, que incluye `icon: LucideIcon`) directamente como prop desde `BottomNav` (Server Component) a `MoreNavSheet` (`"use client"`) rompe el render (`__next_error__`) — exactamente el mismo bug de serialización RSC ya documentado en la entrada del rediseño de tabs (2026-07-16, `nav-link.tsx`). Corregido resolviendo cada item a un objeto serializable (`icon` ya renderizado a `ReactNode`, `label` ya traducido) en `BottomNav` antes de cruzar al cliente.
- **Verificación de las 3 RPCs/flujos del nuevo `e2e/mobile.spec.ts` replicados contra el proyecto remoto antes de confiar en el archivo** (no ejecutable en CI desde esta máquina): `create_company`/`create_vendor`/`create_contract`/`set_contract_snooze`/`create_invitation` con los mismos parámetros del test, y los mismos selectors de Playwright (`nav[aria-label="Navegación móvil"]`, `getByRole("dialog")`, labels exactos de cada item) — confirmado que las 5 páginas cargan sin overflow, que el panel "Más" muestra los 7 items para `org_admin` y que un usuario `finance` invitado no ve la sección "Ajustes" en absoluto (0 elementos, no solo oculta).
- **Verificación visual autenticada con capturas en viewport móvil (Galaxy S8, 360×740)**: confirmado sin overflow horizontal en dashboard/vendors/ficha de vendor (pestañas Detalles y Contratos)/renovaciones/presupuestos; captura de la fila de contrato con compañía+pospuesto legible en 2 líneas sin comprimirse; captura del panel "Más" abierto (backdrop, título, grupos Datos/Ajustes con los 7 items, iconos coherentes con el resto de la piel Lima quirúrgica); confirmado que tocar un item navega y cierra el panel. Datos efímeros borrados al terminar cada script de verificación (0 filas huérfanas).
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` con el mismo patrón de siempre (9 suites que requieren Supabase local, sin cambios respecto a antes de esta sesión). `e2e/mobile.spec.ts` (nuevo, 3 tests: sin overflow en las 5 páginas + pestaña Contratos, bottom nav completo con "Más" para `org_admin`, permisos de rol para `finance`) — sus RPCs/selectors se replicaron contra el remoto antes de confiar en el archivo (ver arriba), y se confirmaron en verde en el run real de CI (no ejecutable localmente). Commit `b8af8b3` pusheado a `main`; **CI run 29911510895 en verde en ambos jobs**, incluida `mobile.spec.ts` en el job de Playwright. Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/vendors` → 307 (redirección a login, confirma rutas vivas sin 500). **CI cerrado en verde.**

## 2026-07-29 — Bloque 3.1: Solicitudes (formulario self-service)

Primer bloque de la FASE 3 (Procurement). Planificado en modo planificación (2 agentes: exploración de patrones existentes + diseño), con 2 decisiones de alcance confirmadas por el usuario antes de escribir código:

- **Sin flujo de "guardar borrador"**: el formulario envía directo a `pending` con un único botón "Enviar solicitud". `draft` queda soportado en el `check` constraint del esquema para el futuro, sin UI de guardar/retomar en este bloque — TASKS.md §3.1 pedía "formulario self-service", no explícitamente un flujo de borrador.
- **RLS solo del propio solicitante**: `purchase_requests_select` usa `requester_id = current_user_id()`, sin lectura ampliada para finance/it_admin/org_admin todavía. Es la primera tabla de "dato de negocio real" del repo modelada sobre ownership por fila en vez de por rol — el único precedente existente era `notifications` (bloque 2.1). La visibilidad de aprobadores/managers se revisa en el bloque 3.2, cuando exista de verdad `approval_rules` y tenga sentido decidir quién puede ver qué.
- **`current_step` deliberadamente sin default** (nullable): solo cobra sentido cuando 3.2 materialice pasos reales de `approval_rules` — ponerle un valor hoy fingiría un flujo de aprobación que todavía no existe.
- **`department_id` con fallback server-side al propio del caller**: si el formulario no especifica departamento, el RPC `create_purchase_request()` lo resuelve a `users.department_id` del solicitante. Verificado con un caso de test explícito (asignar departamento a un empleado vía `update_user_department()`, crear una solicitud sin especificar departamento, confirmar que la fila resultante lo lleva).
- **Timeline de estados — 4 pasos visibles desde el principio, no "van apareciendo"**: `getTimelineSteps()` (`src/features/requests/timeline.ts`, puro, testeado con los 5 estados) siempre expone Enviada→En revisión→Resultado→Comprada con los pasos futuros en "upcoming", salvo el único caso genuinamente distinto: una solicitud `rejected` solo muestra 3 pasos (sin "Comprada", que ya es inalcanzable) en vez de 4 con el último marcado de alguna forma especial — más honesto que fingir que ese paso sigue "pendiente".
- **`getCurrentUserProfile()` NO se amplía con `department_id`**: sigue el precedente ya establecido por `vendors/new/page.tsx` (queries paralelas puntuales en vez de engordar el shape cacheado y compartido por toda la app) — `requests/new/page.tsx` hace su propia query a `users.department_id` (+ `organizations.default_currency` como prefill real de moneda, en vez del `"EUR"` hardcodeado que usa `contract-fields.tsx`).
- **Reuso del combobox de catálogo existente (`SaasCombobox`, bloque 1.2) sin ningún cambio** — mismo patrón de selección/lock-in que `new-vendor-form.tsx`, con `onCreateCustom` para herramientas fuera del catálogo. A diferencia del formulario de vendor, el nombre de la herramienta seleccionada NO queda editable tras la selección (aquí no hay necesidad de ajustar el nombre "oficial" de un vendor, es solo lo que se está pidiendo).
- **Nav "Solicitudes" habilitado en vivo, sin feature flag** (`nav-items.ts`, `href: null` → `/requests`) — coherente con cómo se lanzó cada bloque anterior (renewals, budgets...); el único precedente de gating (`NEXT_PUBLIC_FEATURE_GOOGLE_OAUTH`) es por una dependencia de onboarding sin terminar, no una política general. Era el último `href: null` del archivo — comentario de las líneas 39-43 corregido (ya enumeraba solo 5 bloques de los ~10 que ya tenían página real).
- **Verificación de los 2 RPCs/flujos nuevos (`create_purchase_request`, y el fallback de `department_id` vía `update_user_department`) replicados contra el proyecto remoto antes de confiar en `permissions.test.ts`** (no ejecutable en CI desde esta máquina, igual que todo `permissions.test.ts` del repo): mismos parámetros, mismas aserciones — creación, visibilidad propia, invisibilidad para otro empleado del mismo org y para otro org, insert/update/delete directos bloqueados por RLS, y el fallback de departamento devolviendo el id correcto.
- **Bug real encontrado por el propio CI, no por la réplica manual contra el remoto — y una lección de metodología**: el primer push (commit `f20d982`) pasó lint/typecheck/build/unit tests pero **el job de Playwright falló**: `getByText("Pendiente", { exact: true })` resolvía a 3 elementos en la página de detalle, no 1. Causa real: el estado de paso `upcoming` del timeline usaba la traducción `"Pendiente"` (`Requests.detail.timeline.state.upcoming`), que colisiona textualmente con el propio estado de la solicitud `pending` → `"Pendiente"` (`Requests.status.pending`) — dos conceptos distintos (estado de la solicitud vs. estado de un paso del timeline) con la misma palabra en español, y también en inglés (`"Pending"` para ambos). Corregido alineando con el vocabulario YA establecido en el repo para "todavía no alcanzado" (`renewalTone()`/`budgetTone()` ya usan "Próximo"/"Upcoming" para este mismo concepto en otras features): `upcoming` pasa a `"Próximo"`/`"Upcoming"`. **Por qué mi réplica manual contra el remoto no lo detectó antes de empujar**: el script ad-hoc usaba `locator.isVisible()` (comprueba solo el PRIMER elemento que matchea, sin fallar por ambigüedad), mientras que las aserciones reales de Playwright Test (`expect(locator).toBeVisible()`) exigen "modo estricto" y fallan si el locator resuelve a más de un elemento — mi verificación pasó por usar un método más permisivo que el que realmente corre en CI, no porque el bug no existiera. Corregido y vuelto a verificar con el mismo método estricto (`.count()` exacto + `.isVisible()` sobre un locator ya inequívoco) antes de este segundo push; commit de fix incluido en el mismo cierre.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` con el mismo patrón de siempre — el nuevo `permissions.test.ts` de requests se suma a las 9 suites que ya requerían Supabase local (10 en total, 0 fallos inesperados), `timeline.test.ts` (puro) en verde local. Commit `f20d982` pusheado a `main`; run de CI 30484918545 falló en el job de Playwright (ver bug real de arriba); fix en el commit `7d30a44`, **CI run 30485815528 en verde en ambos jobs**, confirmado directamente con `gh run view` (no solo el código de salida de `gh run watch`, que en el run anterior había reportado 0 pese al fallo real — no se volvió a confiar en él sin comprobar). Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/requests` → 307 (redirección a login, confirma la ruta viva sin 500). Checkbox de aceptación de §3.1 marcado en `docs/TASKS.md`. **Bloque 3.1 cerrado en verde.**

## 2026-07-30 — Bloque 3.1b: Ciclo de aprobación de Solicitudes (single-level)

Interino entre 3.1 y el motor completo de 3.2 (SPECS §6), pedido explícitamente por el usuario. Diseño propuesto en modo planificación (auditoría de estado + lectura de SPECS/TASKS/DECISIONS) y confirmado con 2 preguntas de desambiguación antes de escribir código.

- **Quién aprueba**: finance + org_admin de la org, un único nivel — cualquiera de los dos puede resolver cualquier solicitud pendiente de su org (sin asignación de aprobador individual ni matriz por departamento/monto, eso sigue siendo 3.2). `resolve_purchase_request()` hace el chequeo de rol server-side.
- **Estados**: se añade `cancelled` al `check` de `purchase_requests.status` (antes solo tenía `draft/pending/approved/rejected/purchased`) — requirió migración (drop+add constraint), no era ampliable in-place. Transiciones: `pending→approved`/`pending→rejected` (finance/org_admin, motivo obligatorio en rechazo, columna nueva `rejection_reason`), `pending→cancelled` (solo el propio solicitante), `approved→purchased` (el solicitante o finance/org_admin, botón manual — decisión confirmada con el usuario: sin automatismo de conversión a vendor/contrato, eso es 3.3). Todo vía 3 RPCs `security definer` nuevas (`resolve_purchase_request`, `cancel_purchase_request`, `mark_purchase_request_purchased`), mismo idiom que el resto del repo (mutación bloqueada en RLS, solo las RPCs escriben, `audit_log` en la misma transacción).
- **Notificaciones — SÍ necesitaron cambio estructural** (marcado explícitamente al usuario antes de escribir la migración, tal como pidió): el `check` de `notifications.type` estaba cerrado a `'renewal_alert'` únicamente → ampliado con `purchase_request_submitted`/`purchase_request_resolved`. El índice de idempotencia de 2.1 está scoped a `contract_id` (inútil aquí) → se añadió una columna `request_id` (mismo patrón que `contract_id`) + un segundo índice único parcial `(request_id, user_id, type) where type in (...)`. El endpoint `/api/cron/send-notifications` (pg_net + pg_cron cada 15 min, sin cambios en el disparador) estaba hardcodeado a `renewal_alert` con join a `contracts` — se generalizó con un branch por `type` y un helper compartido (`attemptAndMark`) que intenta email/Teams y marca `sent_at` igual para los 3 tipos; las plantillas nuevas viven en `src/features/requests/send-notifications.ts` (mismo esqueleto que `renewals/send-notifications.ts`, reutiliza su `escapeHtml()`). Destinatarios: `submitted` → todos los finance/org_admin de la org (insertado dentro de `create_purchase_request()`, reemplazada en esta migración); `resolved` → el solicitante (insertado dentro de `resolve_purchase_request()`). `cancelled`/`purchased` no notifican (acción del propio actor sobre su propia solicitud).
- **RLS ampliada**: `purchase_requests_select` pasa de "solo el propio solicitante" a "el propio solicitante O finance/org_admin de la org" — primera vez que esta tabla se lee más allá de ownership por fila. La página `/requests` (lista) hereda el alcance ampliado sin tocar la query (RLS resuelve el filtro); se añadió una columna "Solicitante" condicional cuando el viewer es aprobador.
- **Enlace "Crear vendor/contrato"**: en una solicitud aprobada, visible solo a roles que ya pueden crear vendors (finance/it_admin/org_admin) — enlaza a `/vendors/new` con prefill vía query string (`catalog_id`/`vendor_name`/`department_id`/`cost`/`currency`). `NewVendorForm` acepta ahora `initialSelection`/`initialContract` opcionales; sin ellos el comportamiento es idéntico al de antes (combobox vacío). Sin automatismo: el humano confirma/edita y guarda como siempre — la conversión automática real es alcance de 3.3.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `permissions.test.ts` ampliado con un nuevo `describe` (creación→notificación a aprobadores, aprobar, rechazar sin motivo falla, rechazar con motivo, roles no autorizados, transición solo desde pending, cancelar solo el solicitante, marcar comprada solo desde approved, RLS ampliada, e idempotencia del índice único probada con un insert duplicado directo vía `service_role`) — no ejecutable en esta máquina (requiere Supabase local), se verifica vía CI. Nuevo e2e `requests-approval.spec.ts` (2 `browser.newContext()` en paralelo, solicitante + aprobador) cubre aprobar-con-notificación-en-campanita y rechazar-con-motivo — mismo patrón de guard que el resto de e2e con tenants reales.
  - **CI**: commit `8d3de81` pusheado; run `30495992846` — job de unit tests/RLS en verde, job de Playwright falló por una colisión de strict-mode (`getByText("Rechazada", {exact:true})` resolvía a 2 elementos: el pill de estado y el pill del paso "Resultado" del timeline, ambos dicen "Rechazada" con razón, a diferencia de la colisión pending/upcoming de 3.1 que sí era un accidente de vocabulario — aquí no se renombra nada, se ancla la aserción con `.first()`). Fix en el commit `b525f58`; **run `30496421970` en verde en ambos jobs**.
  - **Verificación visual autenticada contra el proyecto remoto** (script ad-hoc con `service_role`, no comiteado, mismo patrón que el cierre del 2026-07-29): org efímera + `org_admin` (aprobador) + `employee` (solicitante) creados vía `auth.admin.createUser()` (evita la validación de dominio de GoTrue en remoto para `@example.test`) + `create_invitation()`. Conducido con el navegador real contra producción. Hallazgo de entorno: las dos pestañas del navegador comparten el mismo perfil/cookie jar (a diferencia de los `browser.newContext()` aislados de Playwright), así que las dos sesiones se verificaron secuencialmente (login→acción→cerrar sesión→login del otro usuario) en vez de en paralelo — sin impacto en la cobertura, solo en el guion de la verificación. Confirmado con capturas: (1) empleado crea "Figma" (1200€) → queda `Pendiente` con "Cancelar solicitud" visible; (2) org_admin ve la fila en `/requests` con la columna "Solicitante" ("Empleado Verificacion") y la campanita ya con 1 sin leer; abre el detalle → "Aprobar"/"Rechazar" visibles → aprueba → estado `Aprobada`, timeline completo, aparecen "Marcar como comprada" y "Crear vendor/contrato"; (3) empleado recarga → ve `Aprobada` y la campanita muestra "Tu solicitud de Figma fue aprobada"; (4) segunda solicitud "Notion" (800€) rechazada por el org_admin con motivo "Ya tenemos Confluence para esto." vía el diálogo (textarea obligatoria, botón deshabilitado hasta escribir) → detalle del empleado muestra `Rechazada`, el motivo, y la campanita "Tu solicitud de Notion fue rechazada". Org efímera y ambos usuarios borrados al terminar (`auth.admin.deleteUser` + delete de `organizations`, cascada sobre `users`/`purchase_requests`/`notifications`); script y capturas no comiteados.
  - Producción verificada tras el rollout de Railway: `/es` → 200, `/es/login` → 200, `/es/dashboard` → 307, `/es/requests` → 307 (redirección a login, confirma la ruta viva sin 500). Checkbox de aceptación de §3.1b marcado en `docs/TASKS.md`. **Bloque 3.1b cerrado en verde.**

## 2026-07-29 — Sesión de ajustes: etiqueta "Pagos conciliados" + nav "Fases futuras"

Sesión pequeña, sin bloque nuevo (3 ajustes pedidos por el usuario, sin decisiones de alcance que confirmar):

- **Etiqueta "Reconciliado/Reconciliación" → "Pagos conciliados"/"Conciliado" (solo es, solo texto visible)**: revisadas todas las apariciones en `messages/es.json` (landing/features, chip del wizard de import, nav, empty state del gráfico de evolución de gasto, widget de reconciliación pendiente del dashboard, botón "ver cola" del import, página de la cola). Se normalizó también el verbo (`reconciliar`→`conciliar`) donde aparecía junto a las mismas pantallas, para no dejar una mezcla "conciliado"/"reconciliar" inconsistente en la misma vista. El identificador interno `reconciled` (columnas, RPCs `link_reconciliation`/`ignore_reconciliation`/`bulk_accept_reconciliation`, tabla `reconciliation_queue`, namespace i18n `Reconciliation`/`Shell.dashboard.reconciliation`) **no se tocó** — solo valores de string, ninguna clave. `messages/en.json` no se tocó: el inglés ya usa "reconciled"/"reconciliation", que es el término correcto en ese idioma (sin el problema de jerga que sí tiene "reconciliación" en español). Efecto colateral real encontrado: `e2e/mobile.spec.ts` (bloque de fixes de móvil) aserta el texto literal "Reconciliación" en el panel "Más" — actualizado a "Pagos conciliados" en el mismo commit para no dejar el e2e roto.
- **Nav — 3 entradas "Fases futuras" (`Software propio`, `Marketplace`, `Inventario`), solo `org_admin`**: no hizo falta ningún flag nuevo — `href: null` en `nav-items.ts` ya reutiliza el patrón "placeholder deshabilitado" (badge "Pronto", `aria-disabled`, no clicable) que existe desde el bloque 0.4 y que `NavLink`/`MoreNavSheet` ya sabían renderizar; la visibilidad exclusiva a `org_admin` es el mismo `roles: ["org_admin"]` que ya usa el resto de "Ajustes". Lo único nuevo: un tercer valor de `section` (`"future"`, junto a `"data"`/`"settings"`) para darles su propio grupo con cabecera ("Próximamente"/"Coming soon") en sidebar y en el sheet "Más" — `sidebar.tsx`, `bottom-nav.tsx` y `more-nav-sheet.tsx` tocados para añadir ese tercer grupo, mismo patrón exacto que los dos existentes (`dataItems`/`settingsItems`).
- **Colisión de nombres evitada**: ya existía un `### 6.1 Inventario de herramientas internas` en `docs/TASKS.md` (software vibe-coded) — un concepto distinto del `Inventario` nuevo (activos hardware del grupo). Se retituló 6.1 a "Software propio (inventario de herramientas internas...)" para que el nombre visible coincida con la entrada de nav, y se añadieron 6.2 Marketplace (con la nota legal pedida: origen de datos vía partners/APIs licenciadas, scraping de pricing descartado por riesgo legal — el producto está pensado para venderse y pasar due diligence) y 6.3 Inventario (activos hardware, última prioridad) bajo el nuevo título de fase "FASE 6 — Fases futuras".
- **Verificación visual — script ad-hoc, no comiteado**: `signUp()` público contra el proyecto remoto rechazó `@example.test`/`@example.com` con "Email address is invalid" (validación de dominio de GoTrue en el proyecto remoto, más estricta que en local — los e2e existentes que usan `@example.test` están todos gateados a `usesLocalSupabase` y nunca se ejercitan contra remoto por esa vía). Se cambió a `service.auth.admin.createUser()` (bypassa esa validación al ser una acción admin) + `signInWithPassword()` para obtener sesión real; con eso, un org efímero (`org_admin`) y un invitado `finance` confirmaron exactamente lo esperado: `org_admin` ve el grupo "Próximamente" con las 3 entradas (grises, sin `<a>` asociado — solo `<span aria-disabled>`); `finance` no ve ni "Ajustes" ni "Próximamente" (0 elementos). Capturas tomadas y revisadas, org+usuarios efímeros borrados al terminar (`auth.admin.deleteUser` + delete de `organizations`), script y capturas no comiteados.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` con el mismo patrón de siempre (10 suites que requieren Supabase local, sin cambios respecto a antes de esta sesión, 131 passed/124 skipped). Commit `2fafea6` pusheado a `main`; **CI run 30492177218 en verde en ambos jobs**, confirmado con `gh run view` (no solo el exit code de `gh run watch`). Producción verificada tras el rollout de Railway: `/es` → 200, `/es/login` → 200, `/es/dashboard` → 307 (redirección a login, confirma la ruta viva sin 500). **Sesión cerrada en verde.**

## 2026-07-30 — Bloque 3.2a: Motor de aprobaciones (núcleo end-to-end)

Primera pasada del motor completo de SPECS §6, construida sobre 3.1b (que ya dejaba `current_step`/notificaciones preparadas para esto). Planificada en modo planificación (3 agentes Explore en paralelo: estado de 3.1b, infra de cron/JWT/conversión de moneda, roles/RLS/departamentos) + 2 rondas de `AskUserQuestion` antes de escribir código. Alcance dividido explícitamente en 3.2a (este bloque: motor real con matriz default sembrada, sin UI de edición) / 3.2b (diferido: RPCs de edición de `approval_rules` + `approval_delegations`), mismo patrón que 2.3/2.3b y 3.1/3.1b.

**Diseño confirmado por el usuario, con 3 condiciones no negociables:**
- **Links de aprobación — no JWT**: se descartó explícitamente una librería JWT (no existía ninguna en el repo) a favor de reusar el patrón ya probado de las invitaciones (token aleatorio de 256 bits + hash SHA-256 en DB, consumido con `for update`) — la propiedad de un solo uso exige estado en DB de todos modos, así que un JWT no aporta nada y sí añade una dependencia + gestión de claves nueva. Expiran a 72h (no 7 días, como las invitaciones) porque el recordatorio a las 72h ya trae un link fresco. Verificación con `crypto.timingSafeEqual` en `verifyApprovalToken()` (mismo idioma que `isAuthorized()` del cron de notificaciones): se trae el hash esperado vía `service-role` y se compara en tiempo constante, nunca con una comparación de igualdad de SQL expuesta al cliente para la previsualización. La decisión real (POST) pasa por `resolve_purchase_request_via_link()`, que hace su propia verificación atómica (`for update`) y llama a la MISMA función interna (`advance_purchase_request_step()`) que usa la UI — nunca hay una segunda copia de la lógica de aprobación. Mensaje de error idéntico (`invalid_or_expired_token`) para token no encontrado/expirado/usado/revocado — nunca se distingue el motivo.
- **Recordatorio 72h / escalado 7d — idempotencia de dos capas**: columna guarda (`reminded_at`/`escalated_at`, evita repetir la mutación) + índice único parcial en `notifications` (evita duplicar el mensaje), igual que `evaluate_renewal_alerts()` del bloque 2.1. Verificado con test explícito llamando al evaluador dos veces seguidas.
- **Snapshot de pasos inmutable**: `purchase_request_steps` copia los valores de la regla en el momento de crear la solicitud y nunca vuelve a leer `approval_rules` — verificado editando una regla global después de crear una solicitud y confirmando que el paso ya materializado no cambia (mientras que una solicitud nueva sí usa la regla editada).
- **Tope de seguridad al auto-skip** (pedido explícitamente por el usuario, no estaba en el diseño original): si TODA la cadena de pasos resueltos coincidiera con el propio solicitante (0 decisiones humanas de alguien distinto), el ÚLTIMO paso NO se salta — se reasigna a otro rol (`org_admin` solicitante → `finance`; cualquier otro rol → `org_admin`) con `resolved_via='reassigned_self_approval'`, explicado en la UI de pasos. Si ese rol tampoco tiene usuarios en la org, el paso queda `pending` igualmente — atascado y visible en vez de aprobado en silencio.
- **Fallback de departamento sin manager** (`manager_of_requester` con `departments.manager_user_id is null`): escalado a `org_admin` (reusa la misma idea que SPECS §6 ya prescribe para "aprobador desactivado"), con `resolved_via='fallback_no_manager'` explicado en la UI de pasos ("aprobación elevada a administración") y un badge "Sin manager" nuevo en `/team/departments` para que la carencia de configuración sea visible, no descubierta solo cuando le llegan aprobaciones ajenas a org_admin.

**Esquema** (`0023_approval_engine.sql`): `approval_rules` (config org-scoped, `department_id null`=global, sin RPC de edición — solo el seed), `purchase_request_steps` (snapshot: `status` distingue `queued`/`pending`/`escalated_to_org_admin`/terminal, único paso activo a la vez garantizado por índice parcial), `approval_actions` (log fino de movimientos, distinto de `audit_log`), `approval_link_tokens` (sin acceso de cliente en absoluto, como `audit_log`). `convert_amount()` es un puerto SQL literal de `src/features/dashboard/currency.ts` — necesario porque la materialización ocurre atómicamente dentro de `create_purchase_request()` (plpgsql), no se puede llamar a la versión TS desde ahí. `handle_new_user()` siembra la matriz default (4 reglas: <500 auto, 500-5000 manager, >5000 manager+finance) por primera vez desde que existe el trigger — más un backfill de la misma matriz para las orgs ya existentes (sin él, `create_purchase_request()` fallaría con "no approval rule matches" para cualquier org de prueba creada antes de esta migración).

**3 bugs reales encontrados en verificación (ninguno detectable por lint/typecheck/build)**:
1. **`gen_random_bytes()`/`digest()` de pgcrypto no viven en `public`** en este proyecto remoto (Supabase los instala en el esquema `extensions`) — las funciones nuevas usan `set search_path = ''` (mismo hardening que el resto del repo), así que la llamada sin calificar fallaba en runtime ("function gen_random_bytes(integer) does not exist"), invisible hasta ejercitar el RPC de verdad. Diagnosticado sin tocar nada (migración `0024`, un `do $$ raise notice $$` que solo lee `pg_proc`/`pg_namespace`) antes de arreglar con el esquema correcto (`0025`, `extensions.gen_random_bytes`/`extensions.digest`) — nunca se usó `drop extension`/`cascade` sobre una base compartida sin confirmar antes el impacto real.
2. **Recursión infinita de RLS**: al ensanchar `purchase_requests_select` para que el aprobador de un paso viera la solicitud (`purchase_request_steps` inline en la política), y dado que `purchase_request_steps_select` (ya existente) referenciaba `purchase_requests` inline a su vez, Postgres detectaba "infinite recursion detected in policy" en cuanto alguien sin rol finance/org_admin intentaba leer cualquiera de las dos tablas — un manager aprobador real no podía ni abrir `/requests/[id]` (404), aunque `resolve_purchase_request()` sí lo autorizara a decidir vía RPC. Arreglado (`0027`) con el mismo truco que ya usan `current_org_id()`/`current_user_role()`: una función `security definer` (`is_purchase_request_step_approver()`), propiedad del mismo rol que las tablas, que consulta `purchase_request_steps` directamente — los dueños de tabla no están sujetos a la RLS de su propia tabla (salvo `FORCE ROW LEVEL SECURITY`, no usado aquí), así que esa consulta interna nunca reevalúa la política y el ciclo se rompe ahí.
3. **Ambigüedad de embed de PostgREST**: `purchase_request_steps` referencia a `users` dos veces (`resolved_approver_id` y `decided_by`) — el `select(..., users(full_name))` implícito de la página de detalle fallaba con "more than one relationship was found for 'purchase_request_steps' and 'users'". Arreglado especificando la FK exacta (`users!purchase_request_steps_resolved_approver_id_fkey(full_name)`).

Los 3 bugs se encontraron en la MISMA verificación visual autenticada (Playwright contra `pnpm dev`, con capturas), no en la réplica funcional de RPCs — confirma otra vez por qué el procedimiento estándar de este repo insiste en la verificación visual además de la funcional.

- **Verificación funcional exhaustiva contra el remoto** (script ad-hoc con `service_role` + `signInWithPassword()` por usuario real, no comiteado): org efímera con 5 usuarios (org_admin/manager/finance/2 employees) y 2 departamentos (con manager / sin manager) cubriendo los 14 escenarios pedidos explícitamente por el usuario — seed sano vía trigger real (4 reglas), tier auto, tier manager, tier manager+finance (avance de paso completo), autorización negativa + RLS cruzada, fallback sin manager, tope de auto-skip, ciclo completo del link (uso único, reintento, token inventado, expirado, revocación cruzada por resolución vía UI), recordatorio y escalado (doble pasada, sin duplicar), e inmutabilidad del snapshot. Los 14 pasaron. Limpieza verificada a 0 filas en las 4 tablas nuevas + las de siempre (incluido `auth.admin.deleteUser`).
- **Verificación visual autenticada** (mismo org efímero, Playwright contra `pnpm dev`): página del link en sus dos caras (resumen con botones Aprobar/Rechazar; mensaje genérico de fallo), detalle de solicitud con la sección "Pasos de aprobación" nueva (el manager ve su propio paso resuelto — no el de finance, por diseño de RLS, ver arriba), y `/team/departments` con el badge "Sin manager" en el departamento sin manager asignado.
- **Tests**: `src/features/requests/approval-engine.test.ts` (nuevo — materialización por tier, precedencia departamento>global, conversión de moneda, fallback sin manager, tope de auto-skip, inmutabilidad, ciclo del link, idempotencia de recordatorio/escalado) + `src/features/requests/permissions.test.ts` (el describe de 3.1b se reescribió a 3.2a: las aserciones que asumían "finance/org_admin resuelven en bloque" ya no aplican con el motor multi-paso, ahora prueban que el aprobador REAL resuelto en `purchase_request_steps` es quien puede decidir) + `e2e/requests-approval.spec.ts` (2 tests nuevos: multi-paso manager→finance vía UI real, y el flujo del link sin login incluido el reintento con el mismo link). No ejecutables en esta máquina (requieren Supabase local) — se verifican vía CI.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` local con 11 suites requiriendo Supabase local (las 10 de siempre + el nuevo `approval-engine.test.ts`), 0 fallos inesperados (132 passed/149 skipped). `docs/TASKS.md` §3.2 dividido en 3.2a (checkbox de aceptación marcado) / 3.2b (delegaciones + UI de edición, pendiente).
- **Commit `b78b7f0` pusheado a `main`; CI run `30576618437` falló en ambos jobs** — 3 problemas reales, ninguno visto en la verificación contra el remoto (que no pasa por Playwright/CI): (1) `Requests.detail.steps.status.pending` reusaba "Pendiente"/"Pending", colisionando con el pill de estado de la solicitud — mismo patrón exacto ya documentado para el bloque 3.1, esta vez en la sección de pasos nueva; (2) mi propio `approval-engine.test.ts` comparaba `min_amount`/`max_amount` con literales de texto en vez de `Number(...)` — el mismo error de convención ya corregido una vez en `renewal-actions.test.ts`, repetido aquí; (3) el job de e2e no tenía `SUPABASE_SERVICE_ROLE_KEY` en su entorno — la página `/approvals/[token]` lee `approval_link_tokens` con `createServiceRoleClient()` en tiempo de ejecución, y hasta este bloque ningún flujo ejercitado por e2e necesitaba esa clave en el servidor de Playwright. Un cuarto fallo (`renewal-alerts.test.ts`, sin relación directa con este bloque) resultó ser una carrera preexistente: `evaluate_renewal_alerts()` no está acotada por `org_id`, y tanto `renewal-alerts.test.ts` como `renewal-actions.test.ts` la invocan — en paralelo (Vitest por defecto corre un archivo por worker), quien corre primero consume las filas que el otro esperaba insertar vía el mismo índice de idempotencia. `approval-engine.test.ts` cambió el timing lo suficiente para que esta carrera aflorara en CI por primera vez. Arreglado con `fileParallelism: false` en `vitest.config.ts` (serializa los archivos, sin tocar ninguna RPC) + selector `getByRole("option").filter({hasText:"Salesforce CRM"})` (mi propio e2e tenía un `"Salesforce"` ambiguo contra el catálogo real). Commit `0d01efa` con los 5 fixes — **CI run `30577486411` en verde en ambos jobs**, confirmado con `gh run view` directo.
- Producción verificada tras el rollout de Railway (primer poll): `/es` → 200, `/es/login` → 200, `/es/requests` y `/es/team/departments` → 307 (redirección a login, confirma rutas vivas sin 500). **Bloque 3.2a cerrado en verde.**

## 2026-07-31 — Bloque 3.2b: Delegaciones + administración de approval_rules (cierra §3.2)

Segunda pasada del motor de aprobaciones, sobre 3.2a (migraciones `0023`-`0027`). Planificado en modo planificación (1 agente Explore + lectura directa de las migraciones/RLS de 3.2a) con una ronda de `AskUserQuestion` que aportó 3 ajustes antes de escribir código.

**Diseño de la delegación (aprobado con el usuario, con 1 aportación mía razonada y aceptada — solo aprobador concreto, nunca un rol):**
- **Resolución dinámica, nunca congelada en el snapshot**: `purchase_request_steps` no cambia por delegación — sigue describiendo cómo se resolvió el aprobador ORIGINAL. `resolve_effective_approver(p_user_id, p_today)` (un solo salto, nunca sigue una delegación creada a partir del delegado) se invoca en caliente en cada punto donde hace falta saber "quién decide/a quién notificar hoy": la autorización de `resolve_purchase_request()` y la nueva `notify_step_active()`.
- **Aditivo, no exclusivo**: durante la ventana, tanto el original como el delegado pueden resolver — delegar no es ceder el asiento, es añadir cobertura.
- **Refactor de 3.2a con justificación**: las 4 llamadas casi idénticas "resolver destinatario + minar token + notificar" (`create_purchase_request`, `advance_purchase_request_step`, y las 2 pasadas de `evaluate_approval_reminders_and_escalations`) se unificaron en `notify_step_active()` — evitaba una 5ª copia casi-duplicada al añadir la resolución de delegación, y ahora hay un único sitio que puede quedar desincronizado en vez de cuatro.
- **Actor real + delegante, nunca suplantación silenciosa**: `approval_actions.delegated_from_id` (null = actuó por sí mismo). Para el link firmado —sin sesión— el token debe saber DE ANTEMANO para quién se minó: `approval_link_tokens` gana `token_actor_id`/`token_delegated_from_id`, fijados por `notify_step_active()` al resolver el efectivo, y `resolve_purchase_request_via_link()` atribuye la decisión a esos valores en vez de asumir siempre al aprobador original — sin este cambio, el bug real habría sido: un delegado que aprueba desde el email vería la decisión atribuida silenciosamente a quien delegó, no a quien realmente decidió.
- **3 ajustes pedidos tras la propuesta inicial, los 3 incorporados**: (1) si el aprobador EFECTIVO (el delegado) coincide con el `requester_id` de esa solicitud concreta, `notify_step_active()` NO le notifica ni le mina token — cae al aprobador original (la guarda de decisión ya le impediría actuar; notificar a quien no puede decidir no tenía sentido) — test dedicado junto al de anti-auto-aprobación; (2) confirmado que el seed restaurado por `restore_default_approval_rules()` reproduce los mismos umbrales `0/500/5000` literales que `handle_new_user()`, en la moneda default de la org, nunca EUR hardcodeado (`approval_rules.min_amount`/`max_amount` siempre se interpretan en esa moneda porque `convert_amount()` ya convierte el importe de la solicitud a ella antes de comparar); (3) confirmado (grep) que `purchase_request_steps` no tiene ninguna FK hacia `approval_rules` — el borra+reinserta atómico de `save_approval_rule_scope()` es estructuralmente incapaz de romper o bloquear una solicitud en vuelo, con test dedicado (solicitud multi-paso a medio resolver + edición de reglas + avance normal posterior).
- **Ensanche de RLS necesario, mismo bug de 3.2a si se omite**: un delegado vigente debe poder VER la solicitud/paso que va a resolver (no solo estar autorizado por RPC) — `is_purchase_request_step_approver()`, `purchase_request_steps_select` y `approval_actions_select` se ensancharon (mismo idioma `drop policy`/`create policy`) con un nuevo helper `is_active_delegate_of()`, llamado directamente desde políticas RLS (nunca revocado de `authenticated`/`anon`, igual que `current_org_id()`/`is_purchase_request_step_approver()` — a diferencia de `resolve_effective_approver()`, que SÍ toma un `p_user_id` arbitrario y por eso se revoca de todo excepto llamadas internas entre funciones `security definer`).

**Desviación deliberada de `docs/TASKS.md`** (pedía 3 RPCs `create_approval_rule`/`update_approval_rule`/`delete_approval_rule`, edición fila a fila — aprobada explícitamente por el usuario tras proponerla): `save_approval_rule_scope(p_department_id, p_tiers jsonb)` reemplaza ATÓMICAMENTE todo el tramo de un scope (borra+reinserta en la misma transacción). Editar filas sueltas hace que "sin huecos ni solapamientos" sea una validación dependiente del orden de operaciones; modelando la matriz como una partición ordenada (cada tramo = su `max_amount`, el `min_amount` es siempre el `max_amount` del tramo anterior, el último tramo siempre sin límite), la UI construye por diseño una estructura sin huecos ni solapes, y la RPC solo valida la FORMA (orden ascendente, cada tramo con ≥1 paso, `step_order` contiguo, tipos de aprobador coherentes) en vez de comparar contra el estado previo en la DB. `restore_default_approval_rules()`: global repone el seed; departamento borra sus overrides (vuelve a heredar la global). Gate `org_admin`-only (no finance) — mismo criterio que `departments`/`companies` (estructura organizativa, no un asunto financiero como sí lo son los presupuestos).

**UI**: `/team/approval-rules` (editor de tramos por scope — grupo Global siempre presente + un grupo por departamento con overrides, "+ Añadir tramo"/"Quitar último tramo" mantienen la partición contigua por construcción, selector para personalizar un departamento nuevo copiando los tramos globales vigentes como punto de partida editable, "Restaurar valores por defecto" con `ConfirmDialog`, aviso permanente de que los cambios no afectan solicitudes en vuelo) + `/settings/delegations` (autoservicio para cualquier rol: "Tus delegaciones" + "Estás cubriendo a" de solo lectura +, si `org_admin`, "Todas las delegaciones de la organización" en la misma página). Nav: `approvalRules` (`org_admin`) y `delegations` (`roles: "all"`, único ítem de "Ajustes" no restringido a `org_admin`) — cuidado de no reusar la clave `teamRules` ya existente (apunta a `/team/members`).

- **Verificación funcional exhaustiva contra el remoto** (script ad-hoc con `service_role` + `signInWithPassword()` por usuario real, no comiteado, autorización explícita del usuario): 10 escenarios — tramo válido, 6 validaciones rechazadas (huecos, solapes, `auto` con >1 paso, tramo vacío, rol inválido, no-`org_admin`), `restore_default_approval_rules`, delegaciones (crear/solape rechazado/revocar/ajeno rechazado), delegación vigente resuelta por el delegado (actor+delegante correctos en `approval_actions`, notificación solo al delegado), anti-auto-aprobación vía delegado con el edge del punto 1 (notificación cae al original), atribución vía LINK con delegación vigente (`token_actor_id`/`token_delegated_from_id` correctos, decisión atribuida al delegado real), RLS (el delegado ve la solicitud/paso), delegación expirada/revocada (ya no permite actuar, el original sigue pudiendo), e inmutabilidad bajo edición en vuelo. Los 10 pasaron a la primera. Limpieza verificada a 0 filas en todas las tablas nuevas (incluidas `approval_delegations` y las columnas nuevas de tokens) + `auth.admin.deleteUser`.
- **Verificación visual autenticada** (mismo org efímero, Playwright contra `pnpm dev`): timeline "Decidido por {delegado} en nombre de {delegante}" en el detalle de la solicitud; matriz de reglas global con sus 3 tramos y controles de edición; vista `org_admin` de "todas las delegaciones" (vigente/revocada con sus pills de estado); vista propia del delegante ("Tus delegaciones"/"Estás cubriendo a"). Un indicador "1 Issue" del overlay de Next dev se investigó (consola sin errores/warnings relevantes) y se confirmó que es el hydration warning pre-existente de `notification-bell.tsx` ya documentado y descartado como fuera de alcance en una sesión anterior — aparece en cualquier página autenticada, no algo introducido aquí.
- **Tests**: `src/features/delegations/delegations.test.ts` (nuevo — gestión de delegaciones: autoservicio, solape rechazado, auto-delegación rechazada, ajeno rechazado, revocar, RLS; y aplicadas al motor: delegado aprueba con atribución correcta, aditivo, edge del delegado=solicitante, atribución vía link, RLS de visibilidad, sin cadena) + `src/features/approval-rules/approval-rules.test.ts` (nuevo — tramo válido, 5 validaciones rechazadas parametrizadas, `specific_user` fuera de la org, no-`org_admin` rechazado, `restore_default_approval_rules` global/departamento, inmutabilidad bajo edición con solicitud multi-paso en vuelo) + `e2e/requests-approval.spec.ts` (nuevo describe: delegar→el delegado aprueba vía UI→timeline "en nombre de"; `acceptInvitationViaApi`/`setUpDepartmentEngine` se subieron a nivel de módulo para no duplicarlos una 3ª vez). No ejecutables en esta máquina (requieren Supabase local) — se verifican vía CI.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` local con 13 suites requiriendo Supabase local (las 11 de siempre + los 2 nuevos archivos de este bloque), 0 fallos inesperados (132 passed/172 skipped). `docs/TASKS.md` §3.2 marcado completo (3.2a+3.2b) — 3.3/3.4 de la Fase 3 siguen sin empezar, así que la Fase 3 NO se marca cerrada como fase, solo §3.2.
- **Commit `c29c1bd` pusheado a `main`; CI run `30620127265` falló en el job de e2e** — 2 problemas reales, ninguno visto en la verificación funcional/visual contra el remoto (que no ejercita el nav ni el botón "Aprobar" vía RLS+UI combinados de la misma forma que Playwright): (1) **bug real de correctness**: `canApprove` en `requests/[id]/page.tsx` solo comprobaba `resolved_approver_id === me.id` o coincidencia de `approver_role` — nunca consultaba si el usuario actual era delegado vigente, así que un delegado real podía aprobar vía RPC (la verificación funcional con `service_role` lo confirmó) pero JAMÁS veía el botón "Aprobar" en la UI autenticada real — el propio e2e nuevo de este bloque lo detectó al intentar clicarlo. Arreglado añadiendo una tercera condición con `is_active_delegate_of()` (mismo helper ya usado por las políticas RLS, no revocado de `authenticated`, así que ya era invocable vía `supabase.rpc()` sin RPC nueva). (2) La nueva entrada de nav `delegations` (`roles: "all"`) hace que la sección "Ajustes" pase a ser visible para CUALQUIER rol (antes solo tenía ítems `org_admin`) — dos tests preexistentes (`shell.spec.ts`, `mobile.spec.ts`) asumían "Ajustes" como admin-only y ahora fallaban; no es una regresión sino la consecuencia correcta y esperada del diseño de autoservicio aprobado — se actualizaron las aserciones para comprobar que el empleado/finance SÍ ve "Ajustes"+"Delegaciones" pero NO ve los ítems de administración dentro. Commit `53984de` con ambos fixes — **CI run `30620698364` en verde en ambos jobs**, confirmado con `gh run view` directo.
- Producción verificada tras el rollout de Railway: `/es` → 200, `/es/login` → 200, `/es/team/approval-rules` y `/es/settings/delegations` → 307 (redirección a login, confirma rutas nuevas vivas sin 500). **Bloque 3.2 (a+b) cerrado en verde.**

## 2026-07-31 — Bloque 3.3: conversión de solicitud aprobada en vendor/contrato

Diseño propuesto en modo planificación (auditoría de CI + lectura de SPECS/TASKS/DECISIONS + exploración de código existente de vendors/requests) y aprobado por el usuario con 3 ajustes antes de escribir código.

**Diseño aprobado:**
- **Sin estado nuevo**: la solicitud se queda `approved`; la señal de "ya materializada" es la presencia de `converted_contract_id`, no un valor nuevo del `check` de `status` (que ya ha cambiado dos veces en bloques anteriores). La lista `/requests` añade un badge "Convertida" junto al pill de estado cuando aplica.
- **Reutiliza `create_vendor`/`create_contract` tal cual** — la única pieza nueva es `record_purchase_request_conversion(p_request_id, p_vendor_id, p_contract_id)`, una RPC `security definer` que SOLO enlaza (guarda anti-doble-conversión vía `converted_contract_id is not null`, valida que vendor/contrato pertenezcan a la org y que `contract.vendor_id = p_vendor_id`, escribe `audit_log`). Se invoca añadiendo un parámetro opcional `sourceRequestId` a los server actions ya existentes `createVendorWithContract`/`createContract` — nada de vía paralela de creación.
- **Sin transacciones distribuidas (instrucción explícita del usuario)**: la creación de vendor+contrato (2 RPCs ya separadas desde 1.2) y el enlace posterior NO están en la misma transacción. Si el enlace falla tras crear vendor+contrato con éxito, ambos quedan creados y la solicitud sigue ofreciendo "convertir" — enlazable a posteriori al vendor/contrato ya existentes vía el camino de "vendor existente", sin recrear nada. Verificado con un test dedicado (`conversion.test.ts`) que fuerza el fallo del enlace (contract_id inválido) y confirma la recuperación posterior con los ids correctos.
- **Detección de vendor existente por `catalog_id` exacto**: sin heurística nueva, un simple `select` en `vendors` ya scoped por RLS (`finance`/`it_admin`/`org_admin`). Sin `catalog_id` en la solicitud (herramienta custom), se salta la detección.
- **Página intersticial `/requests/[id]/convert`** con gate server-side (rol + `status='approved'` + no convertida ya) ANTES de renderizar — pedido explícitamente por el usuario, no basta con esconder el enlace: una URL directa a una solicitud ajena, no aprobada, o ya convertida redirige (`notFound()`/`redirect()`), nunca expone el intersticial. Si hay match por catálogo, ofrece "Añadir contrato aquí" (formulario mínimo reutilizando `ContractFields`, 4º uso) o "Crear vendor nuevo de todas formas"; sin match, redirige directo a `/vendors/new` precargado (comportamiento de 3.1b sin cambios visibles).
- **`purchased` vs `converted` — distinción explícita para que 3.4 no ambigüe**: son dos conceptos independientes y deliberadamente no acoplados. `mark_purchase_request_purchased()` (3.1b) sigue siendo un botón manual, sin automatismo, que expresa "ya se compró" (acción del solicitante/finance/org_admin sobre el estado de la solicitud). `converted_contract_id` (este bloque) expresa "ya está en el inventario de vendors/contratos" (acción de finance/it_admin/org_admin que materializa el dato real). Una solicitud puede estar convertida sin estar marcada como comprada (o viceversa, aunque el flujo normal las junta) — no se fusionaron ni se hizo que una dispare la otra automáticamente, tal como confirmó el usuario. Cualquier UI futura (3.4, dashboards) debe leer ambos campos por separado si necesita "¿ya se compró?" vs "¿ya tiene contrato real?".

**Esquema** (`0030_purchase_request_conversion.sql`): `purchase_requests.converted_vendor_id`/`converted_contract_id` (FK `on delete set null`, mismo patrón que `catalog_id`/`department_id` de la misma tabla) + `contracts.source_request_id` (FK `on delete set null`, enlace inverso — "origen: solicitud de compra" en la ficha del vendor, con link de vuelta a la solicitud).

- **Verificación funcional exhaustiva contra el remoto** (script ad-hoc con `service_role`, no comiteado): org efímera con org_admin + employee. 8 escenarios, todos en verde a la primera: enlace correcto (con `audit_log`), guarda anti-doble-conversión, rol no autorizado, solicitud no aprobada, contrato de otro vendor, vendor/contrato de otra org, **el caso de enlace-fallido-luego-recuperado pedido explícitamente**, y detección de vendor existente por `catalog_id` (visible en la org, invisible entre orgs). Limpieza verificada a 0 filas huérfanas (`auth.admin.deleteUser` + delete de `organizations`).
- **Verificación visual autenticada** (mismo org efímero, navegador real vía `mcp__claude-in-chrome` contra `pnpm dev` apuntando al remoto): los 3 casos pedidos explícitamente por el usuario — (1) solicitud aprobada sin vendor previo → "Crear vendor/contrato" → sin match → `/vendors/new` precargado → creado → ficha de la solicitud muestra "Ver contrato", ficha del vendor muestra "Origen: solicitud de compra"; (2) segunda solicitud del mismo catálogo → intersticial muestra "Ya tienes Figma en tu stack" → "Añadir contrato aquí" → formulario mínimo precargado → creado → vendor con 2 (luego 3, con una tercera solicitud) contratos, cada uno con su propio origen; (3) navegación directa a `/requests/{id}/convert` de una solicitud ya convertida → redirige de vuelta a su ficha sin exponer el intersticial, el botón "Crear vendor/contrato" ya no existe en esa ficha. Lista `/requests` confirmada con el badge "Convertida". Limpieza verificada a 0 filas huérfanas tras el script de verificación.
- **Tests**: `src/features/requests/conversion.test.ts` (nuevo — los 8 escenarios de arriba, portados a Vitest) + `e2e/requests-approval.spec.ts` (nuevo describe "Cierre del ciclo: conversión a vendor/contrato": aprobar→crear vendor nuevo con origen→segunda solicitud del mismo catálogo→enlazar a vendor existente→reconversión bloqueada, todo en un único test end-to-end). No ejecutables en esta máquina (requieren Supabase local) — se verifican vía CI.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local; `pnpm test` local con 14 suites requiriendo Supabase local (las 13 de siempre + `conversion.test.ts`), 0 fallos inesperados (132 passed/180 skipped). Commit `2f58397` pusheado a `main`; **CI run `30624844702` en verde en ambos jobs**, confirmado con `gh run view` directo (no solo el exit code de `gh run watch`).
- Producción verificada tras el rollout de Railway: `/es` → 200, `/es/login` → 200, `/es/dashboard`, `/es/requests`, `/es/requests/new`, `/es/vendors` → 307; las 2 rutas nuevas (`/es/requests/{uuid}/convert`, `/es/requests/{uuid}/convert/existing/{uuid}`, probadas con un uuid inventado) → 307 (redirección a login, confirma rutas vivas sin 500). `docs/TASKS.md` §3.3: primer checkbox (conversión) marcado; el segundo ("Historial de negociación y notas por vendor") queda fuera de alcance de esta sesión — no fue pedido y ya existe una pestaña "Notas" + renegociación con `savings_records` de bloques anteriores, pendiente de revisar si cubre el criterio o falta algo específico. **Bloque 3.3 (conversión) cerrado en verde.**

## 2026-07-31 — Bloque 3.4: Catálogo interno (detección de solapamiento) — cierra la Fase 3

**Discrepancia real detectada al auditar antes de diseñar**: el usuario pidió "bloque 3.4 — vista de gestión de procurement" (tabla org-wide de solicitudes con filtros y métricas de cabecera, para MANAGER_ROLES). Al leer `docs/SPECS.md`/`docs/TASKS.md` §3.4 real ("Catálogo interno": al solicitar, detectar solapamiento con software ya existente y mostrar el aviso con ahorro neto — sin relación con la vista de gestión), y confirmar que no hay mockup de esa vista en `design-stackx.html`, se preguntó al usuario cómo reconciliarlo. Decisión: construir el §3.4 REAL en esta sesión (es lo que cierra la Fase 3 formalmente); la vista de gestión queda anotada como "Bloque adicional (futuro)" en `docs/TASKS.md` (sin construir), con la nota explícita de que requerirá decidir antes la ampliación de `purchase_requests_select` para incluir `it_admin` (la política actual post-3.2b solo da visibilidad org-wide a `finance`/`org_admin` + solicitante + aprobadores/delegados de paso — no cubre el patrón `MANAGER_ROLES` estándar del resto del repo, que sí incluye `it_admin`). Sin esto, "3.4" se habría cerrado con una feature distinta a la documentada y la Fase 3 se habría marcado cerrada sin que su criterio de aceptación real estuviera satisfecho.

**Diseño aprobado, con 3 condiciones del usuario:**
1. **`check_catalog_overlap()` devuelve dos niveles según el rol del caller** (resuelto internamente vía `auth.uid()`, nunca un parámetro): cualquier empleado ve existencia/vendor/departamento/empresa/owner/nº de contratos activos SIN importes; `MANAGER_ROLES` (finance/it_admin/org_admin) ven además coste/moneda/ciclo de cada contrato. Verificado con un test que inspecciona el payload crudo de la RPC (no solo lo que la UI decide mostrar): `catalog-overlap.test.ts` confirma que un empleado recibe `cost_amount`/`currency`/`billing_cycle` en `null`.
2. **Sin enumerabilidad entre orgs**: la función valida que `p_catalog_id` existe en `saas_catalog` (si no, `raise exception`) y busca estrictamente en `v_caller.org_id` (resuelto de la fila `users` del caller, nunca un parámetro) — un `org_admin` de otra org no puede sondear solapamientos ajenos pasando cualquier `catalog_id`. El helper interno `has_active_catalog_overlap(p_org_id, p_catalog_id)` SÍ toma `org_id` como parámetro (porque lo invocan funciones que ya resolvieron al caller), así que se revoca su `execute` de `public`/`anon`/`authenticated` — invocable solo función-a-función, igual que `resolve_effective_approver()` (3.2b) por la misma razón (un parámetro arbitrario que un cliente podría usar para sondear otra org/usuario).
3. **"Ahorro potencial" etiquetado como estimación, nunca cifra contable**: label "Ahorro potencial estimado" + nota explícita ("Estimación, no una cifra contable...") en la propia UI — es literalmente el coste anual de la solicitud en curso (sin fórmulas especulativas ni conversión de moneda entre lo existente y lo solicitado), coherente con el criterio que se aplicará al futuro motor de ahorros.

**Excepción de rol — alcance exacto**: `check_catalog_overlap()` es la única vía del repo donde un empleado sin rol MANAGER_ROLES obtiene *cualquier* dato derivado de `vendors`/`contracts` (tablas restringidas a MANAGER_ROLES desde el bloque 1.2). El alcance está deliberadamente acotado: (a) solo para el `catalog_id` EXACTO que el empleado está a punto de solicitar, nunca una lista del resto del catálogo de vendors de la org; (b) al nivel básico, ningún importe cruza el límite de rol, solo existencia + a quién preguntar (owner) + dónde (departamento/empresa); (c) no hay mutación posible por esta vía, es una función `stable`.

**`known_overlap` calculado server-side, no aceptado del cliente**: `create_purchase_request()` computa el flag invocando el mismo helper `has_active_catalog_overlap()` con el `org_id`/`catalog_id` ya resueltos — no hay ningún parámetro `p_known_overlap` que un formulario pudiera falsificar. El detalle de la solicitud (para solicitante y aprobador) no guarda un snapshot del contenido del aviso: si `known_overlap` es `true`, se vuelve a llamar a `check_catalog_overlap()` en vivo (mismo nivel de detalle según el rol del viewer) — evita mostrar datos de contrato desactualizados si el vendor cambia después.

**Bug real encontrado y corregido de paso (pre-existente, no introducido por este bloque)**: `src/components/shell/notification-bell.tsx` nunca manejaba los tipos `purchase_request_step_pending`/`_reminder`/`_escalated` (introducidos en 3.2a) — caían al `else` genérico de `renewal_alert`, mostrando texto incorrecto ("X renueva en 0 días"). Se detectó al intentar propagar `known_overlap` a esos 3 tipos: no tenía sentido añadir el aviso a un mensaje ya mal enrutado. Corregido con las 3 ramas que faltaban (reutilizando la clave `purchaseRequestSubmitted` para `step_pending`, que es semánticamente la misma notificación tras el refactor de 3.2a) — no se tocó nada del motor de aprobaciones, solo el render del cliente.

**Esquema** (`0031_catalog_overlap.sql`): `purchase_requests.known_overlap boolean not null default false`; `has_active_catalog_overlap()` (helper interno, revocado de todo rol de cliente); `check_catalog_overlap()` (RPC de dos niveles); `create_purchase_request()` y `notify_step_active()` redefinidas (mismo patrón de `create or replace` ya usado en 0023/0028) para calcular/propagar el flag. Aplicada al remoto con `supabase db push` (sin prompt de contraseña, sesión ya enlazada).

- **Verificación funcional** (`src/features/requests/catalog-overlap.test.ts`, contra Supabase local — no ejecutable en esta máquina, ver CI): sin match → `has_overlap=false`; catalog_id inexistente → error; nivel manager con importes vs. nivel empleado sin importes (payload crudo verificado); contrato cancelado no cuenta; vendor inactivo no cuenta; no enumerable entre orgs; `create_purchase_request()` calcula `known_overlap` correctamente (con y sin solapamiento).
- **Verificación visual autenticada contra el remoto** (org efímera vía `service_role.auth.admin.createUser()` + `create_invitation()`, mismo patrón que sesiones anteriores; navegador real vía `mcp__claude-in-chrome` contra `pnpm dev` apuntando al remoto, script y capturas no comiteados): vendor+contrato "Figma" (36.000€/año) creados en la org; el empleado, al elegir "Figma" en `/requests/new`, ve el banner básico "Ya contratado en tu organización — Ya contratado (1 contrato activo). Consulta con tu equipo antes de duplicarlo." SIN ninguna cifra; envía la solicitud (5000€, cae en tier manager+finance, 2 pasos); el `org_admin` ve la notificación en la campanita con "⚠ Solapamiento conocido con el stack existente"; abre el detalle y ve el nivel completo (fila de contrato "Figma — 36.000 €/año", bloque "AHORRO POTENCIAL ESTIMADO — 5000 € — Estimación, no una cifra contable..."); aprueba el paso 1 (Administrador de organización) sin fricción — el paso 2 (Finanzas) queda "En curso", el motor multi-paso en sí ya está cubierto por `approval-engine.test.ts`/3.2a, no era el foco de esta verificación. Limpieza verificada (`auth.admin.deleteUser` ×2 + delete de `organizations`); script y capturas no comiteados.
- **Tests**: `catalog-overlap.test.ts` (nuevo, 7 tests) + `e2e/requests-approval.spec.ts` (nuevo describe "Catálogo interno: aviso de solapamiento al solicitar" — nivel básico sin `/año` visible, nivel manager con `/año` visible + aviso en campanita + aprobación). No ejecutables en esta máquina (requieren Supabase local) — verificados primero contra el remoto (arriba) y se confirman en verde vía CI.
- **3 bugs reales encontrados por el propio CI, ninguno visto en la verificación manual contra el remoto (cada escenario allí usó su propia org efímera, o no llegó a ejercitar la ruta exacta que falló)**:
  1. `catalog-overlap.test.ts` reutilizaba un único `catalog_id` compartido entre varios tests de la misma org — choca contra `vendors_org_catalog_id_unique_idx` (bloque 1.3, como mucho un vendor por `(org_id, catalog_id)`) en cuanto un segundo test intenta enlazarlo. Arreglado: cada test que crea un vendor usa un `catalog_id` propio, tomado de una lista de 6 fijada en `beforeAll`.
  2. El mismo test de "contrato cancelado" llamaba a `update_contract(..., p_status: 'cancelled')` sin comprobar el error — `update_contract()` rechaza explícitamente ese valor desde el bloque 2.3b ("use `cancel_contract()` to cancel a contract, not `update_contract()`"), así que el contrato nunca quedaba cancelado de verdad y la aserción fallaba. Arreglado usando `cancel_contract()` (la RPC real de cancelación, que además captura el ahorro en `savings_records`) y comprobando su `error`.
  3. `e2e/requests-approval.spec.ts`: `getByLabel("Notificaciones")` resolvía a 2 elementos (el mismo `aria-label` decora tanto el botón disparador como su menú desplegable) — cambiado a `getByRole("button", {name:...})`, el patrón ya usado más arriba en el mismo archivo. Y el coste de la solicitud (5000€) caía en el tier de 2 pasos (manager+finance) — tras aprobar solo el paso 1 la solicitud seguía `pending`, no `approved`, y la aserción final esperaba "Aprobada". Arreglado bajando el coste a 3000€ (tier "manager" de 1 solo paso), coherente con el resto de e2e del repo que ya elige el coste deliberadamente para controlar el nº de pasos.
  Commit `aa43cb8` corrigió (1) y (3, parte getByLabel) pero no vio (2) ni la parte del coste de (3) porque el propio fix de (1) dejó pasar el resto sin ejecutarse hasta el siguiente run de CI — confirmado con `gh run view` directo, no solo el exit code de `gh run watch` (que había reportado 0 en el run de en medio pese al fallo real).
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm build` en verde local. `pnpm test` local falla únicamente en las suites que ya requerían Supabase local (mismo patrón preexistente en esta máquina, sin Docker) — `catalog-overlap.test.ts` incluido, mismo comportamiento que `conversion.test.ts`/`permissions.test.ts`. Commits `6598aa9` (feature) → `aa43cb8` → `f1b39fb` (los 3 fixes de arriba) pusheados a `main`; **CI run `30711749234` en verde en ambos jobs**, confirmado con `gh run view` directo (no solo el exit code de `gh run watch`, que en el run intermedio `30628804833` había reportado 0 pese al fallo real). Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/requests`, `/es/requests/new` → 307 (redirección a login, confirma rutas vivas sin 500). `docs/TASKS.md` §3.4 marcado. **Bloque 3.4 cerrado en verde — FASE 3 (Procurement, 3.1–3.4) queda completa.**

## 2026-08-03 — Rediseño de renovaciones: agenda por tramos (dashboard) + calendario enriquecido

Feedback de uso con volumen real (org de producción con ~50 vendors demo sembrados, ver sesión anterior): la pista de renovaciones (línea de tiempo horizontal, 2 carriles fijos) se saturaba con solapes, y el calendario no tenía forma de saltar a un mes lejano ni ver la densidad del año de un vistazo. Diseño de reemplazo ya decidido con el usuario (mockup aprobado) — sesión puramente de implementación.

**Decisión explícita, no un bug corregido aquí**: la pista calculaba (y la nueva agenda sigue calculando) el tono de cada ticket con `renewalTone(daysUntil)` — días BRUTOS hasta `renewal_date`. El calendario y `buildStackStatus` (donut "Estado del stack") usan en cambio `renewalTone(actionableDaysUntil(...))`, que adelanta el umbral cuando el contrato auto-renueva y hay preaviso. Esta discrepancia ya existía entre 3 componentes del dashboard antes de este bloque. Se preserva tal cual — unificarla habría sido un cambio de comportamiento de negocio no pedido en un rediseño puramente visual. Queda anotado por si se decide unificar en un bloque futuro.

**Dashboard — `renewal-track.tsx` → `renewal-agenda.tsx`, de Server a Client Component**: `buildRenewalTrack()` se sustituyó por `buildRenewalTickets()` (mismo cálculo de tono/preaviso, añade `annualCostOrgCurrency` vía `convertAmount()` para poder sumar por tramo en una sola moneda; se eliminaron `xPercent`/`lane`, artefactos del posicionamiento absoluto de la línea de tiempo ya no aplicable) + nueva `groupRenewalTicketsByTier()` (pura, bucketea por `ticket.tone` — equivalente a comparar contra `CRITICAL_THRESHOLD_DAYS`/`WARNING_THRESHOLD_DAYS` ya que `renewalTone` los encapsula — y filtra por el rango elegido, sin round-trip al servidor: el fetch inicial siempre trae el máximo de 120 días, el selector 30/60/90/120 solo re-filtra en cliente). Estados de expansión (`upcomingExpanded`/`stableExpanded`) son `useState` de sesión, no persistidos. Etiquetas de tramo (`Crítico`/`Próximo`/`Estable`) reutilizan las claves ya existentes de `Shell.dashboard.stackStatus` en vez de duplicarlas.

**Calendario — picker de mes/año, vista anual, chips enriquecidos**: `CalendarMarker` gana `vendorWebsite`/`annualCost`/`currency` (sin conversión de moneda, igual que las filas de la agenda — coste nativo del contrato). Nueva `worstTone()` (prioridad rojo>ámbar>neutro) para colorear el punto de un día con varios marcadores en la vista anual, que por lo demás reutiliza `buildCalendarMonth()` llamada 12 veces (sin duplicar lógica de rejilla). Picker de mes/año (`month-picker.tsx`) construido con `@base-ui/react/menu` (mismo primitivo que `kebab-menu.tsx`, sin librería nueva) — año "en edición" local en el popup, no muta la vista hasta elegir un mes. `MarkerChip` gana `aria-label={vendorName}` explícito (el nombre accesible no se contamina con el logo/coste añadidos, preserva las aserciones e2e existentes por `getByRole("link", {name, exact:true})`) y `title` nativo con nombre+coste completo como red de seguridad cuando el chip no tiene sitio.

- **Tests**: `aggregate.test.ts` (sustituye el describe de `buildRenewalTrack` por `buildRenewalTickets`+`groupRenewalTicketsByTier`, 18 tests) + `calendar.test.ts` (nuevos campos de `CalendarMarker` + describe de `worstTone`, 14 tests) — ambos en verde local. `e2e/dashboard.spec.ts` (nuevo test de la agenda: secciones/rango/expandir-colapsar/navegación) + `e2e/renewals.spec.ts` (nuevos tests de picker y vista anual + aserción de chip enriquecido en el test existente). No ejecutables en esta máquina (requieren Supabase local) — se verifican vía CI.
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm test && pnpm build` en verde local (`pnpm test` falla únicamente en las suites que ya requerían Supabase local antes de este bloque, sin relación con estos cambios). Commit `15abe92` pusheado a `main`; **CI run `30840715338`** — el job "Lint, typecheck, unit tests" falló la primera vez con `Unknown Error: JWT issued at future` en `vendors/permissions.test.ts` (la flakiness de reloj del contenedor Supabase del runner ya documentada el 2026-07-07, no relacionada con este bloque — ambos archivos de test nuevos de esta sesión pasaron limpios en ese mismo run), se relanzó (`gh run rerun --failed`) y quedó **en verde en ambos jobs**, confirmado con `gh run view` directo. Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/renewals` → 307 (redirección a login, confirma rutas vivas sin 500).
- **Verificación visual autenticada con los datos demo YA sembrados** (org real `wowinx`, sin seed nuevo, sin borrar los demo al terminar — pedido explícito del usuario): el magic link de Supabase (implicit grant, tokens en el fragmento de la URL) no lo consume esta app (login está construido para password/PKCE vía `/api/auth/callback`, no hay handler de hash en `/login`) — en vez de perseguir ese flujo, se giró la contraseña de un usuario demo (`role='finance'`, nunca la cuenta real de Oliver) a un valor conocido temporalmente vía `auth.admin.updateUserById()`, se hizo login real por la UI contra `pnpm dev` (mismo remoto que producción — Railway aún no tenía la whitelist de redirect URL necesaria para el magic link), y se rotó la contraseña a un valor aleatorio descartado inmediatamente después de las capturas. Confirmado con datos reales de los ~50 vendors demo: agenda a 120d (Crítico 15/Próximo 14/Estable 11, sumas correctas), agenda a 30d (Próximo baja a 12, Estable a 0 con el estado vacío en positivo), expandir/colapsar Próximo y Estable, calendario mensual denso con chips logo+coste, picker de mes/año abierto, vista anual con puntos de urgencia por mes. **Bloque cerrado en verde.**

## 2026-08-04 — Corrección de semántica: la agenda por tramos se unifica con la fecha accionable (calendario/donut)

Sesión de corrección pura, sin features nuevas. Resuelve la discrepancia anotada explícitamente el 2026-08-03 (arriba): la agenda por tramos clasificaba con días BRUTOS hasta `renewal_date` (`renewalTone(daysUntil)`), mientras el calendario y `buildStackStatus` (donut "Estado del stack") ya usaban `renewalTone(actionableDaysUntil(...))` (preaviso descontado si el contrato auto-renueva). Un contrato con preaviso largo podía verse "estable" en la agenda estando ya fuera de plazo para cancelar — visto de forma concreta en producción: **Notion (Demo)** renueva en 51 días brutos pero con 45 de preaviso son solo 6 días accionables; antes se habría bucketeado en Próximo/Estable, ahora cae en Crítico.

- **Unificación, cero lógica nueva**: `buildRenewalTickets()` (`src/features/dashboard/aggregate.ts`) pasa a calcular `tone` con `renewalTone(actionableDaysUntil(...))` en vez de `renewalTone(daysUntil)` — misma pareja de helpers que ya usaban `buildCalendarMonth()`/`buildStackStatus()`, sin tocar `renewal.ts`. `groupRenewalTicketsByTier()` no cambió: sigue bucketeando por `ticket.tone`, que ahora ya viene calculado con el criterio correcto.
- **`RenewalTicket` gana `actionableDaysUntil`** (`src/features/dashboard/types.ts`) junto al `daysUntil` bruto que ya existía — se conservan los dos porque cada uno alimenta una parte distinta de la fila (ver siguiente punto), no porque hiciera falta un tercer número.
- **Cifra principal de la fila = la que decide el tramo, sin tocar el aviso de preaviso**: en `renewal-agenda.tsx`, el caso general (`runway.daysRemaining`) pasa a mostrar `actionableDaysUntil` en vez de `daysUntil` — es literalmente el número que decide el color/tramo, así que ahora nunca contradice al tono. Los otros dos casos (vencido `runway.overdue`, y con preaviso activo `runway.noticeWarning`) se dejaron **sin tocar** (mismo `daysUntil` bruto de siempre): en ambos, `actionableDaysUntil` es matemáticamente siempre ≤7 (tono rojo garantizado — vencido es vencido en cualquier criterio; `noticeWarning` ya implica que el preaviso venció), así que no hay contradicción posible con el número bruto y cambiarlo solo habría introducido cifras negativas confusas ("-5 días") sin ganar nada.
- **La ventana de la agenda (120d del fetch inicial + selector 30/60/90/120) sigue filtrando por días BRUTOS, deliberadamente sin cambiar**: es un concepto distinto ("¿cuánto falta para la fecha de renovación en sí?", una decisión de cuánto mostrar) del tono/tramo ("¿cuánto falta para tener que decidir?"). El donut no tiene ventana (siempre son TODOS los vendors activos); la agenda sí, por diseño, desde el bloque anterior — no es parte de esta discrepancia y no se tocó.
- **Tests**: `aggregate.test.ts` — nuevo caso explícito pedido por el usuario (`groupRenewalTicketsByTier`: contrato a 40 días brutos con 35 de preaviso → Crítico, no Estable, con aserción de `actionableDaysUntil=5`); nuevo test de `buildRenewalTickets` confirmando que `tone`/`actionableDaysUntil` usan la fecha accionable, no la bruta; los 4 contratos del describe de bucketing existente ganaron `autoRenews: false` explícito (antes heredaban el default `true`/30d preaviso del helper `contract()`, lo que con el nuevo cálculo movía `upcoming-1` a Crítico y rompía la aserción — ese describe testea el bucketing en sí, no la interacción con preaviso, así que se aisló poniendo `autoRenews: false` en vez de recalcular los umbrales a mano).
- **Verificación de coherencia entre agenda y donut, contra los datos demo reales de la org `wowinx` (sin tocarlos, script ad-hoc con `service_role`, no comiteado)**: de 51 vendors activos, 38 tienen exactamente 1 contrato activo y caen dentro de la ventana de 120d de la agenda (universo comparable entre ambas vistas) — **0 discrepancias de tramo** en ese universo (antes del fix había 14 contratos cuyo tramo cambiaba entre el criterio bruto y el accionable). Los conteos globales de Crítico (25) y Próximo (8) coinciden EXACTOS entre agenda y donut; la diferencia en Estable (18 en el donut vs 5 en la agenda) se explica al 100% por los 13 vendors cuyo único contrato activo cae fuera de la ventana de 120d de la agenda (comportamiento preexistente de la ventana, no de este fix).
- **Verificación visual autenticada** (mismo patrón que sesiones anteriores: rotación temporal de contraseña de un usuario demo `finance`, login real vía `pnpm dev` contra el remoto, contraseña rotada a un valor aleatorio descartado tras la captura): confirmado en producción-remoto que **Notion (Demo)** aparece en la sección Crítico con "6 días" (sin nota de preaviso, porque 51d brutos > 45d de preaviso — cae en el caso general, no en `noticeWarning`), contrastando con las filas vecinas que sí muestran "X días · preaviso Yd".
- **Verificación**: `pnpm lint && pnpm typecheck && pnpm test && pnpm build` en verde local (`pnpm test`: 143 passed, únicamente las suites que ya requerían Supabase local fallan en esta máquina, sin relación con este cambio — mismo patrón documentado en bloques anteriores).
- **Commit `df8c8e4` pusheado a `main`; CI run `30937352982` falló en el job de e2e** — bug real encontrado por el propio CI, no visto en la verificación local (`pnpm test` no ejecuta e2e en esta máquina) ni en la verificación funcional/visual (que no usaba estos fixtures concretos): `e2e/dashboard.spec.ts` sembraba un vendor `VendorAmber` (26 días brutos, 14 de preaviso, auto-renueva) y aseraba el texto brutos `"26 días"` — con el fix, la fila ahora muestra correctamente `"12 días"` (26-14, la cifra accionable), así que el test fallaba por una expectativa desactualizada, no por una regresión. Se corrigió la aserción a `"12 días"` y, de paso, se ajustó `VendorNeutral` (70→80 días, con el mismo preaviso de 30 hardcodeado por el helper de este archivo) para que su comentario ("neutro") siga siendo cierto bajo el criterio accionable (70-30=40 caería en Próximo/ámbar; 80-30=50 se queda en Estable) — no estaba asertado en este test, pero dejarlo desalineado habría sido un comentario engañoso. Commit `f4cfa1b` con ambos fixes — **CI run `30938076863` en verde en ambos jobs**, confirmado con `gh run view` directo (no solo el exit code de `gh run watch`).
- Producción verificada tras el rollout de Railway: `/es`, `/es/login` → 200; `/es/dashboard`, `/es/renewals` → 307 (redirección a login, confirma rutas vivas sin 500). **Bloque cerrado en verde.**
